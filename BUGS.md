# Открытые ошибки

Список того, что найдено и ещё не исправлено. Исправленное отсюда уходит — в PROGRESS.md, с разбором.

## Зазор печатной петли: одна ручка, ЧЕТЫРЕ выражения и ДВА разных умолчания

`p.pipGap` читается четырьмя разными записями, и они не согласны между собой:

| место | пол | потолок | зашитое умолчание |
|---|---|---|---|
| `pipGapOf` (общее правило) | 0.15 | 0.8 | **0.35** |
| строка 13552 | 0.15 | 0.8 | 0.35 |
| строка 14143 | 0.15 | **1.2** | **0.30** |
| строка 19382 | 0.15 | 1.2 | 0.35 |

Потолок 1.2 против 0.8 может быть намеренным — у разных соединений разный допустимый люфт, и это
надо УСТАНОВИТЬ по коду, а не решить с ходу. А вот умолчание 0.30 против 0.35 у ОДНОЙ И ТОЙ ЖЕ ручки
защитить нечем.

ЧЕМ ЭТО ПАХНЕТ НА ДЕЛЕ — немногим: `defaultBoxParams()` несёт умолчание панели (0.35), и оно всегда
сильнее. Зашитая запаска достижима ровно одним путём — сохранённым JSON, в котором ключа нет: такой
файл соберёт петлю с зазором 0.30 в одном месте и 0.35 в трёх остальных. Найдено при разборе v25.35.0
и НЕ чинится в ней же: правка требует установить, намеренны ли два потолка, а это отдельная работа.

## Умолчание панели и умолчание, зашитое в код, — всегда две копии одного числа

Следствие того же разбора, но шире: у каждой ручки умолчание записано ДВАЖДЫ — строкой панели
(`default:`) и запаской в коде (`p.knob>0 ? p.knob : N`). Панельное всегда сильнее, поэтому
расхождение этих двух чисел НЕ ВИДНО НИ НА ОДНОМ наборе, кроме сохранённого файла без ключа. Сколько
таких расхождений в файле — не измерено: мой первый замер дал 61, но регулярное выражение оказалось
негодным (оно ловило посторонние числа в той же строке), и число это НЕ подтверждено. Требуется
честный замер разбором выражений, а не поиском по строке.
