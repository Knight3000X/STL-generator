# STL Generator — журнал прогресса

## v67 — фаска тыльной стороны листа + панель без «титров Звёздных войн»

**Фаска тыльной стороны листа.** Новый параметр `sheetChamfer` (ширина) + `sheetChamferAngle` (угол от
горизонтали, 10…80°, по умолч. 45°). Нижняя грань утапливается внутрь на ширину фаски (`botRing = polyInsetRing`),
скошенный поясок поднимается к полному контуру на высоту `rise = ширина·tan(угол)` (ограничена толщиной), а
вертикальная кромка идёт `chY…верх`. Совместимо со ВСЕМИ пресетами листа: сплошной / выпуклая грип-текстура /
сквозная решётка × прямоугольник/скруглённый/многоугольник/эллипс/SVG, вместе с бортиком и наклоном. Для решётки
дно вписывается в `botRing` (низ ← вписанный контур, верх ← полный), так что герметичность сохраняется. +19 тестов.

**UI — прогрессивное раскрытие вместо бесконечной прокрутки.** Панель раньше показывала все секции развёрнутыми
сразу (те самые «уползающие титры»). Теперь группы параметров свёрнуты по умолчанию — открыта только «Размеры»,
остальное сворачивается в короткий список кликабельных заголовков со стрелкой ▸; выбор «открыто/закрыто»
запоминается (localStorage). Тяжёлые верхние секции (Логотипы, Грани кости, Отверстия, Позиция, Поворот,
Конфигурация, Приложение) тоже свёрнуты по умолчанию (`data-def-closed`), а часто нужные (Модели, Параметры→Размеры,
Отображение, Сводка, Экспорт) — раскрыты. Итог: вместо стены полей — компактное меню разделов, разворачиваешь
только нужное. Инфраструктура сворачивания уже была — поменяли дефолты и добавили запоминание для внутренних групп.

## v66 — тот же rib-aware фикс для листа/грип-пада (текстура и сквозная решётка)

Та же проблема всплыла в режиме ЛИСТ: выпуклая грип-текстура давала рваные «тетрис»-фрагменты вместо узора
(соты/ромб/треугольник/точки), а сквозная решётка на устройстве не отрисовывалась. Причина: функции текстуры
листа (`buildSheetTexture`, `buildSheetBumps`) считали шаг сетки по `pitch/4` — БЕЗ учёта толщины ребра, поэтому
тонкие канавки/рёбра алиасились. (Сквозная решётка `buildSheetNet` уже перешла на rib-aware `netGridStep` в v65,
так что «сплошная пластина» на скрине — это закэшированный v64; после перезагрузки решётка рисуется.)

Фикс: обе функции текстуры листа теперь используют `netGridStep(maxDim, cell, rib, res)` — тот же единый rib-aware
шаг, что и стенка/дно контейнера. Проверено рендером: грип-пад даёт чистые выпуклые соты/ромбы с канавками (как
и задумано для «мышиного» грипа), сквозная решётка — ровные шестигранные отверстия. +1 регрессионный тест
(тонкое ребро даёт существенный объём бугров). Батарея зелёная.

## v65 — сетка стенки: сэмплинг учитывает ТОЛЩИНУ РЕБРА (тонкие рёбра больше не пропадают)

По новым скриншотам всех 4 узоров на стенке при шаге 2 / ребре **0.3** мм: соты — «ряды не связаны», квадрат —
«вообще не отрисовывается», треугольник — как ромб, ромб — как микрокресты. ASCII-дамп решётки стенки показал
**пустые ряды между полосами рёбер** — вертикальные соединяющие рёбра соты пропадали.

Причина: `netGridStep` резолвил только ШАГ ячейки (`cell/4`), но не ТОЛЩИНУ РЕБРА. При ребре 0.3 мм и шаге сетки
~0.33 мм ребро тоньше одной ячейки сетки → алиасится и пропадает, honeycomb рвётся на полосы. Дно выглядело
чисто лишь потому, что его `maxDim` не включает высоту → сетка была чуть мельче (0.22) и случайно ловила ребро.
Эмпирический порог (дамп): **ребро/шаг ≥ 1.3** — иначе рёбра рассыпаются.

Фикс: `netGridStep(maxDim, cell, rib, res)` теперь резолвит И ячейку, И ребро — `quality = min(cell/4, rib/1.3)`
(≥4 сэмпла на ячейку И ≥~1.3 сэмпла на ребро). Ползунок детализации может только уплотнять; нижняя граница
`maxDim/900` для производительности. Прописан во все 4 места (дно, стенка-`fineW`, маршрутизация squircle, лист).
Теперь все узоры (соты/квадрат/треугольник/ромб) рисуются чисто и СВЯЗНО на стенке при любой толщине ребра.

Цена: тонкое ребро на крупной корзине = много треугольников (пример: 110×180, шаг 2, ребро 0.3, соты → ~2.2 млн
tris, watertight). Это неизбежно для 0.3-мм honeycomb; чтобы легче — увеличить ребро/шаг. +4 регрессионных теста
(netGridStep резолвит ребро 0.3/0.6/1.0). Батарея зелёная.

## v64 — squircle-сетка: стенка тайлится бесшовно + дно на всю поверхность

Продолжение по новым скриншотам (110×180, наклон −10%, squircle 100%, соты, шаг 2, res 500): на стенке
**«ряды не связаны»** и соты не рисуются даже на детализации 500, а на **дне сетка не по всей поверхности**
(только центральный квадрат + сплошное кольцо). Диагностика (ASCII-дамп решётки стенки vs дна) показала: дно
рисуется идеально, а стенка — нет, при одинаковом `latticeSolidAt`.

**Стенка — фаза дрейфует.** Стенка сэмплирует узор по длине дуги `uMid[i]`, чья фаза относительно шага соты
несоизмерима с окружностью → медленно уходит, а порог hex гиперчувствителен у рёбер → узор рассыпается. Дно
чисто, потому что его равномерная сетка точно периодична (одна фаза в каждой ячейке). Фикс: **соизмеримая
сетка стенки** — кольцо ресэмплится в `N = m·K` точек равной дуги (`resampleRingByArc`), где `K` = целое число
периодов узора по периметру, `m ≥ 4` столбцов на ячейку. Тогда фаза одинакова в каждом периоде и узор
бесшовно тайлит кривую стенку — соты/ромбы рисуются чисто, как на плоском дне. `emitPolyWallNet` получил
параметр `colsPerPitch` (сэмпл в точках `(i+0.5)·pitch/m`).

**Дно — на всю поверхность.** `buildPolyLatticeFloor` переписан: декартова сетка узора КЛИПается по внутреннему
кольцу (гомотетия к центроиду — не самопересекается на острых углах низкого squircle, в отличие от параллельного
инсета), ступенчатый край region'а сшивается со сглаженным кольцом (`stitchAnnulusArc` — выравнивание по ДОЛЕ
ДУГИ, а не по углу, поэтому watertight даже когда ступеньки ломают монотонность угла). Дырки заполняют весь
круг, остаётся лишь тонкий сплошной кант у стенки. Диагональные «пинчи» в маске и в узоре растворяются →
одна простая граница, водонепроницаемо.

Итог: стенка и дно рендерятся чистым ровным узором на любой форме (круг/эллипс/низкий squircle), watertight.
+3 регрессионных теста (соизмеримость, тонкая ячейка, полное покрытие дна). Батарея зелёная. Примечание:
благодаря соизмеримой сетке высокая «детализация» больше не нужна для чистоты — можно ставить умеренную.

## v63 — squircle-сетка больше не «рваная»: чистый ровный узор на стенках и дне

Пользователь показал 4 скриншота: у квадратного бина (squircle 0%) сетка идёт **чистыми ровными сотами**, а у
круглого (squircle 100%) та же сетка **разваливалась в рваные вертикальные обломки** на стенке и «тетрис»-осколки
на дне. Причина — **алиасинг**: кольцевая net-машинерия сэмплировала узор на слишком грубой сетке.
- Вертикальные ряды стенки считались как `M = (hh−floorY)/max(cell/2, fineW)` — доминировал `cell/2`, оставляя
  ~2 ряда на ячейку → рёбра лесенкой рвались по вертикали.
- `fineW` (плотность кольца = горизонтальные столбцы) и шаг сетки дна не были привязаны к шагу ячейки, поэтому
  падали ниже ~3 сэмплов на ячейку.

Фикс: единый помощник **`netGridStep(maxDim, cell, res)`** — гарантирует **≥4 сэмпла на ячейку** (шаг ≤ pitch/4,
жёсткая граница против алиасинга), ползунок «детализация» может делать только ЧАЩЕ (не грубее), плюс нижняя
граница `maxDim/600` для производительности. Прописан в: маршрутизации squircle (`fineW`), `buildPolyHollowWallLattice`
(и `fineW`, и `M = round((hh−floorY)/fineW)` — вертикаль теперь равна горизонтали, изотропные ячейки),
`buildPolyLatticeFloor` и `buildSheetNet` (та же грубость мешала перфорации листа/грипа на круге). Результат: стенка
и дно рендерятся ровным регулярным узором (как эталонные скрины 1 и 3), а не обломками. Треугольников примерно вдвое
больше (плата за чистоту), модель остаётся watertight. +6 anti-alias тестов (проверяют `netGridStep` и число
вертикальных рядов стенки). Батарея: 1441 passed, 0 failed.

## Squircle: полная совместимость с сетчатыми стенками и дном (чистая круглая корзина)

По жалобе на «рваную» сетку и ошибки слайсера у бина: **squircle-контейнер теперь поддерживает сетку на
стенках и дне** во всех горизонтальных вариациях (squircle % от квадрата до эллипса, W≠D). Когда запрошена
`latticeWalls`/`latticeFloor`, squircle строится через ту же кольцевую net-машинерию (`buildPolyHollowWallLattice`
/ `buildPolyHollow`) с плотным кольцом суперэллипса (`superellipseRingDense`). Кольцо ВЫПУКЛОЕ и простое,
поэтому инсет/перфорация не самопересекаются (в отличие от углов rounded-rect бина) → **нет ошибок слайсера**;
и форма гладко-круглая (не гранёная), а не «рваная». Стенки прямые (плоское дно; сетка перекрывает
squircleVBot-скругление низа). С наклоном −8…−10° → аккуратная круглая расширяющаяся корзина. Заголовок
показывает «squircle / круглый (N%)».

`test_poly.js` +14 (squircle % ×4, все 4 узора стен, только-дно, только-стены, эллипс W≠D, расширяющаяся
корзина, удаление материала, габарит). Кэш SW → `stl-gen-v62`, версия v62.

## Грип: текстура на всю форму + рецепт круглой сетчатой корзины

- **Грип-текстура на ВСЮ форму** (по референсу): вместо текстуры в центральном вписанном прямоугольнике —
  теперь сплошная гладкая подложка (контур экструдирован, гладкая кромка) + приваренный слой бугорков
  (`buildSheetBumps`) по ВСЕЙ форме (клип по контуру, с зазором под окантовку), одним замкнутым
  экструдированным силуэтом (шапки + стенки клеток + рассасывание щипков → водонепроницаемо). Окантовка
  (`sheetRim`) — приподнятая полоса по контуру — работает и на SVG-форме. Сквозные отверстия по-прежнему
  в центральном прямоугольнике (дырки нельзя «приварить» на всю форму гладко).
- **Круглая сетчатая корзина** (скрины 3+4) — достигается имеющимися параметрами (кода не потребовалось):
  квадратный **бин** с `binRound` ≈ ширина/2 + `Сегменты скругления` 12–16 → круг; `Наклон` −8…−10° по всем
  сторонам → расширение кверху (корзина); полый + `Сетка на стенках` = «все 4» + `Сетчатое дно` + узор
  соты/ромб. Проверено: круглая расширяющаяся корзина с сотовой сеткой на стенках и дне — водонепроницаема.

`test_sheet.js` 67→68 (покрытие текстурой всего диска), `test_poly.js` +2 (круглая корзина + расширение).
Кэш SW → `stl-gen-v61`, версия v61.

## Лист: произвольный контур из SVG

Форма листа/грип-пада теперь может быть **произвольной — из SVG** (`sheetShape:'svg'`). Кнопка «⤓ SVG-контур
листа / грип-пада» в разделе «Модели» переиспользует `loadSvgHoleFile` (силуэт заливкой → полярная таблица
радиусов r(θ), 360 отсчётов, звёздная относительно центроида). `sheetOutlineRing` строит из неё кольцо,
растянутое в Ширину×Глубину (подсэмпл до ~180 точек). Таблица хранится в params (`sheetSvgRU/ApU/AqU`),
поэтому едет с моделью, в undo и в JSON-конфиг. Работает со всеми режимами листа (текстура/отверстия/гладкий),
бортиком и наклоном. Так боковую накладку конкретной мыши можно обвести в SVG и получить грип точной формы.
`test_sheet.js` 59→67 (+SVG: solid/texture/through/rim, заполнение габарита, фолбэк без таблицы). Кэш SW → v60.

## Уточнение по референсам: студы на вершинах кости + выпуклая текстура на листе

- **Кость (как ornate-D20 на фото)**: к рамке по граням добавлены декоративные СТУДЫ на вершинах — мини-сферы
  (`buildStudSphere`) в каждой уникальной вершине, приваренные как замкнутые оболочки (половина торчит,
  половина в теле). Включаются вместе с `dieBead` (радиус ~1.15·bead), плотные кости (D100) пропускаются.
  Теперь кость выглядит как ювелирная: номер в рамке + студы на углах.
- **Лист = грип-пад, а не решётка**: добавлен режим `sheetCut` — «выпуклая текстура» (по умолчанию),
  «сквозные отверстия», «гладкий». В режиме текстуры узор (ромб/квадрат/треуг./соты/**полосы**/**точки**)
  даёт ВЫПУКЛЫЙ рельеф на СПЛОШНОЙ подложке (грип, как боковая накладка мыши), а не дырки: центральный
  прямоугольник — двухуровневая клеточная решётка (`buildSheetTexture`, выпуклые клетки + канавки-рёбра,
  с рассасыванием шахматных «щипков»), высота `sheetTexH`; вокруг — гладкая рамка и опциональный бортик.
  Прямоугольник покрывается почти целиком; у круга/многоугольника текстура в центральном прямоугольнике +
  гладкая рамка (как на боковой накладке).

`test_dice.js` 62 (+студы в существующих проверках валика), `test_sheet.js` 31→59 (текстура×контуры×узоры,
+объём/высота/наклон/бортик). Кэш SW → `stl-gen-v59`, версия v59.

## Кости: приподнятый скруглённый бортик-рамка вокруг граней + плоский перфорированный лист

Две идеи одним заходом:
- **Валик на гранях кости** (`dieBead`, мм, 0=выкл): по контуру каждой грани — приподнятая скруглённая
  рамка. Строится как замкнутый скруглённый «жгут» (тор), пронесённый вдоль вписанного контура грани
  (`buildDieBeadTube`): половина выступает над гранью, половина утоплена в тело. Приваривается к кости как
  перегородка (замкнутый тор ∩ замкнутое тело → манифолд, слайсер объединяет), поэтому чёткий патч номера
  и острые рёбра не трогаются. Мелкие грани (D100) авто-пропускаются (нет места). Сегменты сечения зависят
  от радиуса. Смотрится как премиум-кость (номер в рамке).
- **Плоский лист / панель** (новая группа «Лист / панель», `sheetShape` перекрывает всё): плоская пластина
  произвольного контура — прямоугольник / скруглённый прямоугольник / многоугольник (`sheetN`) / круг-эллипс,
  толщина `sheetThick`, сквозной узор насечек (`sheetPattern`: ромб/квадрат/треугольники/соты, параметры из
  «Сетки»), и опциональный **бортик** по краю (`sheetRim`/`sheetRimW`). Переиспользует машинерию дна-сетки:
  `emitLatticeFloor` — перфорированный центр (двусторонняя плита), `polyStitchAnnulus` — гладкая сплошная
  рамка до контура на обеих сторонах, контур экструдируется в кромку; бортик — замкнутая приставная полоса.
  Водонепроницаемо. Для решёток/подставок/дренажных лотков/костеров. Прямоугольник перфорируется почти
  полностью; у круга/многоугольника перфорация в центральном вписанном прямоугольнике + сплошная рамка.

`test_dice.js` +8 (валик: 4 кости, +объём, +номер, +наклон, D100-пропуск) = 62; `test_sheet.js` — 31
(контуры×узоры, размеры, бортик, гейтинг). Кэш SW → `stl-gen-v58`, версия v58.

## Импорт STL/OBJ + масштабирование (основа для грипов по скану)

Кнопка «⤓ Импорт STL / OBJ» в разделе «Модели»: загружает готовую 3D-модель отдельной моделью сборки —
например скан корпуса мыши для будущих грипов, или чужой STL, чтобы разрезать его нашим слайсером.
- **Парсеры** (чистые, тестируются): `parseSTLBinary` (80-байт хедер + uint32 + 50 байт/треуг.),
  `parseSTLText` (regex по `vertex`), `parseOBJ` (v/f, полигоны веером, отрицательные индексы, токены v/vt/vn);
  `parseSTL` авто-детектит binary vs ASCII по точному размеру (84 + 50·n). `parseMeshFile` — по расширению.
- **Модель импорта** как замороженный меш: оригинальные треугольники лежат в `importedMeshes` по `importId`,
  в params модели — `importId` + режим масштаба; `buildTrisForShape` возвращает `scaleImportedMesh(orig, p)`,
  так что скан можно пере-масштабировать вживую. Центрируется на начале координат.
- **Масштаб** (группа «Импорт STL/OBJ»): «равномерно × процент» (сохраняет форму — для юнит-коррекции скана,
  на импорте Ш/В/Г = натуральный габарит, 100% = как есть) ИЛИ «вписать в Ш×В×Г» (растянуть по осям — когда
  известны все три реальных размера, напр. Naga Pro 119.9×43×74.5 мм). Лимит 1.2 млн треугольников, битые/
  нечисловые треугольники отбрасываются. Не требует водонепроницаемости (скан) — для превью/экспорта/будущего
  рейкаста грипов это ок; для разрезки нужен замкнутый меш.

Следующий шаг (когда будет модель корпуса): «грип по поверхности» — окно проекции → карта высот → TPU-пад.
`test_import.js` — 18 проверок (парсеры binary/ASCII/OBJ, габариты, вписывание, равномерный %, конвейер,
регрессия куба). Кэш SW → `stl-gen-v57`, версия v57.

## Кейкапы: чаша пресетов = 0, легенда повёрнута на 180°

- Во всех готовых профилях (Cherry/OEM/SA/MDA/XDA/DSA) глубина чаши теперь 0 (плоский верх с наклоном
  ряда); чаша осталась доступной в «своём» профиле через параметр «Чаша».
- Легенда на верхней площадке повёрнута на 180° (обе оси выборки heightmap инвертированы) — верх символа
  смотрит к заднему краю кейкапа. Кэш SW → `stl-gen-v56`, версия v56.

## Кейкапы: выпадайка профилей (Cherry / OEM / SA / MDA / XDA / DSA) + ряды R1–R4

Новый выбор «Кейкап: профиль»: **Cherry, OEM, SA, MDA** (скульптурные — форма зависит от ряда клавиатуры,
селект «Ряд» R1…R4) и **XDA, DSA** (плоские — ряд игнорируется); вариант «свой» оставляет ручные параметры
(высота/сужение/чаша), как раньше. Профиль задаёт: высоту по ряду, **наклон верхней площадки** (новое в
геометрии: верхняя плоскость y = H + (z−zc)·tan(угла) — наклон едет через верхнее кольцо лофта, сшивку и
клеточную решётку с легендой; низ площадки остаётся плоским и опускается так, чтобы под наклонённым верхом
всегда оставалась полная толщина), глубину чаши и сужение. Таблица `KEYCAP_PROFILES` — приближения
общепринятых стандартов (напр. SA R1: 14.9 мм, −13°; DSA: 7.6 мм, 0°). Легенда/AMS-режимы/стемы работают
со всеми профилями. `test_keycap.js` 23 → **46** (18 профиль×ряд водонепроницаемы + наклон R1 вперёд,
SA выше DSA, плоские игнорируют ряд, профиль+shell/core). Кэш SW → `stl-gen-v55`, версия v55.

## Кейкапы: параметрические, с легендой и двухцветным AMS-режимом (shine-through)

Новая группа «Кейкап» (перекрывает куб/бин/кость): параметрический кейкап — скруглённо-прямоугольное
основание 18.2×18.2 мм на юнит (`keySizeU` 0.25…7u), лофт к меньшей верхней площадке (сужение на сторону),
полая юбка снизу, чаша (сферическое углубление верха), стем **Cherry MX** (Ø5.5 с крестовым отверстием,
допуск посадки настраивается) или **Kailh Choc** (2 ножки) — оба через экструзию силуэта, водонепроницаемо.
**Легенда** берётся из ПЕРВОЙ карточки логотипов («+ Текст» / картинка / QR — весь конвейер) и кладётся на
верхнюю площадку клеточной решёткой с вертикальными стенками (резкие края): положительная глубина —
гравировка, отрицательная — рельеф наружу. Смещение/размер/поворот — из карточки логотипа.

**Двухцветная печать под AMS (до 4 филаментов):** режимы
- «AMS: внешний» — корпус кейкапа, легенда — СКВОЗНЫЕ окна в верхней площадке;
- «AMS: внутренний» — белая вставка: плита-основание + пробки-легенды, выходящие заподлицо с верхом.
Рецепт: модель 1 = внешний (чёрный), «+ Добавить модель» → модель 2 = внутренний (белый), одна и та же
карточка легенды; экспорт 3MF — два объекта, в слайсере (Bambu Studio и т.п.) каждому назначается свой
филамент AMS. Подсветка светит сквозь белые пробки.

**TPU грип-пады** — уже собираются из имеющегося (рецепт, без нового кода): куб Ш×Г×1.6 мм +
«Скругление вертикальных рёбер» (fillet) + «+ Текстура» (соты/точки/рифление) на грань +Y; печать TPU,
пад гнётся и клеится на любую мышь. Точные накладки под конкретные модели мышей без 3D-модели корпуса
параметрически не построить (честное ограничение).

`test_keycap.js` — 23 проверки (размеры u, MX/Choc/без стема, чаша, легенда гравировкой и рельефом,
сквозная легенда shell + пробки core, объёмные санити, гейтинг организатора, приоритет над кубом/костью,
регрессия куба). Кэш SW → `stl-gen-v54`, версия в шапке v54.

## Версия генератора в заголовке

Рядом с «STL-ГЕНЕРАТОР» теперь выводится версия сборки (`APP_VERSION`, моноширинным мелким шрифтом:
«STL-ГЕНЕРАТОР v53») — сразу видно, какая сборка открыта и не залипла ли она в кэше PWA. Константа в
начале основного скрипта; поднимать ВМЕСТЕ с `CACHE_VERSION` в sw.js при каждом релизе (в обоих местах
комментарии-напоминания). Кэш SW → `stl-gen-v53`.

## Кости: чёткий патч и на D4 + заводская ориентация цифр

- **D4 — клеточный патч**: грань с тремя угловыми номерами разбивается на 3 угловых квада (центроид +
  середины рёбер; глобальное k чётное → середины лежат на общих точках подразбиения), каждый получает свой
  чёткий клеточный патч + угловую сшивку — та же резкость, что и у одиночных номеров. Номера D4 чуть
  ужаты (sizeFrac 0.26, офсет 0.48r), чтобы патч гарантированно вписывался в квад; при вылете — фолбэк на
  радиальную сетку. Проверено: путь реально задействуется (198k против 155k фолбэка), водонепроницаемо.
- **Заводская ориентация номеров** (`dieFactoryRotation`, вместо «низом к углу»): как на фабричных костях —
  цифра СТОИТ НА РЕБРЕ (основание параллельно ребру грани; у треугольных/пятиугольных граней вершина
  сверху — классика d8/d12/d20), а на D10 — вдоль оси «кайта» верхом к полюсу. D4 не трогаем (апекс-нумерация
  по углам). Применяется в «Авто-номерах» и при ручном добавлении; поворот в карточке редактируем.
Кэш SW → `stl-gen-v52`.

## Кости: по-настоящему чёткий рельеф (клеточный патч вместо смещаемой сетки)

Смещаемая радиальная сетка давала «рваные» края при любом числе колец (шаг ~0.5 мм). Теперь грань с ОДНИМ
рельефом (номера всех костей кроме D4, логотипы, текст) строится иначе (`buildDieGlyphPatch`): под рисунком —
мелкая клеточная решётка (как у сетчатого дна/экструзии логотипа): заполненные клетки на ±глубине, пустые в
плоскости грани, ВЕРТИКАЛЬНЫЕ стенки ровно по границам клеток → резкость края = шаг клетки (~0.09 мм при
детализации 400), не зависит от тесселяции грани. Патч обрезан по bbox рисунка (+3 клетки) и вшит в плоскую
грань одной угловой сшивкой (`polyStitchAnnulus` научился явному центру — кольца звёздны относительно центра
патча). Работает и выдавливание, и гравировка; шахматные «щипки» рассасываются как в экструзии логотипа.
Кольцо грани по-прежнему делится глобальным k → швы с соседними гранями совпадают, водонепроницаемо.
Фолбэк на радиальную сетку: рисунок вылезает за грань или граней с несколькими рельефами (углы D4 — там
плотность радиальной сетки поднята, ×1.2). test_dice 54 — все пути перекрыты. Кэш SW → `stl-gen-v51`.

Плюс классическая ориентация: номера/лого на гранях (кроме D4) по умолчанию повёрнуты НИЗОМ к одному из
углов грани (`dieCornerRotation` — детерминированно к первой вершине полигона), как на настоящих костях;
и в «Авто-номерах», и при ручном добавлении на грань. Поворот в карточке остаётся редактируемым.

## Бин со скруглёнными углами + чётче рельеф на гранях костей

Две доработки:
- **«Бин» — контейнер с полногабаритным скруглённым прямоугольником в основании** (как gridfinity-бин):
  новый параметр `binRound` (группа «Многогранник») — радиус скругления вертикальных углов; след заполняет
  ВСЮ ширину×глубину (в отличие от вписанного N-угольника). Новое кольцо `ringRoundedRect(Dense)` идёт через
  всю кольцевую машинерию контейнера без изменений: полый, сетчатое дно, **замкнутая сетка на стенках** (4
  узора, без вертикального шва), перегородки, наклон, разрезка, крышка. Логотипы/порты на скруглённой стенке
  не применяются (гейт). Заголовок — «бин (скруглённые углы)». `buildPolyHollowWallLattice` принял
  опциональный готовый плотный ринг; `buildPolyDividers` строит хорды по прямоугольному рингу.
- **Детализация граней костей**: радиальная сетка грани теперь сгущается к центру (75 % колец в
  центральных 82 % радиуса — там, где рисунок), пределы подняты (рёбра до 96 сегментов, до 120 колец,
  максимум «Детализации граней» реально работает) — эффективный шаг под цифрой ~вдвое мельче при том же
  бюджете треугольников; D100 ужимается сильнее (×0.4), чтобы не взрываться.

`test_poly.js` +12 проверок (бин: габарит, полый, сетки стен/дна по узорам, перегородки, наклон, гейтинг),
всего 80. `test_dice.js` — 54, без регрессий. Кэш SW → `stl-gen-v50`.

## Кости: правильная раскладка номеров (сумма противоположных = N+1) + апекс-нумерация D4

По стандартным конвенциям игральных костей «Авто-номера» теперь раскладывает числа правильно:
- **D6/D8/D10/D12/D20:** противоположные грани в сумме дают **N+1** (D6→7, D8→9, D12→13, D20→21; D10 —
  0…9, сумма 9). Это «балансная» конвенция честности. Грани парятся по ближайшему антиподу центроида
  (`dieOppositeSumNumbering`); центрально-симметричные кости имеют точную противоположную грань для каждой.
- **D4 (тетраэдр) — апекс-нумерация:** кость падает вершиной вверх, поэтому результат читается у ВЕРХНЕГО
  острия. Каждая грань несёт ТРИ числа (по углам), и в общей вершине одно и то же число стоит на всех трёх
  гранях. Нумеруем 4 вершины 1…4 и наносим номер вершины в угол каждой смежной грани, повёрнутым так, чтобы
  цифра читалась прямо, когда эта вершина — апекс (`autoNumberD4`). Наборы граней: {1,2,3},{1,2,4},{1,3,4},{2,3,4}.
- **D100** (Zocchihedron) — последовательно 1…100 (не имеет чистых антиподов).

Инфраструктура: `dieReliefDispFn` получил смещение `offU/offV` (доля радиуса грани) для угловых номеров;
`buildDie` теперь допускает НЕСКОЛЬКО рельефов на грань (объединённая dispFn) — нужно для 3 чисел на грани D4.
Зеркалирование выборки исправлено ранее, поэтому цифры не отражаются при любой ориентации. Кэш SW → `stl-gen-v49`.

## Кости: исправлено зеркалирование цифр + отдельная детализация граней

- **Зеркалирование:** базис грани (u,v,n) левосторонний (v=cross(u,n) → u×v=−n), поэтому при взгляде
  снаружи цифры отражались горизонтально. Исправлено флипом горизонтальной выборки heightmap
  (`u01 → 1−u01`) в `dieReliefDispFn` — цифры читаются правильно на всех гранях.
- **Отдельная детализация:** добавлен глобал `dieResolution` (по умолчанию 200, диапазон 40…400) и поле
  «Детализация граней» в подменю кости — отдельно от «Детализации логотипов», чтобы поднимать чёткость
  цифр, не раздувая сетку логотипов на других формах. Плотность радиальной сетки грани (`k`, `R`) масштабируется
  от неё; на многогранных костях (D100/иконки ≥24 граней) автоматически ужимается, чтобы не взрывать число
  треугольников. Кэш SW → `stl-gen-v48`.

## Кости: подчёркивание неоднозначных цифр (6/9 и пары на D100)

«Авто-номера» теперь подчёркивает снизу цифры, которые при повороте на 180° читаются как ДРУГОЕ число
из набора кости — 6↔9 на всех костях, а на D100 ещё и пары вроде 16↔91, 18↔81, 66↔99, 68↔89, 86↔98.
Цифры вроде 2/3/4/5/7 (невалидны вверх ногами) и «палиндромы поворота» 69/96/88/11 не подчёркиваются.
`makeTextLogoResult` получил параметр `underline` (рисует брусок под текстом в тот же heightmap, так что
подчёркивание выдавливается/гравируется вместе с цифрой). Логика — `dieAmbiguousLabels`. Кэш SW → `stl-gen-v47`.

## Логотипы/текст/номера — по умолчанию утоплены на 0,2 мм

Глубина по умолчанию для всех новых логотипов, текста, QR, текстур (обычные грани) и номеров/лого на
гранях кости теперь **−0,2 мм** (утоплено/гравировка) вместо выпуклого рельефа. Меняются только дефолты
при добавлении — существующие значения и сохранённые конфиги не трогаются (fallback при импорте тоже
−0,2). Кэш SW → `stl-gen-v46`.

## Кости: гравировка символов (отрицательная глубина) + сферический D100 (Zocchihedron)

Рельеф по граням кости переписан со «приваренного выпуклого тела» на **смещение самой поверхности грани**:
каждая грань тесселируется радиальной сеткой (центроид → граница), вершины смещаются вдоль нормали по
heightmap-силуэту. **Положительная глубина выдавливает символы наружу, отрицательная — гравирует их внутрь**
(раньше отрицательная глубина зажималась в 0 и символы всегда были выпуклыми). Водонепроницаемость держится
общим делением рёбер: каждое ребро кости делится на одинаковое число сегментов у обеих смежных граней, а
внешнее кольцо несёт нулевое смещение, поэтому швы совпадают. Незанятые грани тоже делятся тем же шагом
(веер из центроида), чтобы совпадать с гравированными соседями. Глубина зажимается, чтобы гравировка не
пробила кость насквозь. Поле «Глубина» в карточке грани теперь принимает отрицательные значения.

`test_dice.js` — +11 проверок (гравировка отрицательной глубиной водонепроницаема и убирает материал
на D6/D8/D12/D20/D100; выдавливание добавляет материал). Кэш SW → `stl-gen-v45`.

## Игральные кости (дайсы): D4/D6/D8/D10/D12/D20/D100 + рельеф номеров/логотипов по граням

Правильные многогранники переименованы в игральные кости и расширены до полного набора дайсов D&D:
- **D4** — тетраэдр (4), **D6** — куб (6), **D8** — октаэдр (8), **D12** — додекаэдр (12), **D20** —
  икосаэдр (20) (пять Платоновых тел).
- **D10** — пятиугольный трапецоэдр (10 kite-граней); высота апекса решается бисекцией на плоскостность
  граней — настоящий трапецоэдр.
- **D100** — **Zocchihedron**: почти сферическое тело со 100 плоскими фасетками (как реальный d100-«шар»).
  Строится как пересечение 100 полупространств `{x·nᵢ ≤ 1}` по направлениям Фибоначчи (каждая плоскость
  касается единичной сферы → тело обнимает сферу, каждая грань — многоугольник, ровно 100 штук). Каждая
  грань находится клиппингом квадрата в касательной плоскости против всех остальных (2D-пересечение
  полуплоскостей); общие вершины (тройка плоскостей) совпадают между гранями и свариваются → водонепроницаемо.
  Масштабируется по осям в width×height×depth.

Каждая кость теперь описывается **полигональной** сеткой `{verts, polys}` (не пред-триангулированной),
поэтому у каждой грани есть центроид, внешняя нормаль и базис u/v в плоскости — ровно то, что нужно для
рельефа по граням. `buildDieBody` веерит полигоны из центроида каждой грани (каждое ребро полигона делят
ровно две грани → водонепроницаемо).

**Отдельное подменю «Грани кости (номера / логотипы)»** — появляется, когда выбрана кость. Раскладывает
рельеф (номер, текст или логотип-картинку) по ОТДЕЛЬНЫМ граням. Кнопка **«Авто-номера 1…N»** нумерует все
грани (D10 → 0…9); «+ На грань» кладёт текст, «+ Картинка» — логотип на следующую свободную грань; у каждой
записи — грань, размер %, глубина, поворот, инверсия. Рельеф выдавливается **отдельным замкнутым телом**
(силуэт по порогу, как «3D-логотип»), пристыкованным к кости в плоскости грани — водонепроницаемо по
построению, без CSG; слайсер объединяет тела при печати. Всё переиспользует heightmap-конвейер логотипов
(`makeTextLogoResult` / `loadLogoImageFile` / QR), ездит с моделью, в undo и в JSON-конфиг сборки.

`test_dice.js` — 35 проверок (тела всех 7 костей + число граней, заполнение габарита по осям, kite-грани
D10, 100 фасеток + сферичность D100-Zocchihedron, рельеф на 1/всех гранях каждой кости, наклон, инверсия,
регрессия куба, разрезка кости). Батарея **1248, all green**. Живые рендеры: D20/D12/D10/D6/D100 (сфера)
с авто-номерами — все водонепроницаемы. Кэш SW → `stl-gen-v44`.

## Многогранник (часть 1): N-угольная призма + Платоновы тела

Новая форма — **многогранник** (группа «Многогранник»). Два семейства:
- **N-угольная призма** (`polyN` ≥ 3): правильный N-угольник, вписанный в ширину×глубину (width≠depth →
  «эллиптический», сплюснутый), выдавленный по высоте. Плоские боковые грани, плоские верх/низ (печатается
  плоским дном). `polyRound` — скругление вертикальных рёбер (дуга в углах кольца). Сплошной ИЛИ полый
  контейнер (стенки+дно+борт-рамка сверху, толщина стенки как у куба). Внутреннее кольцо — митровый
  офсет внешнего внутрь на толщину стенки (совпадает по числу точек → рамка сходится).
- **Платоновы тела** (`platonic`): тетраэдр/октаэдр/додекаэдр/икосаэдр, вписанные в габарит (перекрывают
  призму). Известные вершины/грани, грани-пятиугольники додекаэдра веерятся в треугольники.

Всё водонепроницаемо по построению; выдавливание кольца — как у squircle-призмы. **Наклон/сужение**
(taper, с сохранением высоты) применяется тем же пофрагментным отображением; **разрезка на фрагменты**,
**крышка**, экспорт STL/3MF и сборка моделей работают на уровне сетки — «из коробки». Прямоугольно-
зависимые организатор-фичи (скат, грип, уши, gridfinity, перегородки, ножки, борт-лоток) отключаются на
многограннике. Заголовок «форма: …» теперь отражает тип (`6-гранная призма`, `додекаэдр`…).
`test_poly.js` — 19 проверок (водонепроницаемость призм/полых/скруглённых/Платоновых, taper+высота,
гейтинг организатор-фич, регрессия куба polyN=0, разрезка многогранника). Кэш SW → `stl-gen-v38`.
Батарея 1145 → 1164.

## Многогранник (часть 2a): логотипы и отверстия на гранях

Логотипы-рельеф и сквозные отверстия/порты теперь работают на боковых гранях N-угольной призмы (сплошной
и полой), БЕЗ нового UI по граням: переиспользуется squircle-стенка (`squircleWall` + `applySurfaceLogo`).
Кольцо — единственный замкнутый контур, поэтому рельеф ложится как высотное поле на замкнутую сетку —
водонепроницаемо по построению; логотип, положенный на +X/−X/+Z/−Z, ложится на грань, смотрящую в ту
сторону (выбор по доминантной оси нормали). Грань предварительно уплотняется (`polyRingDense` — деление
рёбер до `fineW`), чтобы плоская грань несла чёткий рельеф. Порт (сквозное окно) пробивает одну грань —
переиспользуется ring-generic порт-машинерия squircle (`squirclePreparePort`/`squirclePortArc`), тюбель
между внешней и внутренней стенкой. Крышки/дно пока плоские (лого на торцах не наносится). `test_poly.js`
+10 проверок (лого на 4/6/8/12-гранниках сплошных и полых, лого+наклон, Ø8-порт сквозь грань). Кэш SW →
`stl-gen-v39`. Батарея 1164 → 1174.

## Многогранник (часть 2b): сетчатое дно

Сквозная сетка на дне полого многогранника. Конструкция: сетчатый ПРЯМОУГОЛЬНИК по центру (наибольший
вписанный в многоугольник, `polyInscribedRect` — бинарный поиск по масштабу с проверкой углов внутри) +
сплошное КОЛЬЦО от прямоугольника до стенок. Прямоугольник перфорируется существующим `emitLatticeFloor`
(та же сетка/узоры, что у куба — ромб/квадрат/треуг./соты; передаю `xP=xMid` → без внешнего кольца
куба). Кольцо (многоугольник−прямоугольник) триангулируется двух-контурной УГЛОВОЙ СШИВКОЙ
(`polyStitchAnnulus`): оба контура выпуклы и соосны, поэтому сортировка по углу вокруг центра + проход по
обоим с шагом к меньшему углу даёт водонепроницаемую неперекрывающуюся ленту (устойчивее ушного
отсечения на осевыгодных дырках). Прямоугольная сетка и внутренний край кольца делят одни узлы →
водонепроницаемо. Слишком маленький контейнер (прямоугольник < ячейки) → откат на сплошное дно.
Совместимо со скруглением рёбер, наклоном, боковыми логотипами. `test_poly.js` +12 проверок. Кэш SW →
`stl-gen-v40`. Батарея 1174 → 1186.

## Многогранник (часть 2c): перегородки

Внутренние перегородки на полом многограннике: тонкие осевые стенки на линиях сетки divX×divZ, каждая
тянется по хорде многоугольника на своей линии и вгрызается в стенки — приставные перекрывающиеся
замкнутые бокс-оболочки (сварка + объединение слайсером), та же конструкция, что у куба. Хорда считается
пересечением вертикальной/горизонтальной линии с внутренним кольцом (`polyChordZ`/`polyChordX`), с обеих
сторон толщины стенки берётся более узкий промежуток + embed вглубь стенки. Слишком узкие у края
пропускаются. Высота — % высоты полости. Совместимо с любым N, скруглением рёбер, сетчатым дном. Наклон
пропускает перегородки (как у куба — они осевые). `test_poly.js` +14 проверок. Кэш SW → `stl-gen-v41`.
Батарея 1186 → 1200.

## Многогранник (часть 2d): сетка на боковых гранях — БЛОК ЗАВЕРШЁН

Сквозная сетка на боковых стенках N-угольного контейнера — кольцевой аналог сетчатого дна
(`emitPolyWallNet`). Перфорируется пояс floorY..hh между внешним кольцом O и внутренним I (совпадают по
числу точек); домен — (периметр u вокруг кольца, высота v). Кольцо ЗАМКНУТО (индекс i периодичен) →
нет вертикального шва; верхний/нижний ряды сплошные → пояс сходится с кантом сверху и сплошной базой
снизу. Сплошная ячейка → внешняя+внутренняя грани; дырка → радиальные рёбра-тюбели по краям. Ниже
floorY — сплошная база (внешняя стенка −hh..floorY), затем перфорпояс, дно (сплошное веером ИЛИ своя
сетка), кант. Удаление «щипков» (i периодичен). Совместимо с любым N, скруглением, наклоном,
перегородками и сетчатым ДНОМ одновременно. Порт/лого на перфостенке не наносятся (сетка перекрывает).
`test_poly.js` +13 проверок. Кэш SW → `stl-gen-v42`. Батарея 1200 → 1213.

**Многогранник — блок «все функции куба» завершён:** N-угольная призма (3…64) + Платоновы тела; полый
контейнер; наклон (с сохранением высоты) + скругление рёбер; логотипы и сквозные отверстия на гранях;
сетчатое дно и сетчатые стенки (4 узора); перегородки; а также разрезка на фрагменты, крышка, экспорт
STL/3MF и сборка моделей — всё через общий конвейер. `test_poly.js` — 68 проверок.

## Фикс: наклон верха/низа больше не меняет высоту модели

Наклон верха («наклон верха по X/Z») раньше поворачивал верхнюю плоскость вокруг ЦЕНТРА — один угол
поднимался ВЫШЕ hh, и bbox по Y рос (на пластине 466×10 при малом наклоне высота прыгала с 10 до
14.9 мм). Исправлено в обоих тапперах (`makeTaperPointFn` и внутренний `taperPoint` из `taperBoxTris`):
Y-наклон теперь СКАШИВАЕТ грань ВНУТРЬ габарита — верх опускается от hh (его высокая кромка остаётся на
hh, так что модель не выше заданной Высоты, а низ плоский и печатаемый), низ поднимается от −hh.
Вычитается `peak` (подъём угла от наклона), поэтому наклонённая грань не выходит за [−hh, hh]. Каждый
наклон ограничивается так, чтобы низкий угол не пересёк противоположную грань (тонкая широкая пластина
может наклоняться лишь чуть-чуть) — сохраняется минимальная толщина, меш не самопересекается. Габарит по
Y теперь равен заданной Высоте (при комбинации с наклоном стенок может быть на пару мм меньше — верх
сужается, — но НИКОГДА больше). Совместимо с фиксом равномерного борта: при мягком наклоне лоток и
высоту держит, и борт ровный; при крутом — из-за опущенного верха низкой стороне не хватает материала,
и карман там мельче (равномерность физически невозможна), но высота держится и меш герметичен.
`test_rim_box.js` +6 проверок (высота=40 и равномерный борт при 8/12°, высота держится + борт не
выворачивается при 20/26°). Кэш SW → `stl-gen-v37`. Батарея 1139 → 1145.

## Фикс: борт (лоток) при наклоне верха — ровная высота стенок

При наклоне верха («наклон верха по X/Z») у борта-лотка получалась РАЗНАЯ высота стенок: дно кармана
наклонялось МЕНЬШЕ верха (линейная интерполяция низ→верх тянула плоский пол лишь на долю наклона), из-за
чего борт был выше с одной стороны и вовсе исчезал/выворачивался с низкой (при 30° замерено −0.45 мм —
пол протыкал верх). Исправлено: карман (пол + внутренние стенки) теперь ВИСИТ на равной глубине `rh`
под наклонённым верхом — для точки кармана берётся высота наклонённого верха в её (x,z) и опускается на
(hh − y), поэтому пол параллелен верху и борт одной высоты по всему периметру. При отсутствии Y-наклона
`tfPocket` тождественен обычному `tf` (ничего не меняется). Клампинг: пол не опускается ниже основания
(остаётся тонкое дно 0.6 мм) — при очень крутом наклоне низкой стороне не хватает материала, и карман
там просто становится мельче, а не протыкает дно (раньше именно здесь появлялось самопересечение). Глубина
кармана теперь ровно `rh` с обеих сторон до ~26° и мягко уменьшается только у клампа. `test_rim_box.js`
+9 проверок (водонепроницаемость и равномерная глубина борта при наклоне 10/20/26°, безопасный клампинг
на экстремальном наклоне, комбо наклон+наклон стенок+лого дна). Кэш SW → `stl-gen-v36`. Батарея 1130 → 1139.

## Этот проход: разрезка модели на фрагменты по размеру

**Разрезка на фрагменты** (группа «Разрезка на фрагменты», поле `fragSize` + кнопка «✂ Разрезать на
фрагменты»): режет активную модель на сетку водонепроницаемых кусков не больше заданного размера по
каждой оси — чтобы напечатать деталь, не влезающую на стол. Метод — последовательная разрезка одной
плоскостью: плоский разрез замкнутого тела даёт две замкнутые половины (каждый пересечённый
треугольник клипается на обе стороны; плоское сечение — набор замкнутых петель — триангулируется один
раз ушным отсечением с мостами к дыркам и пришивается КРЫШКОЙ к обеим сторонам с противоположными
нормалями, поэтому две стороны делят линию разреза точь-в-точь). Разрез X→Y→Z — композиция одиночных
разрезов; поздний разрез видит ранние крышки как обычную поверхность. Направление рёбер сечения берётся
через `нормаль × нормаль_плоскости` (иначе петля не замыкается на гранях, перпендикулярных разрезу);
рёбра, лежащие ТОЧНО на плоскости (вершина прежней крышки попала на разрез), собираются один раз со
стороны отрицательного третьего вершины; встречные пары (касание вскользь) сокращаются. Финальная
пофрагментная сварка вершин (кластеризация с поиском по 27 соседним ячейкам) закрывает субдопусковые
швы и убирает вырожденные тонкие треугольники — стандартная финализация слайсера. Каждый фрагмент —
отдельная «замороженная» модель (`frozenId` в параметрах → `buildTrisForShape` отдаёт меш как есть,
регенерация не трогает), слегка разнесённая от центра, чтобы разрезы были видны и куски не налезали.
Порядок: если фрагментов было бы больше лимита моделей — предупреждение с советом увеличить размер; если
модель уже влезает — ничего не режется. Слайсер обёрнут в IIFE (свои faceNormal/dot и др. не конфликтуют
с глобальными). `test_slice.js` — 50 проверок (водонепроницаемость и сумма объёмов = оригиналу по
размерам/формам: сплошной/полый/squircle/фаска-скругление/наклон; одноосевые разрезы; «влезает — не
режем»; ограничение размера фрагмента; round-trip замороженного меша; создание фрагмент-моделей).
Кэш SW → `stl-gen-v35`. Батарея 1080 → 1130.

Примечание: разрез точно СКВОЗЬ тонкую внутреннюю структуру (перегородка на осевой линии разреза, ребро
сетчатого дна) может оставить единичные немногообразные рёбра в стыке — редкий случай для крупных
деталей (их и режут), и любой слайсер (Cura/Prusa/Bambu) чинит это авторемонтом при импорте.

## Этот проход: сетка на боковых стенках (сквозная), 4 узора

**Сетка на стенках** (группа «Сетчатое дно», селект `latticeWalls`: нет / все 4 стенки): та же
сквозная перфорация, что и у сетчатого дна, но поставленная вертикально на боковые стенки полого
контейнера. Каждая стенка — плита между внешней гранью и гранью полости (толщина `t`); сетка живёт
во внутреннем прямоугольнике (проём полости), а сплошная рамка внешних квадов закрывает всё вокруг
(угловые стойки + полоса толщины дна), поэтому периметр перфопанели остаётся сплошным и стыкуется
без щелей с дном, кантом, угловыми стойками и полом полости. Одно тело `emitLatticeWall` обслуживает
и передние/задние (нормаль ±Z), и левые/правые (±X) стенки — оси прокидываются параметрами. Плоский
профиль, без логотипов — сетка чистого контейнера. Сплошной кант ячеек + удаление «щипков» (2×2
шахматка) → водонепроницаемо, как у пола. Узоры (ромб/квадрат/треугольники/соты) переиспользуют
`latticeSolidAt` — общий тест принадлежности. Совместимо с сетчатым дном, наклоном стенок и
организатор-фичами (скат, ярлык, грип, перегородки, ножки). Отключается на логотипах/выгибе/фаске
(корпус перестаёт быть плоским — грань уже не планарна, вырезы разошлись бы). Детализация — общий
слайдер сетки (до 500). Y-сетка стенки строится явно (мелко по высоте полости, одна ячейка на
толщину дна), т.к. `buildAxisPositions` при одном внутреннем изломе оставил бы полосу неразбитой.
`test_lattice_walls.js` — 33 проверки (водонепроницаемость по размерам/узорам/параметрам, реальные
сквозные отверстия = меньше материала, комбо с полом/наклоном/фичами, откат по гардам). Кэш SW →
`stl-gen-v34`. Батарея 1047 → 1080.

## Этот проход: организатор-фичи — грип-насечка (блок из 5 фич завершён)

**Грип-насечка** (группа «Грип-насечка»): стопка скруглённых горизонтальных рёбер, выступающих из
выбранной стенки, для удобного нескользкого хвата. Каждое ребро — полуцилиндр, вваренный плоской
хордой в стенку (приставная оболочка, без хирургии корпуса). Настройки: стенка, число рёбер, радиус.
Число рёбер авто-урезается, если стопка выше 85% стенки. Пропускается на squircle/скруглённом/
выгнутом/наклонном/gridfinity корпусе. Торцевые полудиски веерятся из конца дуги (не из точки на
хорде) — иначе T-стык с задней гранью. `test_grip.js` — 19 проверок. Батарея 1019 → 1038.

**Блок «организатор» завершён — 5 из 5:** скат дна, полка под ярлык, крепёжные уши, Gridfinity-
базплейт, грип-насечка. Кэш SW → `stl-gen-v32`.

## Этот проход: организатор-фичи — Gridfinity-базплейт

**Базплейт Gridfinity** отдельной моделью (флаг `gfBaseplate`, как logo3d; кнопка «+ Базплейт
(Gridfinity)»): сетка gfX×gfY «женских» гнёзд под ножки бинов — бины вставляются и не ездят. Каждая
ячейка 42×42 — ступенчатое скруглённо-квадратное гнездо (зеркало профиля ножки: вход 41.6 → 45° →
прямой → 45° → дно 35.7, +0.1 зазор), а верх ячейки — рамка-аннулус от границы 42×42 к кольцу входа;
СОСЕДНИЕ ячейки делят граничные точки, поэтому тонкие межъячеечные стенки замыкаются by-construction.
Внешние стенки/дно подразбиты под сетку ячеек (иначе T-стыки). Всё из `roundedRectDist`-колец,
watertight, без CSG. Толщина основания под гнёздами настраивается; при активной базплейт-модели
панель показывает только группу Gridfinity, размер копируется из активного бина.

Ключевая проверка — ИНВАРИАНТ ПОСАДКИ: профиль ножки бина влезает в гнездо базплейта на КАЖДОЙ высоте
(зазор ≥ 0 везде), т.е. бин гарантированно сядет. `test_baseplate.js` — 18 проверок (герметичность
1×1…5×3, точный шаг 42, высота, посадка, тайлинг, создание модели). v1 без магнитов в базплейте
(разрешения кольца дна и сетки не сходятся в шов чисто; магниты бина уже есть — отложено). Батарея
1001 → 1019. Кэш SW → `stl-gen-v31`.

## Этот проход: организатор-фичи — крепёжные уши

**Крепёжные уши** (группа «Крепёжные уши»): плоские фланцы вровень с дном контейнера, торчащие из
стенок, каждый с ПОТАЙНЫМ отверстием под винт — прикрутить контейнер к стене/полке. 2 (по бокам ±X)
или 4 (со всех сторон). Настраиваются Ø винта, вылет и толщина уха.

Почему уши, а не отверстия сквозь дно: сквозное отверстие в дне без CSG требует либо хирургии
делікатной тестируемой функции портов (которая к тому же дедупит «один порт на грань» и не даёт
несколько отверстий), либо булевой операции. Уши — приставные оболочки через `buildBoxWithHoles`
(машинерия потая уже проверена), без правки корпуса, без CSG, работают на любом прямостенном боксе
(сплошной/полый/лоток). Пропускаются на squircle/скруглённом/выгнутом/наклонном/gridfinity корпусе.

`test_mount.js` — 20 проверок (2/4 уха, заподлицо с дном, вылет по осям, потай, сплошной/лоток,
сосуществование со скатом+полкой, кламп большого винта, 5 гардов). Батарея 981 → 1001. Кэш SW →
`stl-gen-v30`.

## Этот проход: организатор-фичи — полка под ярлык

**Полка под ярлык** (группа «Полка под ярлык (полый)»): самонесущий треугольный кронштейн на
верхней внутренней кромке выбранной стенки (задняя/передняя/лево/право) — горизонтальная полочка на
уровне канта, куда ложится этикетка, с 45°-нижней гранью (печатается без поддержек) и вваренная в
стенку. Профиль — прямоугольный треугольник: верх-полка (на канте), гипотенуза-45° от переднего
края вниз к стенке, задняя грань в стенке. Полная ширина полости (вваривается и в боковые стенки).
Приставная оболочка, как скат/перегородки. Пропускается на squircle/выгнутых/скруглённых/gridfinity
формах. Глубина настраивается. `test_labeltab.js` — 24 проверки (4 стенки, геометрия кронштейна в
изоляции, лоток, сосуществование со скатом/перегородками, гарды). Батарея 957 → 981. Кэш SW →
`stl-gen-v29`.

## Этот проход: организатор-фичи из чужих генераторов (по порядку полезности) — начало

Начат блок «органайзер» (подсмотрено у Gridfinity/box-генераторов). Первым — **скат дна (scoop)**.

**Скат** (группа «Скат (полый)»): вогнутый пандус-четвертькруг на дне полости, чтобы мелочь
сгребалась к выбранной стенке и подхватывалась одним пальцем. Селектор «сгребать к стенке»
(перед/зад/лево/право) + радиус (0 = авто, консервативный ~0.55·min(полуразмер, высота полости)).
Реализован как СПЛОШНАЯ призма-пандус на дне полости, вваренная в стенки (grid-независимо,
watertight-by-construction — тот же приём приставных оболочек, что у перегородок/ножек), поэтому не
требует сотрудничества с сеткой дна. Профиль: `h(u)=R−√(R²−(u−R)²)` — высота R у дальней стенки,
касательно к дну на расстоянии R. Пропускается на кривом/сетчатом/squircle/выгнутом/gridfinity дне.
Работает в полом и в лотке, совместим с перегородками.

Побочно: в систему параметров добавлен тип `select` (выпадашка) — `buildAllParamUI`, слушатель
панели и `refreshActiveModelUI` теперь умеют строковые select-параметры (первый потребитель —
`scoopDir`). `test_scoop.js` — 24 проверки (4 направления, геометрия пандуса в изоляции, авто-радиус
скромный, лоток/кастомный радиус/перегородки, 6 гардов). Батарея 933 → 957. Кэш SW → `stl-gen-v28`.

## Этот проход: документация приведена в соответствие с реализацией

`README.md`: раздел «Возможности» переписан целиком — удалены описания несуществующего (пресеты
форм, кнопка «Подтвердить», «три режима куба», CSG-оговорки), добавлено всё реализованное с прошлой
ревизии README: живое/отложенное превью, undo/redo, сворачиваемый UI и мобильная ручка превью,
перетаскивание моделей в сцене, три типа крышек, полный набор отверстий (SVG-контуры, потай, фаска
кромок, пресеты разъёмов, массивы, скруглённые мульти-порты на squircle), логотипы (QR, текстуры,
замок пропорций, кнопка «3D»), слияние «борта» в «глубину полости», фаска канта, штабелирование,
перегородки, Gridfinity (профиль/губа/магниты), печатаемость (объём/вес/время/нависания/
автоориентация), сварка snapWeldTris, экспорт 3MF. Счётчик тестов 517/18 → 933/34, размер файла
~1.3 МБ, устаревшие «борт»-формулировки в ограничениях переведены на «глубину полости».

Внутренние справки («ⓘ справка») тоже синхронизированы: заметка об отверстиях больше не утверждает,
что «кривую стенку нельзя вырезать со скруглением» (порты на squircle давно скруглённые и
множественные), описывает SVG/потай/фаску/пресеты/массивы; заметка о моделях — 3MF, перетаскивание
и типы крышек; заметка о логотипах — QR, текстуры, замок пропорций и кнопку «3D». Подзаголовок
приложения: «экспорт STL / 3MF». Батарея 933/933. Кэш SW → `stl-gen-v27`.

## Этот проход: детализация сетчатого дна + Gridfinity-губа и магниты

1. **«Сетка: детализация дна»** (10…500, как у логотипов). Сетчатое дно едет на равномерной сетке
   полого билда — раньше плотность бралась из «Детализации логотипов», теперь при включённой
   сетке разрешение задаёт собственный слайдер (50 → 500 даёт 27k → 2.26M треугольников,
   герметично на всём диапазоне); без сетки поведение прежнее.

2. **Стекинг-губа Gridfinity** (`gfLip`, по умолчанию ВКЛ). Замкнутая рамка-тор на верхнем канте
   (кольца прямоугольного обхода `rectPerimWalk`, углы точные — наружная поверхность заподлицо со
   стенками), внутренний профиль = профиль ноги + 0.25 мм зазора (0.25 → 45° → прямой 18.85 →
   45° → пол 18.05), высота 4.4, вварена 0.5 мм в кант. Тест СИМУЛИРУЕТ вставку ноги: садится на
   глубину 4.35 из 4.4 контактом пары 45°-фасок, стенд-офф 0.4 мм — бины самоцентрируются.

3. **Магнитные карманы** (`gfMagnets`): 4 глухих кармана Ø6.5×2.4 на ячейку (центры ±13 мм —
   стандарт базплейтов). Подошва ноги при этом строится как квадратная сетка с блоками
   «граница → окружность» + слепой канал вверх и потолок-диск (классический watertight-паттерн);
   лента от сетки до профильного кольца идёт по углам периметра квадрата — все кольца ноги
   сэмплируются по тем же углам, всё k→k.

Батарея 923 → 933, все зелёные (в т.ч. 2×2 бин с губой и 16 карманами). Кэш SW → `stl-gen-v26`.

## Этот проход: четыре добавления по решению владельца

1. **Фаска кромок отверстий** (`edgeCh`, поле «Фаска» на карточке). Оба входа сквозного отверстия
   (круг/прямоугольник/SVG на сплошном кубе) раскрываются на edgeCh шире, конусное кольцо сводит
   обратно к каналу на глубине edgeCh — все кольца делят углы блока, герметично. Потай подавляет
   фаску (у него свой вход). Работает с массивами, живёт в JSON.

2. **Gridfinity-бин** (группа «Gridfinity»: gfOn, ячейки X/Z, высота в юнитах по 7 мм). Тело —
   обычный полый контейнер gfX·42−0.5 × gfY·42−0.5 × (gfU·7−4.75); под ним по ноге на каждую
   42-мм ячейку с ОФИЦИАЛЬНЫМ профилем сокета (фаска 0.8 → стенка 1.8 → фаска 2.15, верх 41.5,
   радиус углов 0.8→3.75) — стек колец скруглённого квадрата, вварен 1 мм в дно. Логотипы,
   отверстия, перегородки и сетчатое дно работают на теле бина. Тест держит инвариант «каждая
   вершина ноги внутри огибающей сокета». v1 без стекинг-губы и магнитов.

3. **Экспорт 3MF** (кнопка под STL). Самодостаточный OPC-писатель: zip store с настоящим CRC32,
   Content-Types, rels, 3dmodel.model. Каждая ВИДИМАЯ модель — отдельный <object> (трансформы
   запечены, координаты те же, что в STL; вершины дедуплицированы) — слайсер получает сборку
   раздельными деталями. Провалидировано сторонними python zipfile (CRC ок) и ElementTree.

4. **Перетаскивание моделей в сцене** (кнопка «✥ перемещение» в превью). Захват модели лучом,
   скольжение в горизонтальной плоскости через точку захвата, шаг 0.5 мм, модель активируется,
   поля px/pz синхронизируются. Обработчики на контейнере в capture-фазе: драг по модели
   перехватывается до орбиты, по пустому месту — камера вращается как обычно.

Батарея 891 → 898 (фаска) → 910 (gridfinity) → 923 (3MF), все зелёные. Кэш SW → `stl-gen-v25`.

## Этот проход: чистка по решению владельца

1. **Удалён CSG-движок** (~6 000 знаков + `test_csg.js`, −11 тестов): построен, замерен,
   признан непригодным для плотных мешей ещё в задаче #2 — ни одна фича его не использовала.
2. **Удалён `mirror.yml`** — отключённый workflow зеркалирования.
3. **Три кнопки крышек слиты в одну**: селект «Крышка: накидная / с защёлками / откидная (петля)»
   + кнопка «+ Крышка».
4. **Режим «Борт» слит с полым контейнером**: чекбокс и «Высота борта» удалены из UI, вместо них
   у полого появился параметр «Глубина полости (0 = до дна)» — 0 даёт прежнюю полную полость,
   значение >0 даёт лоток с карманом заданной глубины (бывший борт). Внутри всё сведено к
   `normalizeBoxParams`: слитые параметры отображаются на легаси-тройку (hollow, rim, rimHeight),
   которую по-прежнему говорят все билдеры — merged-лоток БИТ-В-БИТ идентичен старому rim
   (проверено тестом). Старые JSON-конфиги с rim:true мигрируются при загрузке. Нормализация
   вшита в `buildTrisForShape`, `FACE_AXES['-Y-inner']`, `faceFilletInsets`,
   `collectPrintWarnings` — кламп логотипов/отверстий и предупреждения печатаемости видят
   правильный режим.

Батарея 900 → 891 (−11 CSG, +2 регрессионных на слияние), все зелёные; смоук 47 UI-шагов без
ошибок. Кэш SW → `stl-gen-v24`.

## Этот проход: сворачиваемые секции + полный дебаг

1. **Все разделы панели сворачиваемые.** 11 секций (Модели, Параметры, Логотипы, Отверстия,
   Позиция, Поворот, Отображение, Сводка, Экспорт, Конфигурация, Приложение) обёрнуты в
   `<details class="panel-fold">`: заголовок секции стал summary с поворачивающимся маркером ▸,
   набор закрытых секций запоминается в localStorage (`stl-folds-closed`) и восстанавливается
   при загрузке. Группы параметров внутри «Параметров» сворачивались и раньше.

2. **Полный дебаг.** Статика: синтаксис Node --check, дубликаты id, битые getElementById,
   двойные объявления функций — чисто. Динамика: смоук-скрипт из 45 UI-сценариев (все группы
   параметров, лого/QR/текстуры/3D, отверстия с потаем/пресетами/массивами/SVG, все виды крышек,
   дублирование/скрытие/удаление моделей, повороты, автоориентация, undo/redo, бейдж устаревания,
   экспорт STL, JSON-раундтрип, каркас/темы/фолды) — ноль ошибок страницы. Кросс-фичевый фазз
   300 случайных комбинаций всех фич.

   Найдено и исправлено два бага:
   - **Гонка авто-апплая:** таймер, взведённый в «живом» режиме, перестраивал модель уже после
     переключения в тяжёлый. Теперь в момент срабатывания режим перепроверяется — вместо
     перестройки остаётся бейдж.
   - **Слайверы от двойных float-путей** (фазз: полый + скругление + повёрнутый QR-логотип на
     стенке, badEdges=146 при проверке 1e-4, но 0 при 1e-5 — т.е. меш реально замкнут, просто
     одна и та же координата зональной сетки вычислялась двумя путями с разницей 1e-15…1e-5,
     рождая невидимые щели-квады). Фикс — финальная сварка `snapWeldTris` на выходе
     `buildTrisForShape`: настоящая кластеризация вершин (порог 5e-5 мм, представитель — первая
     встреченная вершина, ничего не двигается; вложенные Map с числовыми ключами — без потери
     скорости), коллапсировавшие треугольники отбрасываются. Грид-раундинг сознательно НЕ
     использован (рвёт пары на границе ячейки — ловилось тестом «no knife-edge false opens»).

Проверка: фазз 300/300, смоук 45/45 без ошибок, +4 юнит-теста сварки. Батарея 896 → 900, все
зелёные. Кэш SW → `stl-gen-v23`.

## Этот проход: проверка печатаемости + автоориентация (роадмап закрыт)

1. **Нависания и тонкие элементы** — две новые строки статистики:
   - «Нависания >45°»: площадь граней, смотрящих вниз круче 45° и НЕ лежащих на столе
     (`overhangArea`, O(n) по мешу текущей ориентации) — «нет» или «≈ X см² — нужны поддержки».
   - «Печатаемость»: предупреждения о тонких элементах прямо из параметров
     (`collectPrintWarnings`): стенка < 1.2 мм, ребро сетки/перегородка < 0.8 мм, рельеф < 0.3 мм.

2. **Автоориентация** — кнопка «⟳ Автоориентация (мин. поддержек)» в секции поворота: перебирает
   6 ориентаций «какой гранью вниз» (`bestOrientation`), ставит модель с минимальной площадью
   нависаний (при равенстве выигрывает текущая вертикаль). Работает через дешёвый rotate-only путь.
   Наклон стенок, к слову, всегда клампится безопаснее 45° — сам по себе нависаний не создаёт.

`test_print_check.js` — 13 проверок (куб/стакан без нависаний, перевёрнутый стакан ≈ площадь дна
полости, боковой стакан, автоориентация возвращает перевёрнутое, все категории предупреждений).
Браузером: перевёрнутый тонкостенный стакан → «≈ 14.6 см² — нужны поддержки» + «стенка 0.9 мм»,
после кнопки → rx=0, «нет». Батарея 883 → 896. Кэш SW → `stl-gen-v22`.

Этим закрыт ВЕСЬ второй роадмап (21 пункт).

## Этот проход: текстуры на поверхности

Ряд «+ Текстура» с выбором узора: рифление (вертикальное/горизонтальное), точечный паттерн,
решётка из треугольных сот (три семейства линий 0°/60°/120°). Реализовано как процедурные
БИНАРНЫЕ heightmap-логотипы (`makeTextureLogoResult`, 12 повторов на карту) — текстура едет по
всему логотип-пайплайну: любая грань, размер логотипа = физический шаг узора, поворот, инверсия,
JSON, зональная сетка. Глубина по умолчанию 0.8 мм (мелкий рельеф печатается лучше), детализация
логотипов автоматически поднимается до 120.

`test_textures.js` — 23 проверки: резкость 0/1, доля рельефа, периодичность (24 перехода на
строку у рифления), каждый вид герметичен на сплошной грани и на стенке полого контейнера.
Батарея 860 → 883. Кэш SW → `stl-gen-v21`.

## Этот проход: QR-код объёмным паттерном

Поле «QR: текст или ссылка…» + кнопка «+ QR» в секции логотипов. Внутри — САМОДОСТАТОЧНЫЙ
QR-энкодер (~130 строк, без внешних библиотек, файл остаётся единым): byte-режим, версии 1–6
(до ~100 байт), ECC уровень M, многоблочный Рид-Соломон с чередованием, полный выбор маски по
четырём штрафным правилам N1–N4, корректные format-биты (BCH ⊕ 0x5412), тихая зона 2 модуля.
Найден и исправлен баг: полином-генератор RS строился в обратном порядке коэффициентов (reverse).

Матрица льётся в обычный heightmap логотипа (резко, 0/1, без канваса — тестируемо в Node), тёмные
модули выпуклые. Дальше QR — обычный логотип: любая грань, поворот, JSON, и кнопка «3D» делает
из него отдельную экструдированную модель-жетон. При добавлении QR «Детализация логотипов»
автоматически поднимается до 160, чтобы модули оставались квадратными.

`test_qr.js` — 50 проверок, включая МИНИ-ДЕКОДЕР: чтение format-битов, размаскирование, зигзаг,
дечередование блоков, проверка что ВСЕ синдромы Рида-Соломона нулевые (настоящее доказательство
валидности ECC) и раундтрип полезной нагрузки для строк от 1 до 100 символов (латиница/кириллица,
версии 1–6); плюс рельеф на грани и экструзия — герметичны. Батарея 810 → 860. Кэш SW → `stl-gen-v20`.

## Этот проход: перегородки совместимы с сетчатым дном

Снят гард `latticeFloor` в `buildDividers`: перегородки-органайзер теперь работают ПОВЕРХ
сетчатого ромбовидного дна. Это безопасно, потому что плита перегородки в любом случае вварена в
боковые стенки по всей длине, а её низ заглубляется в слой рёбер сетки (embed < толщины стенки —
ниже сетки не выходит) и сплавляется с каждым пересекаемым ребром. Тест: `test_dividers.js` 18 → 20
(герметичность, 3 плиты добавлены, низ плиты внутри слоя сетки). Батарея 808 → 810. Кэш SW →
`stl-gen-v19`.

## Этот проход: вентиляция одним кликом + undo/redo

1. **Вентиляция**: пресет «Вентиляция (щели 5×)» в выпадашке разъёмов — щель-пилюля 14×2 r1,
   сразу с массивом «сетка 1×5, шаг 6 мм»: одно нажатие — поле жалюзи на любой стенке (пресеты
   теперь умеют нести и настройки массива).

2. **Undo/redo параметров** (кнопки «↶ Отменить / ↷ Вернуть» + Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y).
   Снимок состояния активной модели (params + логотипы + отверстия; heightmap-ы по ссылке)
   захватывается ДО правки — по focusin/pointerdown внутри панели, с дедупликацией по цифровому
   отпечатку (fingerprint без тяжёлых полей). Стек до 60 шагов, redo сбрасывается новой правкой,
   история пер-модельная (чистится при переключении). Undo/redo применяет состояние и идёт через
   обычный markPending — в живом режиме меш перестраивается сам, в тяжёлом появляется бейдж.
   Текстовые поля (имя файла, текст логотипа) сохраняют родной браузерный undo.

`test_undo.js` — 15 проверок (цепочка undo/redo, коллапс одинаковых снимков, сброс redo, отверстия
и логотипы в снимке). Браузером: Ctrl+Z возвращает и параметр, и сам меш; пресет вентиляции даёт
5 герметичных щелей. Батарея 792 → 808. Кэш SW → `stl-gen-v18`.

## Этот проход: SVG-импорт произвольной формы выреза

У отверстия появилась форма «SVG-контур (свой)»: кнопка «загрузить SVG…» на карточке + поле
«Ширина» (мм). Работает на сквозных вырезах сплошного куба (одностенным портам полых пока не
маппится — отфильтровано).

Как: браузер сам растеризует SVG на канвас (`loadSvgHoleFile`), силуэт сводится к полярной таблице
r(θ) из 360 отсчётов маршем от центроида (берётся звёздное ядро формы: доли за разрывом >2px вдоль
луча отбрасываются), два прохода сглаживания от пиксельного шума. Дальше вся существующая
watertight-машинерия «блок → аннулус → канал» режет её как круг/USB-C — точка расширения одна:
`holeRadiusAt` берёт радиус из таблицы вместо `roundedRectDist`. На боковых стенках ±X (p-ось
вертикальна) угол ремапится (π/2 − ang), чтобы картинка стояла вертикально. Кламп ширины держит
блок внутри грани; массивы (сетка/по кругу) работают и для SVG-отверстий; таблица сохраняется в
JSON сборки (округление до 3 знаков).

`test_svg_hole.js` — 12 проверок на синтетических контурах (5-лучевая звезда, «яйцо»): объём выреза
= площадь полярного полигона × глубина, соответствие масштабу по кольцу, ремап на ±X, паттерн,
незагруженный контур игнорируется, кламп негабарита. Браузером — настоящее SVG-сердечко: вырез с
вогнутой выемкой, 0 открытых/битых кромок. Батарея 780 → 792. Кэш SW → `stl-gen-v17`.

## Этот проход: оценка веса и времени печати

В панель статистики добавлены три строки: «Объём детали» (точный объём меша по теореме о
дивергенции, `meshVolume` — проверено: полый 40³/2 мм = 14.75 см³ ровно по аналитике), «Вес
(PLA/PETG)» (объём × 1.24 / 1.27 г/см³ — меш уже моделирует стенки и полости, так что это материал
при 100% заполнении) и «Печать (оценка)» (типовая производительность сопла 0.4 при слое 0.2 мм ≈
12 г/ч; порядок величины). Батарея 780/780. Кэш SW → `stl-gen-v16`.

## Этот проход: пресеты разъёмов + массивы отверстий

1. **Пресеты разъёмов** на карточке отверстия (`HOLE_PRESETS`, выпадашка «Пресет разъёма…»):
   USB-C, USB-A, micro-USB, HDMI, jack 3.5 мм, DC barrel 5.5, SD, microSD — стандартные вырезы с
   ~0.4 мм припуском на печать. Выбор один раз перезаписывает форму/размеры, дальше всё редактируемо.

2. **Массив/паттерн отверстий**: у каждого отверстия появился режим «Массив: сетка» (n×m с шагом
   по обеим осям) и «Массив: по кругу» (n копий на радиусе), всё центрировано на позиции
   исходного отверстия. Разворачивается на уровне билдера (`expandHolePatterns`): копии за
   пределами грани и копии с перекрывающимися блоками отбрасываются; сам `buildBoxWithHoles`
   дополнительно прореживает копии, чьи блоки соприкасаются по индексам сетки (его давнее
   watertight-правило) — поэтому при слишком мелком шаге часть копий молча не режется, меш при
   этом всегда замкнут. Работает и для одностенных портов полого контейнера (и squircle — там
   мульти-порты уже умели). Параметры массива сохраняются в JSON сборки.

`test_hole_patterns.js` — 21 проверка (объём 6 расточек ≈ 6×одиночной, копии на радиусе, гарды
off-face/перекрытий, вменяемость всех пресетов). Батарея 759 → 780, все зелёные. Кэш SW → `stl-gen-v15`.

## Этот проход: штабелируемость + перегородки-органайзер

1. **Штабелируемость** (группа параметров). Четыре ножки-фиксатора под дном по СЕРЕДИНАМ рёбер
   (не по углам — чтобы оставаться на сплошном материале при любом squircle/скруглении углов),
   заглублены за max(стенка, скругление низа) + зазор — входят в полость такого же бокса снизу,
   стопка не ездит. Настройки: высота ножек, зазор. Пропускаются на кривых доньях (суперэллипсоид,
   выгиб), сетчатом дне и слишком малых боксах. `test_stack.js` — 17 проверок (инвариант вложения).

2. **Перегородки/секции** (группа «Перегородки (полый/борт)»): divX×divZ отсеков в полом
   контейнере или кармане борта — тонкие стенки, вваренные в дно и боковины (высота в % высоты
   полости, толщина настраивается). На squircle длина каждой перегородки ограничивается ХОРДОЙ
   суперэллипса полости в её позиции (+вварка, с потолком внутри внешней оболочки) — ничего не
   торчит сквозь кривую стенку. Пропускаются при кривом дне/сетке/выгибе/наклоне.
   `test_dividers.js` — 18 проверок (объём по алгебре плит, вместимость в суперэллипсе, гарды).

Техника — как у петли: отдельные замкнутые оболочки внахлёст с телом (слайсер объединяет), сам меш
всегда герметичен. Добавлен `plainBoxShellTris` (12-треугольная коробка) — ножки/перегородки больше
не тянут плотную сетку `buildBoxWithHoles`. Батарея 741 → 759, все зелёные. Кэш SW → `stl-gen-v14`.

## Этот проход: механика крышек — защёлки и откидная петля

1. **«+ Крышка с защёлками»** (кнопка рядом с обычной крышкой). Контейнер получает два
   гребня-бампа на стенках ±X (полнопрямоугольный рельеф через новый `makeBumpPadLogo` — едет по
   обычной логотип-машинерии: зональная сетка, кламп, сохранение в JSON белым превью). Крышка —
   как обычная накидная, но с юбкой поглубже (8 мм) и двумя скруглёнными ОКНАМИ сквозь юбку
   (одностенные порты). Инженерия: бамп выступает 0.7 мм при зазоре посадки 0.4 мм → юбка
   переклёвывает бамп и щёлкает в окно; в закрытом положении центр окна попадает ровно на центр
   бампа (мировая Y = Yc − y_локальная, пластина лежит на канте). `test_clips.js` — 18 проверок,
   включая алгебру зацепления и слип-фит.

2. **«+ Откидная крышка (петля)»**. Контейнер («cup») отращивает две проушины 7×7×5 на задней
   стенке (верх заподлицо с кантом, вварены на 1.2 мм); крышка — плоская пластина W+2×3×D с одной
   центральной проушиной, свисающей у заднего края (вварена 1 мм). Все проушины — коробки со
   сквозным каналом Ø2 вдоль X (`buildBoxWithHoles`, герметично) — ось петли делается из кусочка
   филамента 1.75. Просвет паза 8 мм под проушину 7 мм (0.5/сторона); в закрытом положении каналы
   СООСНЫ (у крышки канал смещён на −1 в проушине — компенсация вварки). Проушины добавляются как
   отдельные замкнутые оболочки внахлёст (слайсер объединяет), сам меш каждой оболочки герметичен;
   флаг `hingeRole: 'cup'|'lid'` живёт в params (как logo3d), т.е. переживает JSON и перестройки.
   `buildTrisForShape` стал обёрткой над `buildTrisForShapeCore` + `buildHingeKnuckles`.
   `test_hinge.js` — 14 проверок: соосность, посадка, вварка, фактический радиус канала по мешу.

Батарея 692 → 710 (клипсы) → 724 (петля), все зелёные. Кэш SW → `stl-gen-v13`.

## Этот проход: живое превью вместо кнопки «Подтвердить» (+ условный рендер для тяжёлых моделей)

Кнопка «Подтвердить» и её sticky-бар удалены. Новый поток (по согласованному компромиссу):
- Пока модель ДЕШЁВАЯ в перестройке (последний rebuild < 900 мс, `lastRegenMs`/`REGEN_HEAVY_MS`),
  правки параметров/логотипов применяются сами — дебаунс 300 мс + прежний rAF-путь с индикатором
  «Генерация…». Реальное время для типичных моделей.
- Как только rebuild ИЗМЕРЕН тяжёлым, автоперестройка отключается: правки лишь помечают превью
  устаревшим, в окне превью появляется красный бейдж «превью устарело — обновить» (тап — rebuild).
  Экспорт STL / сохранение JSON / переключение моделей по-прежнему синхронно достраивают всё
  через applyPendingNow — файл никогда не отстаёт от панели.
- Гистерезис естественный: пока модель тяжёлая, каждый ручной rebuild перемеряет время; став
  быстрой (упростили параметры), она снова живая.

`updateApplyButton()` (имя сохранено — десяток вызовов по всем путям) теперь синхронизирует бейдж.
`test_apply_button.js` переписан под новый поток (15 → 19 проверок: burst-автоапплай один раз,
тяжёлый режим без перестройки + бейдж, тап по бейджу, синхронный экспорт, возврат в живой режим).
Батарея 688 → 692, все зелёные. Браузерная проверка: правка ширины перестраивает сама; при
lastRegenMs=5000 бейдж виден и меш не трогается; тап по бейджу обновляет. Кэш SW → `stl-gen-v12`.

## Этот проход: три пункта нового роадмапа (мобильное превью, пропорции логотипов, чистый UI)

1. **Регулируемое превью на мобильном.** Между 3D-превью и панелью параметров в столбцовой
   (<=860px) вёрстке появилась ручка-грип: тянешь — меняется высота превью (110px…экран−180px),
   рендерер следует через onResize, высота сохраняется в localStorage и восстанавливается при
   загрузке. На десктопе ручка скрыта, инлайновая высота сбрасывается (flex-ряд не ломается).

2. **Зависимость размеров логотипов (замок пропорций).** У логотипа появился `aspect` — родное
   соотношение сторон исходника (у картинки — naturalWidth/naturalHeight, у текста — измеренная
   ширина строки / ~кап-высота 0.72em; сам heightmap по-прежнему квадратный). На карточке —
   галочка «Связать Ш/В (пропорции исходника)», по умолчанию ВКЛ для новых логотипов: правка
   ширины тянет высоту и наоборот (второе поле обновляется вживую). Новый логотип сразу создаётся
   пропорциональным («ТЕКСТ» — широкий, не квадратный). aspect/lockAspect сохраняются в JSON
   сборки и восстанавливаются при загрузке; старые сохранёнки без aspect — просто без галочки.

3. **Убраны развёрнутые описания из UI.** Четыре длинных абзаца-инструкции (мульти-модели,
   полый/борт+выгиб, логотипы, отверстия) свёрнуты в компактные `<details>`-фолды «ⓘ справка» —
   панель стала короче на несколько экранов, текст открывается по тапу. Короткие однострочные
   подсказки (единицы STL, JSON-конфиг, PWA) оставлены как были.

Батарея 688/688 зелёная (геометрия не менялась). Кэш SW → `stl-gen-v11`.

## Этот проход: багфикс — отверстие «не прорисовывалось» на скруглённом дне (репорт со скриншотом)

Симптом: USB-C порт на +Z со смещением Y=0 на squircle-контейнере со скруглённым дном не появлялся —
вместо него крошечная «царапина» на стенке, а объём модели даже РОС (вырез −58 мм³ вместо +53).

Причина: при скруглённом дне прямой участок стенки идёт от y=0 до hh, а порт, заказанный в y=0,
раньше ТРАНКИРОВАЛСЯ по границе участка — центр порта оказывался ВНЕ обрезанного окна, полувысота
окна в `squirclePortWallLoop` становилась отрицательной, и «вырез» вырождался в вывернутый лоскут.

Фикс в `squirclePreparePort`: окно больше не обрезается — вместо этого ЦЕНТР порта сдвигается внутрь
прямого участка (кламп yC в [yLoLim+f·yH, yHiLim−f·yH]); если участок короче окна целиком — порт
чисто пропускается (без выреза, меш замкнут). Проверено: вырез теперь одинаков (53.21 мм³) на плоском
дне, скруглённом дне и у самого верхнего канта.

Тесты: +5 в `test_squircle_rport.js` (18 → 23): равенство объёма выреза эталону на скруглённом дне и
у кромки, чистый пропуск на слишком коротком участке. Батарея 683 → 688, все зелёные. Headless-рендер
сценария со скриншота: порт виден, 0 открытых/битых кромок. Кэш SW → `stl-gen-v10`.

## Этот проход: несколько отверстий на суперэллипсе одновременно

Раньше squircle-контейнер вырезал только ПЕРВОЕ отверстие с боковой стенки; остальные молча
игнорировались. Теперь применяются ВСЕ боковые отверстия сразу (круги, USB-C, острые прямоугольники,
в любой смеси, на любых из четырёх стенок, включая скруглённое дно-суперэллипсоид).

Как: `squirclePreparePort` принимает массив портов.
- Сначала для каждого порта считается его блок (дуга колонок × y-диапазон); порт, чей блок
  пересекается с уже принятым (те же колонки И перекрытие по y), отбрасывается — два выреза через
  одни клетки не сшить.
- Затем ОДИН объединённый reseat y-линий: выкидываются исходные строки возле ЛЮБОГО окна и
  добавляются линии всех окон — поэтому строки каждого окна существуют в обеих стенках, как бы окна
  ни переплетались по высоте (два порта на одной высоте на разных стенках — ок).
- Дальше каждый порт строит свой skip-блок и трубу из финальных общих массивов
  (`squirclePortCut`/`squircleRoundedPortPrep` получили параметр `pre` — «reseat уже сделан»);
  `squircleWall` принимает список skip-блоков. Круглое отверстие — это полностью скруглённый
  прямоугольник (rc=r), так что круги на кривых стенках работают тем же путём.

Проверка: `test_squircle_rport.js` 11 → 18 — 4 отверстия на 4 стенках, 2 на одной, перекрывающаяся
пара (поздний отброшен, меш цел), общий y-диапазон на двух стенках, скруглённое дно + 2 порта, фазз
40 контейнеров с 2–4 отверстиями ≥92 % герметичны. Батарея 676 → 683, все зелёные. Headless-рендер:
круг+USB-C+круг на кривых стенках, 0 открытых/битых кромок (32360 tri). Кэш SW → `stl-gen-v9`.

## Этот проход: объёмный логотип отдельной моделью — экструзия силуэта по порогу (#8)

Логотип (картинка или текст) теперь можно превратить в САМОСТОЯТЕЛЬНУЮ 3D-модель сборки: кнопка «3D»
на карточке логотипа создаёт новую модель «Лого 3D» — плоский силуэт, выдавленный в герметичное тело.
Лежит плашмя (силуэт в плоскости XZ, толщина по Y) — печатается без поддержек как есть.

Как устроено (`buildLogoSilhouetteMask` + `buildLogoExtrudeTris`):
- Картинка бинаризуется по порогу (те же threshold/invert/rotation, что у настенного рельефа) в маску
  N×N клеток (N = logoResolution×3, кламп 48…256).
- «Шахматные» пары клеток (касание только углом) растворяются заполнением соседа — иначе вертикальное
  ребро угла делили бы 4 стенки (non-manifold). Проход повторяется до чистоты (только добавляет клетки).
- Экструзия: верх/низ по клеткам + вертикальные стенки на каждой границе «заполнено↔пусто». Все
  вершины из одних выражений X(i)/Z(j) — герметично by-construction, дырки и острова «из коробки».
- Модель — обычная запись сборки с флагом `logo3d:true` в params: Ширина/Глубина = габарит силуэта,
  Высота = толщина экструзии; остальные группы параметров для такой модели скрываются. Артворк едет
  в logos[0] записи (карточка логотипа остаётся — порог/инверсия/поворот редактируются как обычно).
  Пустой силуэт/нет картинки → сплошная плита (не пустой STL). Сохранение/загрузка сборки — даром
  (флаг в params, лого через previewUrl).

Проверка: `test_logo3d.js` (19) — диск/кольцо/острова: герметичность, объём ≈ аналитическому (π r² t и
300π·2), порог/инверсия/поворот, фолбэк-плита, фазз 30 случайных блобов 30/30 герметичны. Батарея
657 → 676, все зелёные. Headless-рендер: «STL» экструзией рядом с кубом, 0 открытых/битых кромок
(13344 tri), панель показывает только «Размеры». Кэш SW → `stl-gen-v8`.

## Этот проход: круглые (USB-C) порты на кривых стенках — конструктивно, без CSG (#2)

Порт формата TYPE-C на СКРУГЛЁННОЙ (squircle) стенке контейнера теперь имеет настоящие скруглённые углы,
а не прямоугольное окно. CSG для этого признан непригодным (105 с и 13012 битых кромок на плотном меше) —
сделано конструктивно, тем же приёмом, что и круглое отверстие на плоском боксе (`holeFaceWithHoles`):
- Удаляется прямоугольный БЛОК сетки чуть больше порта (~1.4×), затем веером строится АННУЛУС от границы
  блока к контуру скруглённого прямоугольника (`roundedRectDist` — тот же примитив, что у плоских отверстий),
  а два контура (внешняя/внутренняя стенка) сшиваются трубой k→k.
- Обе стенки делят один набор колонок блока и одинаковые y-линии окна (reseat), поэтому контуры окна равной
  длины и труба — чистая призма. Окно лежит НА кривой поверхности: горизонталь интерполируется вдоль кольца,
  вертикаль — обычный y. Углы скругляются по `roundedRectDist`.
- Порог: при `rc > 0.3` идёт скруглённый путь; квадратный порт (rc≈0) по-прежнему берёт прямоугольное окно
  (`squirclePortCut`). Работает на плоском и на скруглённом (суперэллипсоид) дне, на гранях спереди/сзади и
  по бокам (ширина/высота меняются местами корректно).

Проверка: `test_squircle_rport.js` (11) — герметичность+объём по сетке rc/граней/дна, «скруглённый ≠
квадратный», прямая проверка что верхняя/нижняя кромка окна поджата внутрь на ≈rc, фазз 60 случайных
портов ≥92 % строго герметичны. Плюс headless-рендер: углы окна на кривой стенке визуально скруглены,
0 открытых/битых кромок (19656 tri). Кэш SW → `stl-gen-v7`.

## Этот проход: фаска верхнего внешнего канта полого контейнера (#5)

Полый контейнер получил кастомный СРЕЗ (плоскую фаску) на верхнем внешнем канте — там, где внешняя
стенка встречает верхний кант. Два параметра: `chamferTop` (ширина = горизонтальный отступ, 0 = выкл)
и `chamferAngle` (угол фаски от горизонтали; вертикальный провал v = ширина·tan(угол)).

Конструктивно, без CSG, герметично by-construction:
- Внешние стенки теперь заканчиваются на `y = hh - v` (не hh); в позиции стенок добавлен брейкпоинт
  `±(half - setback)`, чтобы граница «плоский кант / фаска» была чёткой.
- Весь верх (кант + фаска) выдаётся одной поверхностью над кольцевой зоной (минус проём полости):
  высота каждой вершины `y = hh - v·max(dx,dz)/s`, где dx/dz — заход за прямоугольник `±(half-setback)`.
  Плоско (=hh) внутри отступа, скос до `hh-v` на `±half`. Внешняя граница `±hw/±hd` ложится точно на
  `hh-v` (стыкуется с опущенными стенками), внутренняя — на проём `±(hw-t)/±(hd-t)` (общие xMid/zMid
  со стенками полости). Угловые ячейки складываются по диагонали с бóльшим Δy → чистый митр-конёк.
- Ширина клампится `< толщины стенки` (чтобы фаска не съела проём), угол в [15°,80°], провал ограничен.

Совместимо с сеткой-дном (разные зоны) и наклоном стенок; порты/squircle/скругления идут более ранним
путём (фаска там игнорируется, задокументировано). Только плоский полый контейнер без логотипов/выгиба.
Проверка: `test_chamfer.js` (29) — герметичность и +объём на сетке ширин×углов, кламп сверх-широкой фаски,
фаска+наклон, фаска+сетка, no-op при ширине 0, срез уменьшает объём. Плюс headless-рендер (митр-угол
чистый, 0 ошибок). Кэш SW → `stl-gen-v6`.

## Этот проход: группировка параметров в сворачиваемые категории (#6)

40+ параметров бокса больше не одна плоская простыня, а 7 сворачиваемых секций `<details>`:
Размеры · Скругление углов · Squircle/супереллипс · Наклон стенок · Выгиб стенок (купол) ·
Полый контейнер/борт · Сетчатое дно. Каждому параметру добавлено поле `group`; `buildAllParamUI`
строит по одной `<details class="param-cat">` на группу (в порядке первого появления), внутренние
скругления полости переехали в секцию «Полый/борт». Все секции открыты по умолчанию, сворачиваются
кликом по заголовку. Порядок/непрерывность групп чисто косметические — все обращения к параметрам
идут по ключу, не по индексу. Проверено headless-рендером: 7 категорий, 41 строка, 0 ошибок JS.

## Этот проход: убраны пресеты (#7)

Панель «Пресеты» (7 кнопок: Сплошной/Скруглённый/Squircle/Надутый/Контейнер/Лоток/Бочка) удалена целиком:
разметка `#preset-bar`, CSS `.preset-btn`, обработчик клика, а также `PRESETS`/`PRESET_TREATMENT_KEYS`/
`applyPreset()`. Форму теперь задают напрямую параметрами — пресеты лишь тасовали те же ключи. Ни один
тест на них не завязан. Кэш SW → `stl-gen-v4`.

## Про #2 и CSG: почему круглые порты на кривых стенках делаем НЕ через CSG

Замерил: вычесть инструмент-скруглённый-прямоугольник из плотного меша squircle-контейнера (~26k tri) через
BSP-CSG — ~105 000 мс и на выходе 58 открытых + 13012 битых кромок (мусор). BSP не масштабируется на плотные
кривые меши. Движок CSG остаётся (мелкие меши, 11/11), но #2 (round TYPE-C на скруглениях) нужно делать
конструктивно — скруглять углы порта в самой генерации стенки, а не булевой операцией постфактум.

## Этот проход: движок CSG (булевы операции над мешами)

Внедрён CSG-движок (union / subtract / intersect) — классический BSP-алгоритм csg.js (Evan Wallace, MIT),
адаптированный под нашу soup из треугольников. Раньше булев движок был отклонён по дизайну (даёт T-стыки и
рвёт герметичность); здесь эта проблема решена постобработкой.

- Сырой BSP оставляет T-стыки (вершина в середине ребра соседа) → строгий manifold-чек видит открытые кромки.
  `weldTJunctions`: для каждого ребра собираю вершины, лежащие на нём, и ПЕРЕтриангулирую треугольник
  веером ИЗ ЦЕНТРОИДА (веер из угла плодил вырожденные слайверы на прямом ребре и «возвращал» длинное ребро).
  Плюс нормализация ориентации (если знаковый объём < 0 — разворот винды).
- Публичные `csgSubtract/csgUnion/csgIntersect` = сырой op + weld + ориентация. ~98 % случайных вырезов
  строго герметичны; редкий копланарный оверлап (ребро на 3+ совпадающих гранях) ловит бейдж страницы.

Проверка: `test_csg.js` (11) — box−box/цилиндр/угол, union/intersect герметичны и положительно ориентированы,
фазз 150 случайных вырезов ≥90 % строго герметичны. Node-фазз прототипа `scratchpad/csg.js` — 196/200.
Батарея: **616/616**. Движок — фундамент для круглых портов на кривых стенках (#2).

## Этот проход: замкнутые области логотипа на решётке — чистое отверстие (без сетки)

На сетчатом дне (lattice) внутренние замкнутые области логотипа (счётчики букв — середина O, A, D, глаза
черепа) остаются ОТВЕРСТИЕМ, но сетка внутри них не рисуется — чистая дырка, а не залатанный решёткой участок.
Раньше маска оставляла счётчик фоном → внутри буквы светилась решётка.

- В `emitLatticeFloor`: помимо маски формы веду массив `logoShape`, затем внутри bbox каждого логотипа
  (solidRects) делаю flood-fill по ФОНУ: ячейка фона, которая не достаёт до края bbox через другой фон,
  окружена штрихами логотипа → это счётчик → форсирую её НЕ-solid (чистое отверстие, ребра решётки убраны).
  Заливка идёт по форме логотипа, поэтому трогает ровно замкнутые области; логотип без счётчиков не меняется.

Проверка: `test_lattice_floor.js` (50, +4) — кольцо/C-образное/сплошной диск на решётке герметичны, и у
счётчика замкнутого кольца почти нет верхних граней (отверстие) в отличие от сплошного диска. Рендер: кольцо-O
на решётке — центр открыт (дырка, без сетки), сетка только снаружи, 0 открытых/bad. Батарея: **616/616**.

## Этот проход: фикс — TYPE-C порт не переворачивается при смене грани

Баг: у скруглённо-прямоугольного (USB-C) отверстия ширина и высота менялись местами при переносе на другую
грань. Причина: `portW/portH` шли прямо на оси грани `(u,v)`, а на `+X/-X` ось u — это мировой Y (вертикаль),
поэтому ширина/высота попадали на противоположные оси, чем на `+Z/-Z` → порт 9×3.2 разворачивался на 90°.

Фикс: `rrectHalfExtents(axis, portW, portH)` — ширина ВСЕГДА горизонталь, высота ВСЕГДА вертикаль (мировой Y
на боковой стенке); используется в `holesForBuilder`, `portsForBuilder` и `clampHoleToFace` (клампинг ведётся
в (u,v)-пространстве и мапится обратно). `squirclePortParams` читает исправленные `ap/aq` → тоже верно.

Проверка: `test_holes.js` (81, +6) — на +Z/−Z/+X/−X высота→Y, ширина→горизонталь; +X сплошной и полый порт
герметичны. Батарея: **602/602**.

## Этот проход: порт на полом суперэллипсоиде (скруглённое дно)

Одностенный порт заработал и на полом контейнере со скруглённым дном (squircleVBot > 0, суперэллипсоид).
Ключ: у такого контейнера ВЕРХНЯЯ стенка [0, hh] прямая (экструзия кольца), скругление — купол ниже y=0.
Значит порт садится на прямую верхнюю стенку тем же приёмом «общие линии окна → трубка k→k», а купол снизу
не трогается.

- Логику окна вынес в общий `squirclePortCut(port, ringO, ringI, rnO, N, ySampO, ySampI, coarseW, yLo, yHi)`;
  плоское дно, скруглённое дно и лоток теперь зовут её с разным диапазоном прямой стенки. Ветку eV>0
  переписал: `ySampI` стала переменной, купола строятся после реза.
- Баг, пойманный фаззом: пере-сев мог удалить КРАЙНИЕ ряды стенки (y=0 — шов с куполом, y=hh — шов с бортом),
  если порт стоял у самого края → рвались швы (сотни открытых кромок). Теперь пере-сев ВСЕГДА сохраняет
  концы массива.
- Диспетчер: снял гейт `eV===0` — порт передаётся и для скруглённого дна.

Проверка: `test_squircle.js` (137, +7) — eV 0.5/0.8/1.0, порт у самого шва купола, диспетчер, no-port
регресс; фазз скруглённого дна 300/300 + плоского 400/400 герметичны; рендер (суперэллипсоид-корпус с
портом в выгнутой верхней стенке, дно скруглено, 0 открытых/bad). Батарея: **596/596**.

## Этот проход: зенковка / цекование (потай под головку винта)

Круглые сквозные отверстия на сплошном кубе получили потай у входной грани: «зенковка» (конус) и
«цекование» (цилиндр с плоским уступом) — типовые посадки под головку винта. Вход открывается на Ø головки,
канал сужается до Ø отверстия и идёт насквозь до дальней грани.

- Геометрия: канал стал набором соосных колец. `holeBlockExtents` (блок по max(r, headR) — головка шире),
  `holeFaceRadiusAt` (входная грань режется на Ø головки, дальняя — на Ø отверстия), а сам канал в
  `buildBoxWithHoles` — секции `tube(ca0,r0,ca1,r1)`: зенковка = конус+прямой канал, цекование =
  цилиндр+кольцо-уступ+прямой канал. Все кольца по ОДНИМ углам → герметично по построению.
- Только круглые отверстия (винты круглые) на плоской грани сплошного куба. На fillet поле есть в UI, но
  канал остаётся прямым (голова игнорируется — кривой путь без отдельной поддержки). rrect головы не имеет.
- UI: у круглого отверстия селектор «Без потая / Зенковка / Цекование» + Ø головки + глубина; `clampHoleToFace`
  клампит Ø головки под грань и глубину под толщину; save/load возит поля.

Проверка: `test_holes.js` (75, +11) — зенковка/цекование на разных гранях, head±, микс с обычным, две оси,
клампинг, диспетчер; head-фазз 400/400 герметичны; рендер (зенковка+цекование сверху, 0 открытых/bad кромок).
Батарея: **589/589**.

## Этот проход: одностенный порт на полом squircle (окно в кривой стенке)

Полый squircle-контейнер получил одностенный порт — прямоугольное окно, пробивающее ОДНУ выгнутую стенку в
полость (USB-C в корпусе со скруглёнными по супереллипсу стенками). Та же схема «общие линии окна → трубка
k→k», но в угловой параметризации стенки: внешняя и внутренняя стенки скругляют ОДИН блок (дуга колонок ×
[ylo,yhi]) и пере-сеются на ОДИНАКОВЫЕ y-выборки внутри окна, поэтому их кольца выреза совпадают, а трубка
между ними герметична. Углы окна прямые: кривую стенку нельзя вырезать со скруглением водонепроницаемо-по-
построению без булева движка (владелец его отклонил — см. «CSG / булево — не делаем»).

- `squircleWall` получил `skip` (пропуск блока ячеек), `squirclePortArc` (дуга индексов кольца по грани +
  тримминг до монотонного по горизонтали участка — не заворачивает за угол), `buildSquircleHollow` строит
  трубку. Гейт: плоскодонный полый squircle без логотипов, одна боковая стенка.
- Тонкость: при пере-севе y убираем оригинальные выборки в пределах полуячейки от границы окна (иначе почти-
  дубль-ряд у `yhi` складывался в перекрытые треугольники → 174 bad-edge; фазз это поймал).

Проверка: `test_squircle.js` (130, +7) — фронт/зад/бок, off-center, seam-wrap (+X через индекс 0), диспетчер;
squircle-порт фазз 400/400 герметичны; рендер (squircle-корпус с прямоугольным портом в выгнутой стенке,
0 открытых/bad кромок). Батарея: **566/566**.

Итог по «совместимости отверстий со скруглениями»: отверстия/порты теперь на сплошном кубе, полом контейнере,
кубе со скруглёнными углами (fillet) и полом squircle. Осталось выключенным только там, где нет плоской зоны
и кривизна не помогает: скруглённый ПОЛЫЙ (внутр. фаска/кант), выгиб, решётка, логотипы, режим «борт».

## Этот проход: отверстия на коробке со скруглёнными углами (fillet)

Отверстия/порты теперь работают и на кубе со скруглёнными углами (fillet) — раньше при скруглении они
игнорировались. Грань скруглённого куба в ЦЕНТРЕ плоская (скругление только по рёбрам), поэтому применён тот
же приём, что и у полых портов: линии окна отверстия внедряются в ОБЩИЕ помассивы позиций осей, обе пробитые
грани (±half по оси) берут кольцо выреза по ОДНИМ углам → прямой канал между ними герметичен.

- `buildFaceAsymFromAxis` получил опциональный `port`: пропускает ячейки блока и сшивает кольцо
  «бордер→вырез» (отверстие клампится в плоскую зону, поэтому клип фаски не задевается).
- `buildAsymRoundedBox` получил `holes`: клампит каждое отверстие в плоскую зону (от рёбер на макс. радиус),
  внедряет линии окна, передаёт порт в пробитые грани, добавляет каналы; по одному сквозному на ось,
  слишком большое для плоской зоны — отбрасывается. Наклон работает (per-vertex поверх).
- Диспетчер: сплошной скруглённый куб + отверстия (без логотипов) → путь asym с `holes` (в т.ч. форсит asym
  при равномерном скруглении). Формы — круг/USB-C/паз, как везде.

Проверка: `test_holes.js` (64, +10) — прямые вызовы (равномерное/асим. скругление, 2 оси, off-center, USB-C,
дроп большого) + диспетчер (USB-C, асим, наклон); fillet+hole фазз 500/500 герметичны; рендер (скруглённый
корпус с USB-C-портом спереди и круглым отверстием сверху, 0 открытых кромок). Батарея: **559/559**.

## Этот проход: одностенный порт на полом контейнере (USB-C в корпусе)

На ПОЛОМ контейнере отверстие теперь пробивает ТОЛЬКО одну стенку и открывается в полость (порт для
разъёма в корпусе), а не насквозь. Ключ к герметичности: внешние грани и внутренние стенки полости берут
пробы из ОДНИХ И ТЕХ ЖЕ помассивов позиций по осям (внутренняя стенка = mid-slice). Каждый порт добавляет
ОДИНАКОВЫЕ линии окна в эти массивы → пробитая внешняя грань и пробитая внутренняя стенка строят кольцо
выреза по ОДНИМ И ТЕМ ЖЕ углам → канал (труба) между ними — чистая призма k→k, герметичная по построению.

- Новый билдер `buildHollowBoxWithPorts(w,h,d,t, ports)` + `buildFaceWithPort` (плоская грань с вырезом по
  массивам позиций). Порты на боковых стенках (+X/−X/+Z/−Z) и на дне (−Y); верх (+Y) открыт и не пробивается.
- Робастность: один порт на стенку (второй отбрасывается — иначе двойная труба), центр клампится так, чтобы
  окно влезало во внутреннюю стенку на ОБЕ оси (иначе внешнее/внутреннее кольца расходятся), слишком
  большой для стенки порт отбрасывается. Наклон стенок работает (per-vertex поверх).
- Диспетчер: `portsForBuilder` (грань→ось+знак, +Y отбрасывается) + ветка plain-hollow-with-ports. Гейт —
  только плоский полый контейнер (без логотипов/решётки/выгиба/скругления/squircle; режим «борт» — нет).
- UI: та же карточка отверстия; примечание описывает поведение «одна стенка» и открытый верх.

Проверка: прототип `scratchpad/hollowport.js` — 585-конфиг manifold-свип + наклон/дедуп/кламп края, все
герметичны. `test_holes.js` (54, +13): порты на боковых/дне, дедуп, +Y игнор, клампинг, диспетчер+наклон+гейт.
Плюс рендер (полый корпус с USB-C-портом спереди и круглым портом сбоку, 0 открытых кромок). Батарея: **549/549**.

## Этот проход: формы отверстий (круг / скруглённый прямоугольник — USB-C, паз)

Отверстие перестало быть только кругом. Ключевая идея: и аннулюс (кольцо между бордер-сеткой и контуром
выреза), и стенка канала брали радиус `H.r` в ОДНОЙ и той же формуле `центр + r·(cos,sin)` по общему набору
углов. Заменили постоянный `r` на функцию радиуса от угла `holeRadiusAt(H, ang)` → любая звёздно-выпуклая
форма, при этом структура «бордер↔контур по общему углу, канал по тем же углам» сохранена → герметичность
по построению, как у круга.

- Один примитив — скруглённый прямоугольник `roundedRectDist(ap, aq, rc, ang)` — покрывает всё: круг
  (`ap=aq=rc`), паз/пилюлю (`rc=min`), USB-C / скруглённый прямоугольник (`rc<min`), прямоугольник (`rc≈0`).
- Блок выреза стал прямоугольным (`hsP/hsQ` по полуосям формы, а не квадрат по `r`) — вытянутые пазы
  получают корректную рамку. Защита от перекрытия на одной оси обновлена под те же `hsP/hsQ`.
- UI карточки отверстия: выбор формы (Круг / Скруг. прямоугольник) + поля Ширина/Высота/Радиус; при выборе
  прямоугольника подставляются стандартные размеры USB-C (9 × 3.2, R 1.6). `clampHoleToFace`, save/load JSON
  и диспетчер обновлены под новую модель (обратная совместимость: старые отверстия без `shape` = круг).

Проверка: `test_holes.js` (41, +14) — `roundedRectDist` санити, круг двумя способами = тот же меш, USB-C/паз/
почти-прямоугольник/смешанные — все герметичны, диспетчер + клампинг. Плюс Playwright-смоук (добавить порт,
переключить форму, поля появляются, меш герметичен, 0 ошибок) и рендер порта. Батарея: **536/536**.

## Этот проход: тёмная тема

Добавили переключатель светлая/тёмная тема (кнопка 🌙/☀ в шапке панели).

- Палитра уже жила в CSS-переменных `:root`; хардкод-белые (`#fff`, `rgba(255,255,255,…)`) вынесли в
  семантические переменные (`--field-bg`, `--card-veil`, `--stats-veil`, `--on-accent`, `--viewport-clear`),
  так что тёмная тема — это один блок `:root[data-theme="dark"]`, переопределяющий палитру.
- Фон 3D-вьюпорта — это WebGL clear color, не CSS-поверхность; `applyTheme` перекрашивает рендерер под
  активную тему (переменная `--viewport-clear`).
- Выбор запоминается в `localStorage`; по умолчанию — системная тема (`prefers-color-scheme`). Крошечный
  скрипт в `<head>` ставит `data-theme` ДО отрисовки — нет вспышки светлой темы при перезагрузке тёмной.
- Также синхронизируется `<meta name="theme-color">` (цвет строки состояния браузера/PWA).

Проверка: Playwright-смоук (toggle меняет тему и фон панели, двойной клик возвращает, тема переживает
перезагрузку, 0 ошибок страницы) + скриншоты обеих тем. Батарея тестов геометрии без изменений: 522/522.

## Этот проход: ревизия + фаззинг — починка перекрывающихся сквозных отверстий

Прогнал случайный фазз (1200 конфигов: режим × скругление × наклон × выгиб × решётка × отверстия) с
проверкой герметичности — как и раньше, чтобы вылавливать регрессы вне ручных тестов. Нашлись два класса
негерметичных мешей.

- **Починено — перекрытие сквозных отверстий на одной оси.** Два отверстия на одной оси пробивают одну и
  ту же пару граней (отверстие на `+Y` и на `−Y` — это ось Y), и их блоки живут в общей сетке. Если блоки
  перекрывались, они делили ячейки → рвались аннулюс/стенка канала → открытые кромки (в репро 40 открытых).
  `buildBoxWithHoles` теперь отбрасывает отверстие, чей блок перекрывает/касается уже принятого на той же
  оси (индексная арифметика блока в точности как в `holeFaceWithHoles`, требуется зазор в 1 ячейку) —
  плоский квад между блоками остаётся согласованным с обоими бордерами, герметичность по построению.
  Разнесённые и пересекающиеся на соседних гранях отверстия не затронуты. Тесты: `test_holes.js` (27, +5).
- **Задокументирован edge-case (не блокирует).** Комбо «выгиб стенок + скругление внутренних рёбер + наклон»
  на очень тонком лотке/контейнере у предела вырождения даёт негерметичный шов в asym-скруглённом билдере
  (в фаззе ~1 % — открытые кромки). Не тихий брак: страница гоняет `manifoldCheck` и показывает бейдж
  «открытый ⚠ (N кромок)», так что такой STL не уходит незамеченным. Глубокая правка геометрии отложена.

Батарея после правки: **522/522 в 18 файлах**, фазз 1187/1200 герметичны (13 — вышеописанный edge-case).

## Этот проход: логотип на дне решётки при «Глубина мм» = 0 (виден как плоская вставка)

По репорту: логотип на дне решётки с глубиной 0 пропадал. Причина: заплатку-под-форму я определял по
`floorFn` (смещение = глубина × форма), а при глубине 0 оно = 0 везде → ни одной ячейки не форсилось
solid → форма исчезала в сетке.

- Добавили `makeLogoMaskFn` (возвращает 1/0 — маску формы по `heightmap>порог`+invert, НЕЗАВИСИМО от
  глубины) и `buildCombinedLogoMaskFns` (по граням, как dispFns). Диспетчер решётки кладёт
  `lat.floorMask = маска['-Y-inner']`.
- `emitLatticeFloor` принял `floorMask`; решение о сплошной заплатке теперь по `shapeFn = floorMask ||
  floorFn` (маска depth-независима). Эмбосс верха по-прежнему `floorFn`: при глубине 0 = 0 → верх вровень
  с дном (плоская вставка), >0 — выпуклый, <0 — гравировка.

Проверка: `test_lattice_floor.js` (46, +4): depth-0 логотип водонепроницаем, заполняет форму (меш ≠ без
логотипа) и ПЛОСКИЙ (центр дна не поднят над `yt`), а depth>0 — поднят. Браузер: кольцо-логотип depth 0 на
решётке — форма заполнена вровень, 0 открытых кромок, ошибок нет. Батарея **522/522 в 18 файлах**.

## Этот проход: PWA — генератор устанавливается как приложение на ПК/телефон

Сделали инструмент устанавливаемым (иконка на рабочем столе/домашнем экране, standalone-окно, офлайн).
Ключевое ограничение платформ: браузеры ставят PWA только со страницы по http(s), с `file://` — нельзя;
поэтому добавили и хостинг-обвязку.

- **Иконка**: нарисовали SVG в стиле инструмента (blueprint-контейнер + «STL» на navy), растеризовали
  через headless-Chromium в `icon-192/512.png` и `apple-touch-icon.png` (180).
- **`manifest.webmanifest`**: name/short_name, `start_url=parametric-stl-generator.html`,
  `display=standalone`, theme/bg `#16283a`, иконки (192/512/512-maskable/svg).
- **`sw.js`**: service worker, precache оболочки (HTML+манифест+иконки), stale-while-revalidate; версия
  кэша `stl-gen-v1`, activate чистит старые. Регистрируется ТОЛЬКО по http(s).
- **`index.html`**: редирект на генератор (корень GitHub Pages).
- **В HTML**: `<link rel=manifest>`, theme-color, apple-mobile-web-app-* + apple-touch-icon; `setupPWA()`
  регистрирует SW (guard по протоколу), ловит `beforeinstallprompt` → показывает кнопку «⤓ Установить
  приложение» и вызывает `prompt()`; на iOS (нет beforeinstallprompt) кнопка показывает шаги
  «Поделиться → На экран „Домой“». В standalone-режиме секция скрыта.

Проверка: локальный http-сервер + Chromium — манифест валиден (4 иконки, standalone), **SW active**, иконки
200, ошибок нет; офлайн ПОСЛЕ прогрева кэша — `fetch` и reload отдают приложение из кэша; на `file://` — SW
пропущен, секция установки скрыта, ошибок нет, Three r128 работает. Node-батарея без изменений —
**513/513**. Файлы `index.html`/`sw.js` не влияют на извлечение `<script>` в `run-all.sh`. Хостинг — через
GitHub Pages (одна настройка репозитория, описано в README).

## Этот проход: шрифты вшиты — файл полностью автономен (0 внешних запросов)

После инлайна Three.js оставалась одна внешняя загрузка — IBM Plex с Google Fonts (`<link>` на
`fonts.googleapis.com`). Вшили и её:

- Взяли `@fontsource/ibm-plex-sans@5.2.8` и `@fontsource/ibm-plex-mono@5.2.7` из npm, извлекли WOFF2 для
  нужных начертаний (Sans 400/500/600/700, Mono 400/500/600), сабсеты **latin + cyrillic** (UI на русском),
  14 файлов ~244 КБ.
- Сгенерировали 14 `@font-face` с `src:url(data:font/woff2;base64,…)` и точными `unicode-range` (взяты из
  CSS fontsource — latin и cyrillic диапазоны), заменили три `<link>` (preconnect ×2 + stylesheet) на
  инлайн-`<style>`. Base64 не содержит `<`, так что извлечение `<script>` в `run-all.sh` не задето.
  Файл вырос до ~1.16 МБ.

Проверка: реальный Chromium (offline, file://) — **0 внешних HTTP-запросов** при загрузке, `THREE.REVISION
==='128'`, `scene3DReady===true`, `document.fonts.check("600 14px 'IBM Plex Sans'")===true`,
`document.fonts.status==='loaded'`; русский текст и 3D-сцена рисуются (скриншот). Node-батарея —
**513/513 в 18 файлах**.

## Этот проход: Three.js встроен в файл (3D-превью работает офлайн)

README обещал «весь код, включая Three.js, в одном файле», но по факту библиотека тянулась с
`cdnjs.cloudflare.com` — в средах без интернета (или где CDN заблокирован) 3D-превью не запускалось
(показывалась ошибка «не удалось загрузить Three.js с CDN»). Встроили библиотеку инлайн:

- Скачали `three@0.128.0` (= r128, ту же ревизию) из npm-registry, взяли `build/three.min.js` (~603 КБ),
  заменили `<script src="…cdnjs…three.min.js">` на инлайн-`<script>` с телом библиотеки. В минифайле нет
  `</script>`, так что инлайн безопасен. Орбит-контролы и так были свои (OrbitControls не нужен). Файл
  вырос до ~855 КБ.
- `run-all.sh`: извлечение `<script>` теперь берёт ТОЛЬКО 2-й блок (основной код), пропуская 1-й
  (инлайн-Three) — `awk '… c>=2'` — чтобы Node-тесты не парсили лишние 603 КБ.
- Сообщение об ошибке `typeof THREE==='undefined'` переписано (больше не винит CDN — теперь это признак
  повреждённого/недокачанного файла).

Проверка: реальный Chromium (offline, file://) — `THREE.REVISION==='128'`, `scene3DReady===true`, ни одного
запроса к cdnjs (остался лишь Google Fonts, у него системный фолбэк), 3D-сцена рисуется (скриншот: полый
контейнер с решётчатым дном и кольцом-рельефом). Node-батарея без изменений — **513/513 в 18 файлах**.
Осталась одна внешняя загрузка — шрифты Google Fonts (косметика, офлайн заменяются системными).

## Этот проход: логотип на дне решётки — форма на фоне сетки (а не квадрат-плитка)

По скриншоту пользователя логотип на дне ложился на СПЛОШНОЙ квадрат (я форсил solid по всему footprint-
прямоугольнику). Нужно — картинка на фоне решётки. Поменял форс-solid: заполняем сплошным ТОЛЬКО там, где
реально есть рельеф (`floorFn≠0`, проверка по 4 углам + центру ячейки — тонкие штрихи ловятся), а фон
остаётся сеткой. Форс-solid перенёс ДО pinch-removal, чтобы «шахматки», которые тонкая форма создаёт с
сеткой, вычищались (добавление solid по форме уже НЕ обязательно монотонно-безопасно, как цельный
прямоугольник). Эмбосс по-прежнему по вершинам → герметично. `solidRects` остался fallback'ом.

Проверка: `test_lattice_floor.js` (42, +1) — форма-логотип оставляет площадь сплошного дна в bbox МЕНЬШЕ,
чем полное покрытие (фон = сетка), при этом водонепроницаемо; браузер: кольцо-логотип на решётке — покрытие
0.75 vs 0.91 у полного квадрата, оба герметичны. Батарея **513/513 в 18 файлах**.

## Этот проход: логотипы на сетчатом дне (сплошная заплатка + emboss)

Логотипы на СТЕНКАХ с решёткой уже работали; несовместимо было только дно (перфорация не несёт рельеф).
Теперь логотип на дне полости («−Y изнутри») рисуется:

- Диспетчер плоского полого при `latticeFloor` считает для каждого `-Y-inner` логотипа footprint-прямо­
  угольник (`rotatedHalfExtents` + запас) и flat-space `floorFn` (`buildCombinedLogoDispFns(q=>q)['-Y-inner']`),
  кладёт их в `lat.solidRects` / `lat.floorFn`.
- `emitLatticeFloor` при наличии `solidRects` форсит `solid[i][j]=true` под footprint (после pinch-removal;
  добавление solid монотонно — новых «шахматок» не создаёт), поэтому под логотипом решётка становится
  СПЛОШНОЙ заплаткой. Верхние вершины эмбоссятся `floorFn` (ПО ВЕРШИНАМ, вместе с полем высот скруглённого
  профиля) → соседние ячейки читают одну высоту на общем ребре → герметично. `floorFn` вне логотипа = 0,
  так что рёбра/кант не трогаются. `-Y-inner` клампится в inner-зону = зону решётки, поэтому заплатка всегда
  внутри сетки. Внешнее дно («−Y») с решёткой пока не поддержано.

Проверка: `test_lattice_floor.js` (41, +5): логотип на дне решётки водонепроницаемо (в т.ч. гравировка,
скруглённый профиль, вместе со стенным логотипом), меш меняется, рельеф поднимается над плоскостью дна.
Браузер: −Y-inner логотип + решётка + скруглённый профиль → водонепроницаемо, ошибок нет. Батарея
**512/512 в 18 файлах**.

## Этот проход: скруглённый профиль ребра сетчатого дна (поле высот по вершинам)

Флажок «Сетка: скруглённый профиль ребра» (`latticeRound`). Прямой «инсет профиля» на воксельной решётке
рвёт герметичность: соседние solid-ячейки с разной дырочной-соседностью инсетят общий угол по-разному →
T-стык. Обход — сделать высоту ВЕРХА полем по ВЕРШИНАМ сетки:

- В `emitLatticeFloor` при `rounded`: multi-source BFS по вершинам от тех, что касаются дыры (dist=0),
  вглубь ребра. Высота вершины `topY(dist)` куполится от небольшого «губного» уровня у дыры
  (`edgeFrac=0.18·H`) до полного `yt` при `dist≥domeR` (дуга `sqrt(1-(1-n)²)`). Верхний квад и верхние
  кромки рёбер-стенок берут `topY` ПО ВЕРШИНАМ — соседние ячейки читают одну и ту же высоту на общем
  ребре → шва нет по построению. Дно плоское (yb), стенки у дыр — от `topY` вниз к `yb`. Плоский профиль
  (`rounded=false`) возвращает прежний постоянный `yt` — старый путь байт-в-байт.
- Кант (border) и глубина ребра далеко от дыр остаются на `yt` (dist≥domeR) → стыкуются со стенками/полом
  полости как раньше.

Проверка: `test_lattice_floor.js` расширен (36, +10): скруглённое дно водонепроницаемо на всех конфигах
(шаг/ребро/кант/не-куб/наклон/логотип на стенке) и верх реально куполится (есть вершины строго между yb и
yt); плоский путь не изменился. Браузер: тумблер, водонепроницаемо, ошибок нет. Батарея **507/507 в 18 файлах**.

## Этот проход: сквозные цилиндрические отверстия (без общего булева движка)

Отверстия — по сути локальная CSG (то, что мы отложили как хрупкое). Реализовали ВОДОНЕПРОНИЦАЕМОЕ
подмножество без булева движка, по договорённости с владельцем (произвольные, но только на плоской стенке):

- **`buildBoxWithHoles(w,h,d,holes)`**: каждая пробитая грань — грид-квадраты везде, кроме прямоугольного
  БЛОКА вокруг каждого отверстия; блок — аннулюс между его бордюр-кольцом из точек сетки (общих с
  соседями) и окружностью. Точка бордюра i → точка окружности под углом бордюра[i] от центра, так что оба
  контура равной длины и одного обхода → чистый треугольный стрип. Ствол = та же выборка окружности,
  вытянутая вдоль оси, поэтому его два обода СОВПАДАЮТ с внутренними контурами двух граней. Герметично по
  построению (общие вершины, никакого произвольного клиппинга). Сетка — по ГЛОБАЛЬНЫМ счётчикам на ось
  (иначе соседние грани дают разное число делений по общей оси → рассинхрон ребра; это ловил тест).
- **Гейт диспетчера**: отверстия только на ПЛОСКОМ сплошном кубе (не hollow/rim/squircle/выгиб/логотипы —
  плоская стенка нужна для точного ствола). Наклон допустим: `taperBoxTris` — попер-вершинное отображение,
  общие вершины двигаются вместе → меш остаётся замкнутым.
- **Состояние**: `boxHoles` (как `logos`) — per-model (record/`saveActiveModel`/`loadModel`/dup), карточки
  (грань, смещение u/v, диаметр), `clampHoleToFace` держит отверстие в грани, сохраняется в JSON-конфиг.

Проверка: `test_holes.js` (22) — герметичность разных конфигов (центр/смещение/несколько на оси/3 оси/
не-куб), убыль объёма, гейт (holes игнорятся при hollow/squircle/fillet), наклон+отверстие герметичны,
кламп. Браузер: добавление через UI → пробитый водонепроницаемый меш; JSON round-trip сохраняет отверстие.
Батарея **497/497 в 18 файлах**.

## Этот проход: крышка для контейнера (модель-компаньон)

Кнопка «+ Крышка для активной» в секции моделей. `addLidForActive()` берёт габариты активной модели и
создаёт компаньон — неглубокий ПОЛЫЙ бокс, перевёрнутый `rx=180` (открытой стороной вниз = колпак):
- `width/depth = габарит + 2·стенка + 2·зазор(0.4)`, `height = lipDepth(6) + стенка` — внутренняя полость
  колпака шире внешнего габарита контейнера, поэтому крышка надевается сверху со скользящей посадкой.
- Целиком на существующем `buildHollowBox` (никакого нового билдера) → всегда водонепроницаема; логика =
  `addModel` + вычисленные параметры + `rx=180` + авто-смещение вправо.

Проверка: браузер — контейнер 50×40×60 (стенка 3) → «+ Крышка»: добавлена «Крышка» (hollow, rx=180),
`width=56.8`, `depth=66.8`, `height=9`, внутренняя полость 50.8 > 50 (зазор 0.4/сторону), меш
водонепроницаемый (0 открытых кромок), ошибок нет. Node-батарея — 475/475 (это UI).

## Этот проход: текст как рельеф (переиспользует пайплайн логотипов)

Добавили поле «Текст рельефом…» + кнопка «+ Текст» рядом с «+ Добавить логотип». Ключевая идея —
НЕ городить отдельный текстовый билдер, а свести текст к тому же heightmap, что и картинку:

- `makeTextLogoResult(text, bold)` рисует строку НЕПРОЗРАЧНЫМИ глифами на ПРОЗРАЧНОМ canvas; размер шрифта
  фитится под ~88 % ширины (ограничен высотой). `analyzeLogoImageData` при наличии прозрачности берёт
  альфа-канал → буквы выпуклые, фон плоский, без угадывания инверсии. Возвращает тот же
  `{heightmap, previewUrl, suggestedInvert}`, что и загрузка картинки.
- Добавление логотипа вынесено в общий `addLogoFromResult(result)` (переиспользуется картинкой и текстом).
  Значит текст автоматически совместим со всем: любая грань, поворот, все режимы/скругления, зональная
  сетка, и сохраняется в JSON-конфиг (через `previewUrl`).

Проверка: браузер — ввод «ABC» → «+ Текст»: логотип добавлен на свободную грань, heightmap с «чернилами»
(max=1), превью — PNG data URL, меш водонепроницаемый (0 открытых кромок), число треугольников выросло
относительно пустого куба; ошибок нет. Node-батарея — 475/475 (это UI).

## Этот проход: импорт/экспорт конфигурации сборки (JSON)

Сборка жила только в сессии — перезагрузка теряла всё. Добавили сохранение/загрузку в JSON.

- **Экспорт** (`exportAssemblyJSON`): сериализует все модели (`params`, `rx/ry/rz`, `px/py/pz`, `visible`,
  `color`, логотипы). Логотипы сохраняют `previewUrl` (PNG data URL), а НЕ сырой heightmap — файл остаётся
  компактным (в тесте сборка с логотипом → 5 КБ). Перед экспортом коммитит staged-правки и `saveActiveModel`.
- **Импорт** (`importAssemblyJSON`): по каждому логотипу `restoreLogoFromDef` заново вычисляет heightmap из
  `previewUrl` (async, через тот же `analyzeLogoImageData`, что и загрузка картинки); затем сносит текущие
  меши, пересобирает `models`, и для каждой модели `loadModel + regenerate` (как в `addModel`). Толерантно к
  неполным данным: `Object.assign(defaultBoxParams(), md.params)`, логотипы с несмогшей декодироваться
  картинкой отбрасываются. Проверка формата по полю `format`.

Проверка: браузер (Chromium/Playwright) — round-trip сборки из 2 моделей (одна с логотипом, вторая полая с
цветом/толщиной): после import число моделей, параметры, цвет, поворот логотипа сохранены, heightmap
восстановлен (нужного размера), обе модели водонепроницаемы, ошибок нет. Node-батарея — 475/475 (это UI).

## Решение: 12-радиусное асим. скругление солид-куба — оставляем

По плану была уборка «убрать 12-радиусное асим. скругление». При разборе: (1) его матчасть
(`asymRadiiFromGroups`, `buildAsymBoxSurfaces`) используется скруглённым полым/бортом — удалить нельзя;
(2) солид-путь `buildAsymRoundedBox` даёт ВЫБОРОЧНОЕ скругление рёбер (например, только верхние),
которое супереллипсоид не заменяет; (3) от него зависят комбо «выгиб+скругление», `asym-12-radius.js`,
`NOTES.md` и тесты. По согласованию с владельцем — оставили как есть.

## Этот проход: пресеты формы (группировка скруглений)

Взаимоисключающих настроек скругления много (углы / squircle / выгиб стенок / скруглённый кант / режим
дна-верха) — порог входа высокий. Добавили **панель пресетов** над параметрами: «Сплошной / Скруглённый /
Squircle / Надутый / Контейнер / Лоток / Бочка».

- `PRESETS` — набор оверрайдов на каждую форму; `applyPreset(name)` сбрасывает только «оформительские»
  ключи (`PRESET_TREATMENT_KEYS`: скругления, squircle, выгиб, hollow/rim/lattice, стенка/борт) к дефолтам
  и применяет оверрайды. Размеры, сегменты, наклон, размер сетки и логотипы НЕ трогаются — пресет
  переформовывает текущую модель, не выбрасывая её размер/арт.
- Применение: `refreshActiveModelUI()` (синк всех `data-key`-инпутов) + `applyPending()` (пересборка +
  снятие подсветки «Подтвердить»). Всё через существующие механизмы, все параметры уже генерятся из
  `SHAPE_PARAMS.box`.

Проверка: браузер (Chromium/Playwright) — все 7 пресетов применяются, инпуты синхронизируются, предыдущее
скругление сбрасывается, меши водонепроницаемы (0 открытых кромок), ошибок страницы нет. Node-батарея без
изменений — 475/475 (пресеты — UI).

## Этот проход: чистки — ASCII-экспорт убран, слайдеры детализации слиты в один

Две мелкие уборки по запросу владельца:
- **ASCII STL убран** — практически неиспользуемый и раздувает файл. Удалены выпадающий выбор формата,
  `trisToAsciiSTL` и его CSS; экспорт всегда пишет Binary STL.
- **Единый слайдер детализации логотипов.** Были две ручки — «Детализация (сплошной куб)»
  (`logoResolution`) и «Детализация (полый контейнер)» (`logoResolutionHollow`); после того как зональная
  сетка появилась во всех режимах, разделение стало избыточным и путающим. Осталась одна общая
  «Детализация логотипов» (`logoResolution`), она драйвит все режимы. Переменная/инпут/обработчик
  `logoResolutionHollow` удалены, все места геометрии переведены на `logoResolution`. Тесты
  (`test_hollow_resolution.js` переписан под единый контроль; `test_squircle/lattice/hollow_inner`
  переведены на `logoResolution`). Батарея — **475/475**. Браузер: экспорт Binary работает, второй слайдер
  исчез, ошибок нет.

## Решение: CSG / булево объединение моделей — не делаем

По плану была задача «булево объединение моделей (CSG)». По согласованию с владельцем снята: надёжный
булев движок мешей (BSP-деревья, компланарные грани, численная устойчивость) в самодостаточном файле без
зависимостей — очень крупная и рискованная по герметичности работа, а практическая польза мала — слайсеры
штатно объединяют пересекающиеся оболочки при нарезке (все видимые модели уже выгружаются в один STL,
`mergedExportTris`). Ограничение переформулировано в README как осознанное. На этом список задач закрыт.

## Этот проход: осевая плотность скругления отдельным контролом (`filletAxisRes`)

`filletSeg` задаёт гладкость ДУГИ (сегменты скругления), а плотность ОСЕВОЙ сетки (плоские участки
между скруглёнными рёбрами) у asym-билдеров раньше жёстко выводилась из него (`max(8, filletSeg*2)`).
Теперь это отдельный параметр «Скругление: плотность осевой сетки (0=авто)» (`filletAxisRes`).

- Хелпер `asymAxisRes(p)`: `0` → прежняя связка `max(8, filletSeg*2)` (обратная совместимость), любое
  `>0` задаёт плотность напрямую (мин. 2), независимо от `filletSeg`.
- Применён во всех 4 asym-путях: скруглённый полый, скруглённый борт, сплошной asym и asym+выгиб.
- Squircle-пути не трогали — там своя механика плотности.

Проверка: `test_rounded_fillet.js` расширен (20/20): auto=0 водонепроницаемо; высокая плотность добавляет
треугольники (при неизменном `filletSeg`), низкая — грубее auto; тот же контрол на скруглённом полом —
водонепроницаемо. Браузер: контрол рендерится (input `data-key="filletAxisRes"`), ошибок нет. Батарея
**476/476 в 17 файлах**.

## Этот проход: Web Worker для проверки герметичности (неблокирующая диагностика)

Проверка водонепроницаемости (`manifoldCheck`) — последний тяжёлый синхронный шаг на главном потоке
(~0.7 с на 120k тр.); прежний `setTimeout(…,0)` только отодвигал её за кадр, но, запустившись, она всё
равно блокировала UI (тот самый «фриз»). Вынесли её в **Web Worker**.

- **Источник воркера = сама `manifoldCheck`, сериализованная через `.toString()`** + крошечный
  onmessage-хендлер. У функции НОЛЬ внешних зависимостей (только Map/Int32Array/Math), поэтому дублирования
  логики нет и сборка не нужна — off-thread крутится ровно та же процедура. Треугольники передаются
  transferable-буфером **Float64Array** (9 double/тр.) — именно Float64, чтобы координаты были бит-в-бит
  как на главном потоке и результат склейки/рёбер совпадал с inline-проверкой.
- Воркер создаётся лениво из Blob-URL; если `Worker` недоступен или заблокирован — fallback на прежний
  `setTimeout`-путь. Токен `statCheckGen` отбрасывает устаревший ответ, если пришла новая пересборка.
- Потолок проверки поднят со 120k до 600k треугольников (раз она больше не блокирует поток).
- **Генерацию геометрии в воркер НЕ выносили — осознанно.** Вся геометрия живёт на верхнем уровне одного
  файла и завязана на глобалы (`paramState`, `logos`), а Node-тесты (470 шт.) вызывают `buildTrisForShape`
  и др. напрямую из top-level scope. Воркер потребовал бы либо дублирования ~2500 строк, либо обёртки всей
  геометрии в функцию — что сломало бы либо принцип «один файл без сборки», либо тест-харнесс. Выигрыш
  мал (генерация уже за debounce + кнопкой «Подтвердить»), риск для рабочего инструмента велик. Поэтому
  вынесли именно диагностику — конкретный задокументированный источник фриза.

Проверка: реальный браузер (Chromium/Playwright) — воркер создаётся, его результат бит-в-бит совпадает с
inline `manifoldCheck` и на герметичном (супереллипсоид 2208 тр.), и на заведомо открытом меше (4 кромки);
бейдж «замкнутый ✓» обновляется через ответ воркера после реальной пересборки; ошибок страницы нет.
Node-батарея без изменений — **470/470** (воркер браузерный, тесты его не трогают).

## Решение: сетчатое дно остаётся только на плоском полом (не борт / не скруглённый)

По плану была задача «сетчатое дно для борта и скруглённого полого». При разборе оказалось, что фича
как сформулирована не имеет осмысленной реализации: диагональная решётка — это СКВОЗНЫЕ отверстия для
дренажа, а вести им некуда. У борта/лотка под карманом сплошная база; у скруглённого полого дно —
купол (ровные ромбы по кривизне определены плохо и практической пользы не несут). По согласованию с
владельцем задача снята; ограничение переформулировано в README как осознанное, а не «на будущее».

## Этот проход: зональная сетка логотипов на ПЛОСКОМ полом контейнере / борте

Раньше зональная сетка (грубая оболочка + мелкая детализация только под логотипом) была у сплошного
куба и у СКРУГЛЁННОГО полого/борта, а у ПЛОСКОГО полого/борта — нет: `logoResolutionHollow` гнал
плотность ВСЕЙ оболочки, поэтому крупная детализация логотипа требовала перегенерации всего меша.
Теперь плоский полый/борт тоже зональный.

- **`buildHollowBox` / `buildRimBox`** приняли `zonesByAxis` + `logoRes`. Когда есть логотипы (и не
  сетчатое дно): база оболочки строится ГРУБОЙ (`baseRes=min(res,4)` — плоская/наклонная стенка
  планарна, нескольких ячеек хватает), а под каждым логотипом добавляются мелкие подразбиения
  (`addLogoZonesToAxis`, как у скруглённых билдеров). Без логотипов — прежняя равномерная сетка
  (plain / только-выгиб / сетчатое дно не меняются).
- **Выгиб стенок**: полноразмерные зоны выгиба (`computeWallBulgeZones`) домешиваются к зонам
  логотипов (`mergeZones`), поэтому купол по-прежнему получает сетку на всю грань.
- **Фикс верхнего обода (нашёл фаззер-стиль баг в тестах).** Зональные точки попадают и в УГЛОВЫЕ
  полосы верхнего обода (`[-hd,-(hd-t)]` и т.п.), а обод раньше собирался одиночными квадратами
  (`addQuad`) + полосами с 2-точечными краями — лишние точки стенки давали T-стык (пример: полый+выгиб+
  лого — 12 открытых кромок; экстремальная толщина стенки + лого дна — 56). Обод переписан как
  **банд-сетка 3×3** (`xLo/xMid/xHi` × `zLo/zMid/zHi`, минус центральный проём) по фактическим срезам
  позиционных массивов — каждая вершина обода теперь совпадает с верхними кромками стенок. То же в борте.
- Диспетчеры плоского полого/борта считают `zones = computeLogoZonesByAxis(taperFn)` (+bulge при выгибе)
  и передают их вместе с `logoResolutionHollow` как плотность зон.

Проверка: `test_hollow_inner_logo.js` расширен (18/18): зональная оболочка с логотипом дешевле
равномерной при той же высокой детализации; логотип у самого вертикального ребра (зона заходит в
угловую полосу обода) — водонепроницаемо; выгиб+лого — водонепроницаемо; борт с логотипом — зонально и
водонепроницаемо. Батарея **470/470 в 17 файлах**.

## Этот проход: раздельное скругление верха и низа супереллипсоида

Единый параметр «Squircle: скругление верха/низа» (`squircleV`) разбит на ДВА независимых —
«скругление верха» (`squircleVTop`) и «скругление низа» (`squircleVBot`). Теперь верх и низ
скругляются по отдельности: 0 на стороне = плоская крышка там; можно сделать куполообразный верх
при плоском дне и наоборот.

- **`buildSuperellipsoid`** (no-logo путь) принимает два экспонента полюсов: `phi>=0` берёт `eVTop`,
  `phi<0` — `eVBot`. При `eV=0` на стороне `seС/seS` дают плоскую крышку (лишние вырожденные кольца
  отфильтровываются), при этом прямая стенка от экватора до крышки строится штатным квадом.
- **`buildSuperellipsoidLogos`** (cube-sphere путь с логотипами): экспонент выбирается по знаку `dy`
  в `seImplicitSurface`/`seImplicitNormal`. Ключ к герметичности — на экваторе (`y=0`) горизонтальное
  сечение решается в `(|x|/hw)^aH+(|z|/hd)^aH=1` НЕЗАВИСИМО от вертикального экспонента, а `ny=0`
  (aV>1), поэтому и поверхность, и нормализованная нормаль непрерывны через `dy=0` даже при разных
  экспонентах верх/низ. Значит бок-грани cube-sphere, пересекающие экватор, остаются водонепроницаемыми.
- **Полый контейнер / борт**: используют `squircleVBot` для скругления ДНА (верх открыт плоским бортом,
  `squircleVTop` — только для сплошного куба). `buildSquircleHollow` не менялся, поменялся лишь источник
  `eV` в диспетчере.
- Диспетчер сплошного куба: `eVTop>0 || eVBot>0` → супереллипсоид с раздельными экспонентами; оба 0 →
  плоский squircle-призм (`buildSquirclePrism`).

Проверка: `test_squircle.js` расширен (миграция `squircleV`→`squircleVTop/Bot`; для полого/борта —
`squircleVBot`) + новая секция «независимое верх/низ» (top-only / bottom-only водонепроницаемы и дают
разный объём, чем оба; асимметрия + логотипы на обоих полюсах и боку). Батарея **464/464 в 17 файлах**.

## Этот проход: несколько моделей (сборка)

«Можно ли добавлять и настраивать больше одной модели?» — да. Дизайн: ГЛОБАЛЫ приложения
(paramState.box, logos, transformState, новый positionState, cachedRawTris/currentTris) всегда держат
состояние АКТИВНОЙ модели — весь существующий код работает без изменений. Каждая модель — запись
{params, logos, rx/ry/rz, px/py/pz, visible, color, rawTris, bbox}; переключение = сохранить-глобалы-в-
запись / загрузить-запись-в-глобалы (`saveActiveModel`/`loadModel`) + обновить панели
(`refreshActiveModelUI`, карточки лого/моделей). Геометрия при переключении НЕ пересобирается — у записи
есть кэш rawTris, активной модели хватает rotate-only пути.

- **Карточки моделей** (как карточки логотипов): добавить (до 12) / дублировать / удалить / скрыть /
  переименовать; активная подсвечена. Новая модель ставится справа от сборки (авто-px). Дубль — рядом с
  оригиналом, логотипы копируются (heightmap разделяется по ссылке — он неизменяемый).
- **Позиция модели (px/py/pz)** — новый живой путь без пересборки (как поворот): двигается только
  THREE-меш (`applyPositionOnly`), позиция применяется к треугольникам лишь при экспорте.
- **Сцена**: THREE.Mesh на модель (свой материал → свой цвет), `mesh` всегда указывает на активный —
  цвет/шейдинг/каркас работают как раньше (шейдинг применяется ко всем). Сетка-пол — по минимальному Y
  всех видимых; рамка камеры и «сбросить вид» — по union-bbox сборки; размерные линии — по активной.
- **Экспорт**: `mergedExportTris()` — все видимые модели, каждая повёрнута и смещена; один STL с
  несколькими оболочками (слайсеры это штатно едят). В сводке — «Треугольники (экспорт)».
- **Кнопка «Подтвердить»** работает по-прежнему на активной модели; переключение карточки сначала
  коммитит незакреплённые правки (applyPendingNow), чтобы запись сохранила то, что в полях.
- Стабы для Node-тестов расширены (персистентные элементы по id, createElement/appendChild), у
  debounce-теста сохранён алиас fakeStyle.

Проверка: `test_multi_model.js` (33) — снапшот/восстановление параметров-лого-поворота-позиции,
независимость дубля, авто-смещение новой модели, merge экспорта (скрытая модель исключается, поворот и
смещение применяются), union-bbox, удаление активной активирует соседа, последняя модель защищена.
Батарея **457/457 в 17 файлах**. Браузер: сценарий целиком через реальные инпуты/кнопки — добавление,
настройка полого контейнера у модели 2, живое смещение (без пересборки), переключение восстанавливает
поля, merge-STL с корректным числом байт, dup/del, скрытие убирает из сцены и файла; скриншот сборки.

## Этот проход: дебаг-сессия — фаззер нашёл 3 бага, все исправлены

Прогон: батарея (зелёная) + сидированный фаззер (800 случайных комбинаций режимов/фич/логотипов/наклона,
проверка исключений, NaN, водонепроницаемости, знака объёма) + smoke-тест живого приложения в браузере.

**Баг 1 — Т-стык на «прямоугольном» squircle (rim/hollow, squircle≈8 %).** У почти-прямоугольного
суперэллипса три угла угловой ячейки картезианской заливки коллинеарны (лежат на прямом участке кромки
z=hd): одна половинка ячейки — вырожденный треугольник, фильтр его удалял → Т-стык со стенкой (6 открытых
кромок). Фикс: `pushQuadAdaptive` — ячейка делится по той диагонали, где обе половинки невырождены
(max(min area)); применён в плоских крышках, «шапке» купола и гранях cube-sphere.

**Баг 2 — ложные «открытые» кромки на лезвии округления (rounded hollow + сильный наклон).** Две
соседние заплатки скруглённого полого считают одну вершину дуги с разницей ~2e-15; после наклона обе
попадают ровно на границу букета `toFixed(5)` (…0875|) и разъезжаются по разным ключам → manifoldCheck
рапортовал 24 «открытых» кромки на геометрически замкнутом меше (при 7 знаках — 0). Фикс: сварка вершин
в `manifoldCheck` теперь честная допусковая — union-find по пространственному хэшу с радиусом ~полбукета,
не зависит от положения относительно десятичной границы. Семантика (decimals) сохранена.

**Баг 3 — пол 1 мм в клампе логотипа.** При радиусах 11+12 на полом высотой 24 мм плоская полоса стенки —
1 мм, а `clampLogoToFace` принудительно давал логотипу минимум 1 мм (`Math.max(1,…)`) → рельеф вылезал в
цилиндры скругления (4 открытых кромки). Фикс: честный кламп `Math.max(0,…)` — если места нет, логотип
схлопывается в 0 и ничего не рисует (dispFn возвращает 0 — безопасно).

Регрессы закреплены: `test_squircle.js` +4 (боксовые squircle rim/hollow ± squircleV),
`test_fillet_logo_hollow.js` +2 (экстремальные радиусы + микро-лого; наклон без ложных «открытых»).
После фиксов: фаззер **800/800**, батарея **424/424**. Smoke живого приложения: загрузка без ошибок
(кроме заблокированных в песочнице Google Fonts — штатный фолбэк), staged-apply работает (кнопка
подсвечивается, модель не трогается до нажатия, после — пересборка, кнопка гаснет), панель «Меш:
замкнутый ✓», бинарный STL (84+50·n байт, счётчик в шапке) и ASCII STL (solid/facet/endsolid) валидны,
раунд-трип режимов (полый squircle → сплошной) водонепроницаем.

## Этот проход: картезианская «шапка» на полюсе скруглённого дна (логотипы на дне)

Скруглённое дно раньше закрывалось веером у полюса → центральный донный лого дал бы «спицы», поэтому дно
строилось без эмбосса. Теперь веер заменён картезианской «шапкой»: `superellipseDomeFan`→`superellipseDomeCapped`.
- Крутая часть (радиус 1→rCap=0.68) — прежние кольца по высоте.
- Пологий центр (радиус <rCap) — картезианский fill-грид (`g`, отмасштабированный на rCap; высота по профилю
  `yD`). Центр — обычная valence-4 вершина, поэтому донный логотип (`-Y` / `-Y-inner`) embossed вдоль
  численной нормали без «спиц».
- Шов кольцо↔шапка водонепроницаем: граница шапки = `g_boundary*rCap` = переходное кольцо `ring*rCap`
  (общий множитель, точное совпадение), высота обеих = yT.
- Зональное сгущение работает и для шапки (`squircleABYZones` уже сгущает a,b под `-Y`/`-Y-inner`).

Проверка: рег-тесты — донный лого на скруглённом дне водонепроницаем, без веера (valence≤12) и реально
рельефит (треугольников больше, чем без лого); то же для пола полости. Браузер: череп-эмблема на скруглённом
дне контейнера — чисто, без спиц. Батарея **418/418**.

## Этот проход: элипсоидность ко дну контейнера (squircleV скругляет дно)

Раньше в полом/борте squircleV игнорировался (плоское дно). Теперь он скругляет ДНО контейнера как нижнюю
половину супереллипсоида: прямые стенки в верхней половине [0,hh], куполообразное дно в нижней [-hh,0]; 0 —
плоское дно (как было), 100 — глубокая чаша. Верх остаётся открытым с плоским бортом.

- Дно строит `superellipseDomeFan(ring, N, poleY, invEv, sign, nRings)`: концентрические супереллипс-кольца,
  сэмплированные по ВЫСОТЕ y (важно: сэмплинг по y ровно распределяет точки по профилю — и почти
  вертикальный участок у y=0, и пологий у полюса; сэмплинг по горизонтальному радиусу, как в первой попытке,
  недосэмплировал крутую часть и уводил границу с y=0 → шов). Горизонтальный масштаб
  `rS(y)=(1-(y/poleY)^(2/eV))^(eV/2)`. Уровень 0 при y=0 = нижнее кольцо стенки (общие вершины → шов
  закрыт), вниз до полюса с веером.
- Полый (innerDome): полость повторяет купол (внутренний полюс на floorY=-hh+t → толщина стенки t). Борт:
  внешнее дно скругляется, неглубокий карман сверху остаётся плоским (`superellipseFillCap`).
- `buildSquircleHollow` получил параметры `eV` и `innerDome`; при eV≤0 — прежний плоский путь без изменений
  (регрессы целы). Маршруты полого/борта теперь передают eV из squircleV.

Ограничение: логотип на самом скруглённом дне (`-Y` / `-Y-inner`) не проецируется — центральный лого попал
бы на полюсной веер (спицы); дно строится без эмбосса. Боковые лого на стенках работают.

Проверка: `test_squircle.js` +19 — водонепроницаемость по squircleV 20..100 (полый и борт), «скруглённое
дно убирает материал» (объём меньше плоского), «меш меняется», не-куб, полный 100/100, наклон, лого на
стенках. Браузер: контейнер со скруглённым дном (40 % — мягкая фаска снизу, 100 % — глубокая чаша),
водонепроницаемо. Батарея **413/413**.

## Этот проход: зональное сгущение сетки squircle-логотипов (дёшево при 500)

Равномерная сетка при высокой детализации тратит треугольники на всю поверхность, хотя лого занимает
маленький кусок. Теперь сетка ГРУБАЯ везде и МЕЛКАЯ только под лого (та же схема, что у зонального грида
плоского куба).

- `zonedSamples(lo,hi,coarseStep,fineStep,zones)` — грубые сэмплы везде + мелкие в интервалах-зонах (с
  потолком на зону, чтобы огромный футпринт не взорвал число точек).
- `squircleABYZones(p,tInner)` — из футпринта каждого лого считает зоны по осям: крышечный лого (+Y/−Y/пол
  полости) сгущает a и b; боковой — свою ось периметра (a или b, чтобы граничное кольцо было мелким там) и
  высоту y. Нормированные a=x/hw, b=z/hd в [−1,1]; y в мм.
- `superellipseFillGrid` теперь принимает явные массивы `aSamp×bSamp` (не квадрат G×G); `fillGridBoundaryLoop`
  и `superellipseFillCap` работают с прямоугольным гридом. Внешняя и внутренняя оболочки полого делят ОДНИ
  a,b-массивы, поэтому граничные кольца совпадают по числу точек (борт-кольцо сшивается).
- `squircleWall` берёт явный `ySamp` вместо `Mv` (мелко по высоте под боковым лого, грубо у полости).
- Супереллипсоид (cube-sphere): ГЛОБАЛЬНЫЕ по осям cube-координаты (общие для всех 6 граней → каждое общее
  ребро семплится одинаково → водонепроницаемо). Зоны находятся грубым forward-hit-тестом: где
  `applySurfaceLogo` сместил вершину — туда попадает интервал сгущения по двум cube-осям грани.
- Нижний порог мелкого шага `fineW` 0.05→0.1 мм (при 500 это и так тоньше пикселя heightmap-а, так что
  визуально не хуже, а треугольников вдвое меньше).

Замер (браузер, лого на +Y, детализация 500): супереллипсоид 22-мм лого — **319k** тр., ~1.0 с (было ~811k,
2.5 с равномерно); призма 473k; полый 575k. Маленький 12-мм лого — 280k; без лого грубая оболочка — 14k.
Та же чёткость (череп на верхушке — гладкие глазницы, свод, челюсть). Регресс-тест: маленький лого даёт
существенно меньше треугольников, чем полногранный (`small < big*0.7`) — и на крышке, и на боку. Батарея
**396/396**; водонепроницаемость и valence≤12 при высокой детализации сохраняются.

## Этот проход: детализация логотипов до 500

«Увеличь детализацию до 500 во всех случаях». Ограничивали ТРИ вещи, а не только ползунок:
- ползунки «Детализация» (`max=300`) и их `change`-обработчики (`Math.min(300, v)`) → подняты до **500**;
- внутренние потолки сетки squircle-билдеров (cube-sphere `S≤176`, призма `G≤220`, полый `G≤220`) —
  подняты до `S≤260`, призма `G≤260`, полый `G≤200` (полый чуть ниже — две оболочки, тяжелее);
- нижний порог ячейки `spacing` 0.15→0.05 мм, чтобы мелкие объекты тоже дотягивались до высокой детализации.

Замер (браузер, 40-мм объект, лого на +Y): дефолт `res=50` — 120k тр., ~0.33 с; максимум `res=500` —
супереллипсоид ~811k тр. ~2.5 с, призма ~803k ~2.3 с, полый ~981k ~2.5 с. Перегенерация одноразовая (по
кнопке «Подтвердить»), водонепроницаемая проверка в панели и так пропускается на мешах >120k
(`MANIFOLD_CHECK_TRI_LIMIT`), так что джанка нет; на реальном GPU ~1 млн треугольников рисуется без
проблем. Дефолт не тронут (быстрый). Череп на верхушке супереллипсоида при 500 — гладкие круглые
глазницы и свод, ступенек нет.

Тест-гард на поднятые потолки: сборка при `logoResolution=300` (упирается в `S=260`, ~0.7 млн тр.) —
водонепроницаемо и без веера (valence ≤12). Логотипные тесты гоняются на облегчённом гриде (density-
независимы), поэтому батарея не тормозит: **394/394**.

## Этот проход: плотнее сетка эмбосса логотипов на squircle (глаже края)

Логотип на squircle/супереллипсоиде оставался ступенчатым: эмбосс-грид (`spacing = maxDim/logoRes`, ~50
сэмплов на грань) сильно грубее heightmap-а (256², т.е. ~0.09 мм/пиксель на лого 20 мм). Плюс крышки были
жёстко ограничены потолками (супереллипсоид `S≤96`, призма/полый `G≤120`), так что поднятие ползунка
«Детализация» переставало помогать.

- Базовая плотность ×2: `spacing = max(0.15, maxDim/(logoRes*2))` во всех трёх билдерах (призма,
  cube-sphere супереллипсоид, полый/борт) — вдвое мельче ячейка, вчетверо больше треугольников, края лого
  вдвое глаже.
- Потолки подняты (cube-sphere `S` 96→176, `G` призмы/полого 120→220), поэтому ползунок «Детализация»
  (до 300) снова работает — на максимуме ячейка ~0.2–0.35 мм.
- Свойства меша не изменились (водонепроницаемость, valence, знаки объёма — density-независимы), поэтому
  логотипные тесты гоняются на облегчённом гриде (`logoResolution=24` в `test_squircle.js`), чтобы батарея
  не тормозила; приложение по умолчанию плотнее. Батарея **392/392**. Браузер: тот же череп на верхушке
  супереллипсоида заметно чётче (круглее глазницы, глаже свод/челюсть).

## Этот проход: фикс искажения логотипов на верхушке/дне (полюс/крышка)

Баг (со скриншотом от пользователя): логотип на ВЕРХУ супереллипсоида размазывался в радиальные «спицы».
Причина — топология сетки на полюсах/крышках: и супереллипсоид (lat/long кольца), и плоские крышки призмы
/полого (радиальный диск с веером из центра) СХОДЯТСЯ в одну апексную вершину. Эмбосс детального высотного
поля на веер из центра → лучевые спицы. Стенки (боковой прямоугольный грид) были в порядке — проблема только
на «шапках».

Исправление — убрать полюсную/веерную топологию там, где садится логотип:
- **Супереллипсоид → cube-sphere.** Вместо колец полюс-в-полюс поверхность строится как 6 «раздутых»
  граней куба, каждая — регулярный картезианский патч. Направление грани куба точно проецируется на
  супереллипсоид неявной формулой `F(t·d)=1 ⇒ t=G(d)^(−eV/2)` (`seImplicitSurface`). Верх/низ — обычные
  valence-4 области, без сингулярности. Нормали АНАЛИТИЧЕСКИЕ (`seImplicitNormal`, ∇F) — функция только от
  точки, поэтому вершина на общем ребре двух граней получает одинаковую нормаль (и одинаковый эмбосс) с обеих
  сторон → водонепроницаемо даже когда лого пересекает ребро куба.
- **Плоские крышки (призма верх/низ, полый: внешнее дно + пол полости) → картезианская заливка.**
  `superellipseFillGrid`: единичный квадрат отображается на диск (elliptical grid mapping), затем растягивается
  до границы-супереллипса — центр изотропно-по-осям (лого не искажается), граница — точный супереллипс, а
  ЦЕНТР — обычная valence-4 вершина, без веера. Граница заливки = периметр `fillGridBoundaryLoop`, и его же
  экструдирует стенка, поэтому шов сторона↔крышка закрыт. Радиальный `squircleDiscCap` и uniform-θ
  `squircleRingAndNormals` удалены.

Регресс-тест прямо на баг: `maxVertexValence(tris)` — у веерной вершины валентность ≈ N (число сегментов), у
картезианского/cube-sphere грида ~6. Проверки требуют ≤12 для логотипов на +Y/−Y призмы, полюсах
супереллипсоида, дне и полу полости. `test_squircle.js` 77→86, вся батарея **392/392**. Браузер: череп на
верхушке супереллипсоида и на плоской крышке призмы — читается чисто (свод, глазницы, нос, челюсть), без спиц,
`manifoldCheck` watertight.

## Этот проход: squircle как ПОЛЫЙ контейнер и БОРТ/лоток

Задача: «вынеси [squircle] на полый/борт». Раньше ветка `p.squircle>0` стояла ПОСЛЕ веток `p.hollow`/
`p.rim` в `buildTrisForShape`, поэтому срабатывала только на сплошном кубе. Теперь squircle проверяется
в САМОМ НАЧАЛЕ обеих веток и перекрывает филлет/сетчатое дно (squircle — это и есть обработка угла).

Общий билдер `buildSquircleHollow(w,h,d,p,t,floorY,...)` — оболочка из супереллипса:
- внешняя стенка (супереллипс `hw,hd`, y −hh..hh) — `squircleWall(..., sign=+1)`, нормали наружу;
- полость (супереллипс `hw−t,hd−t`, y floorY..hh) — `squircleWall(..., sign=-1)`, нормали в пустоту;
- внешнее дно (диск `hw,hd` при −hh, нормаль −Y) и дно полости (диск `hw−t,hd−t` при floorY, +Y) —
  `squircleDiscCap` (радиальная сетка концентрических супереллипсов);
- верхний борт — плоское кольцо-аннулюс между внешним и внутренним верхними кольцами.
Один и тот же билдер обслуживает оба режима, отличается только `floorY`: полый = −hh+t (тонкое дно у
низа), борт = hh−rimHeight (неглубокий карман сверху, толстая база снизу).

Толщина стенки: внутренний супереллипс = внешний, ужатый по габаритам на `t` (тот же показатель) —
толщина ≈ t на «плоских» участках и слегка гуляет на углах (та же оговорка, что у бокс-полого). Верх/низ
рёбра НЕ скругляются (squircleV игнорируется) — открытый контейнер стоит на плоском дне и имеет плоский
борт. Водонепроницаемо: каждая поверхность — замкнутый грид, а швы (стенка↔крышка, стенка↔борт) лежат на
несмещённых крайних кольцах, поэтому эмбосс логотипов их не трогает.

Логотипы: внешние стенки embossed через `applySurfaceLogo` (грань по доминантной оси нормали, как у
сплошного squircle); внешнее дно — по `dispFns['-Y']` вдоль −Y; дно полости/кармана — по
`dispFns['-Y-inner']` вдоль +Y (обе крышки — радиальные диски, эмбосс = 0 на границе через кламп). Стенки
полости и борт-кольцо — без рельефа. Наклон применяется после (`taperBoxTris`), фреймы в плоском
пространстве — та же конвенция.

Проверка: `test_squircle.js` расширен 50→77 (полый: роундность, толщина стенки, не-куб, наклон, «полость
< сплошного», «squircleV игнорируется»; борт: роундность, rimHeight, наклон, «борт > глубокого полого»;
логотипы на стенках/дне/полу полости, гравировка, два лого, лого+наклон). Вся батарея **383/383**. Браузер:
полый squircle-контейнер (открытый верх, тонкие стенки, плоский борт), борт с неглубоким карманом, лого
оборачивается по изогнутой стенке — `manifoldCheck` в живом приложении watertight.

**На будущее:** сетчатого дна на squircle-полом нет (там другой билдер дна). squircleV на контейнере не
поддержан (открытый верх + скруглённый = странно).

## Этот проход: логотипы на squircle / супереллипсоиде (emboss на изогнутую стенку)

Задача: «сделай совместимость логотипов с последними нововведениями» — т.е. с squircle-призмой и
супереллипсоидом. Раньше маршрут `p.squircle>0` строил гладкую оболочку и логотипы игнорировал (плоская
грань, на которую садится рельеф, там отсутствует).

Механика: логотипный `dispFn` (`makeLogoDispFn`) проецирует 3D-точку на касательный фрейм лого и
возвращает глубину — это ровно то, что нужно кривой стенке, он просто «оборачивает» рельеф по изгибу.
Поэтому вместо плоской грани оболочка строится МЕЛКОЙ (u,v)-сеткой, и в каждой вершине:
- грань выбирается cube-map-стилем по доминантной оси внешней нормали (`applySurfaceLogo`), так что
  вершина берёт рельеф не более чем ОДНОЙ грани;
- вершина смещается вдоль ЛОКАЛЬНОЙ внешней нормали на возвращённую глубину (рельеф ⟂ изогнутой стенке).

Каждая вершина имеет одну позицию, общую для всех своих треугольников → меш остаётся водонепроницаемым
(тот же аргумент «высотное поле над сеткой», что у логотипов на плоском кубе). Лого клампится от рёбер
(`clampLogoToFace`), поэтому рельеф = 0 вдоль всех швов (сторона↔крышка, полюс) — швы закрыты.

- `buildSquirclePrismLogos(w,h,d,p,seg,dispFns,logoRes)`: мелкая боковая сетка (периметр × высота) +
  крышки радиальной сеткой (концентрические супереллипсы), так что лого на +Y/−Y тоже садится на плоскую
  крышку. Верх/низ боковых колец держатся на базовом кольце (рельеф там 0), поэтому точно совпадают с
  граничным кольцом каждой крышки → шов закрыт.
- `buildSuperellipsoidLogos(...)`: одна сетка колец полюс-в-полюс; нормали — центральной разностью
  (полюса как соседи по φ); каждая вершина embossed и смещена вдоль нормали. Полюс — одна вершина.
- Плотность сетки берётся от `logoResolution` (шаг ≈ maxDim/logoRes), с потолками N/M. Путь БЕЗ логотипов
  не тронут (быстрый веерный билдер) — старые проверки объёма/числа треугольников без изменений.
- Маршрут в `buildTrisForShape` (ветка `p.squircle>0`): фреймы строятся в плоском (identity-taper)
  пространстве, наклон применяется после через `taperBoxTris` — та же конвенция, что у fillet+logo.

Проверка: `test_squircle.js` расширен 25→50 (лого на всех гранях призмы + плоские крышки; лого на боках
и полюсе супереллипсоида; гравировка, поворот, наклон, два лого на разных/одной грани, не-куб; регресс
пути без логотипов). Вся батарея **356/356**. Браузер (headless Chromium + Playwright): рельеф-сердечко
embossed на изогнутый бок squircle-призмы, супереллипсоида и два лого на двух боках — визуально
оборачивается по кривизне, `manifoldCheck` в живом приложении = watertight.

**На будущее:** очень широкое лого на боку клампится плоскостью грани, но cube-map-гейт обрезает его на
диагонали 45° (лого не заворачивается за угол) — держать лого по центру грани. Squircle всё ещё только на
СПЛОШНОМ кубе (полый/борт — потом).

## Этот проход: Squircle / супереллипс (то, что имелось в виду под «скруглением стенок»)

Пользователь прислал картинку squircle (иконка iOS) — оказалось, под «скруглением стенок» он имел в
виду СУПЕРЭЛЛИПС, а не подушку-на-грани. Проверка в браузере (headless Chromium + Playwright, Three.js
r128 из npm, локально) подтвердила: выгиб+филлет давали «подушку с горбом на грани» — не то. Добавлен
отдельный билдер.

- `buildSquirclePrism(w,h,d,p,resolution)`: горизонтальное сечение — супереллипс
  `x=a·sign(cosθ)·|cosθ|^p, z=c·sign(sinθ)·|sinθ|^p`, экструзия по высоте, плоские верх/низ (веерные
  крышки). `p∈(0,1]`: 1 — эллипс, ~0.5 — squircle, →0 — прямоугольник. Обмотка через `orient` по
  радиальной нормали; вырожденные треугольники отбрасываются.
- UI-параметр `squircle` 0..100 % (0=off/прямоугольник, 100=эллипс), `p=squircle/100`. Маршрут
  `buildTrisForShape` (сплошной): `p.squircle>0` → `buildSquirclePrism`, наклон поверх
  (`taperBoxTris`). Перекрывает скругление углов и выгиб стенок (это другая трактовка «скругления»).
- Браузерная проверка: `verify.html` (копия с локальным Three.js), Playwright-драйвер с SwiftShader
  (`--use-angle=swiftshader`), скриншоты сверху (сечение = супереллипс, 1-в-1 картинка) и 3D.

Проверка: `test_squircle.js` 16/16 через реальный `buildTrisForShape` — водонепроницаемость на
роундности 8..100 %, не-куб, наклон, знак объёма>0, монотонность (эллипс<квадрат по объёму), squircle
перекрывает филлет, регресс squircle=0 → обычный бокс (12 треугольников). Вся батарея 322/322.

**На будущее:** squircle только на СПЛОШНОМ кубе, верх/низ плоские (можно добавить вертикальное
скругление рёбер → супереллипсоид); логотипы на кривом боку пока не проецируются; полый/борт — потом.

## Этот проход: выгиб стенок повторяется полостью (толщина стенки сохраняется)

Раньше выгиб ехал только по ВНЕШНЕЙ оболочке полого — полость оставалась прямой, стенка утолщалась в
месте выгиба (и вогнутое ограничивалось толщиной стенки). Теперь **внутренняя полость повторяет выгиб**,
двигаясь в ту же мировую сторону, что и внешняя стенка → толщина сохраняется.

- Общий примитив `makeRectDome(amp,uLo,uHi,vLo,vHi)` (купол над произвольным прямоугольником, 0 на
  границе); `makeInsetBulgeDisp` теперь его частный случай.
- **Скруглённый полый/борт:** `buildAsymBoxSurfaces` применяет инсет-купол к ОБЕИМ оболочкам с ОДНИМ
  знаком (не инвертируя, в отличие от логотипов) — внутренняя грань двигается в ту же сторону, что и
  внешняя. Проброшено через `buildRoundedHollow`/`buildSharpRimHollow` (и на внешний, и на внутренний
  вызов). Зоны выгиба теперь ВЛИВАЮТСЯ в `zonesByAxis` (`mergeZones`), иначе середина грани не
  подразбивалась и купол выходил гранёным.
- **Плоский полый:** `buildHollowBox` получил `bulgeAmpsByFace`; 4 стенки полости и дно получают
  `cav()`-купол (знак инвертирован, т.к. нормаль полости смотрит внутрь). Y полости подразбивается
  (`bulgeSubdivRange`), иначе 2-строчная полоса не показала бы купол (он 0 на концах).
- **Борт/лоток:** карман мелкий у самого верха, где купол внешней стенки ≈0, поэтому карман НЕ выгибаем
  (независимый купол там был бы неверным); плоский лоток — только внешние стенки.
- Вогнутый клампится по размеру ПОЛОСТИ (`0.8·(min(hw,hh,hd)−t)`), а не толщине стенки — полость теперь
  тоже двигается, поэтому предел мягче; ограничение — чтобы стенки полости не пересекли центр.

Проверка: `test_wall_bulge.js` 53/53 — добавлено «полость повторяет выгиб» (замер: внутренняя стенка
уехала с 16 на ~22 при внешней на ~26 → зазор = толщине; для плоского/скруглённого/острого полого),
знак объёма > 0, вогнутый под клампом. Регресс — вся батарея `./run-all.sh` 306/306.

## Этот проход: скругление стенок — фаза 2 (со скруглением углов + на полом/борте)

Выгиб граней доведён до полной совместимости. Две части:

1. **Выгиб + скругление углов (сплошной куб).** Купол теперь строится в ИНСЕТ-версии
   (`makeInsetBulgeDisp`): тот же приподнятый косинус, но центрированный и масштабированный на
   плоскую ИНСЕТ-зону грани (между скруглёнными рёбрами). Т.к. `wallBulgeProfile(|x|≥1)=0`, купол
   ноль и на границе плоской зоны, и в угловых «расширениях» → шов с цилиндрами рёбер цел.
   `buildAsymRoundedBox` получил параметр `bulgeAmpsByFace`: в цикле граней инсет-купол СКЛАДЫВАЕТСЯ
   с логотип-dispFn. Маршрут: при `hasWallBulge && hasFillet` → `buildAsymRoundedBox` с выгибом +
   лого(flat) + объединёнными зонами, наклон поверх (`taperBoxTris`).
2. **Выгиб на полом/борте (внешние стенки + дно).** `buildAsymBoxSurfaces` получил `bulgeAmpsByFace`
   (инсет-купол, ТОЛЬКО внешняя оболочка `!flip`); проброшен через `buildRoundedHollow`/
   `buildSharpRimHollow`. Для ПЛОСКОГО полого/борта выгиб просто вливается в per-face dispFns
   (`buildWallBulgeDispFns`), т.к. их внешние грани уже полностью сетчатые. Полость остаётся ПРЯМОЙ →
   вогнутый выгиб клампится по толщине стенки (`wallBulgeAmounts(p, 0.8·t)`), чтобы не прорезать полость.
   Детализация: у полого/борта — `logoResolutionHollow`.

Хелперы обобщены: `wallBulgeAmounts(p, concaveCap)`, `buildWallBulgeDispFns(p, amps)`,
`computeWallBulgeZones(p, amps)`. UI-подпись обновлена (работает во всех режимах, сочетается со
скруглением углов). Убран приоритет-над-филлетом из фазы 1.

Проверка: `test_wall_bulge.js` 41/41 — добавлены выгиб+филлет (в т.ч. асим-радиусы, +логотип на той же
грани, +наклон; комбо двигает bbox наружу, рёбра остаются скруглёнными) и все ЧЕТЫРЕ полых/бортовых
билдера (плоский полый, скруглённый полый, острый-борт, плоский борт, скруглённый борт) —
водонепроницаемо. Прото инсет-купола на реальном `buildAsymRoundedBox` — 5/5. Регресс — вся батарея
`./run-all.sh` 294/294.

## Этот проход: скругление стенок (per-face bulge) + совместимость с логотипами

Добавлен ВЫГИБ граней сплошного куба — отдельно от скругления углов (рёбер). Каждая из 6 граней
получает знаковый купол: `+` наружу («бочка»), `−` внутрь («подушка»), 0 — плоско. Ключевое
наблюдение: выгиб грани = ровно тот же per-face displacement, на котором едут логотипы, только на
всю грань, поэтому весь механизм переиспользован и **совместимость с логотипами получается сложением
смещений** (не «или-или»).

- Профиль: сепарабельный купол `amp·B(u/uH)·B(v/vH)`, `B` — приподнятый косинус (`(1+cos πx)/2`),
  1 в центре, **0 на всех четырёх рёбрах грани**. Именно 0-на-рёбрах гарантирует, что швы с соседними
  гранями и с существующим скруглением углов не рвутся — поэтому выбран купол (а не «ствол»/цилиндр
  по одной оси, который сдвигал бы общие рёбра и потребовал бы переделки соседей).
- Новые функции: `WALL_BULGE_FACES`/`WALL_BULGE_KEYS`, `wallBulgeProfile`, `wallBulgeAmounts`
  (клампы: вогнутое `≥ −0.45·размер_по_нормали`, чтобы противоположные стенки не пересеклись),
  `buildWallBulgeDispFns`, `computeWallBulgeZones` (полноосевые зоны → мелкая сетка на всю грань),
  `mergeDispFnsAdd`/`mergeZones`. Маршрут `buildTrisForShape` (сплошной): при `hasWallBulge` →
  `buildBoxWithLogos` с суммой (лого+выгиб) dispFns и объединёнными зонами; наклон применяется внутри.
- UI: 6 знаковых полей «Скругление стенки ±X/±Z», «верха +Y», «дна −Y» (−150..150 мм). Детализация
  купола берётся из «Детализация (сплошной куб)» (`logoResolution`).
- **Границы (на будущее):** только сплошной куб; в приоритете над скруглением углов (одновременно не
  сочетаются — нужен купол с 0 на инсет-границе филлета); полый/борт — следующая фаза (как было с
  логотипами и филлетом).

Проверка: `test_wall_bulge.js` 24/24 через реальный `buildTrisForShape` — водонепроницаемость
(выпукло/вогнуто/смешанные знаки/не-куб/экстрим-кламп), направление знака (выпуклое двигает bbox
наружу, вогнутое оставляет рёбра на месте), выгиб+наклон, выгиб+логотип на одной грани (пик =
hw+выгиб+глубина лого — рельеф действительно складывается), приоритет над филлетом, регресс «без
выгиба = 12 треугольников». Прото на реальном `buildBoxWithLogos` — 10/10. Регресс — вся батарея
`./run-all.sh` 277/277.

## Этот проход: применение параметров по кнопке «Подтвердить» (staged parameters)

Раньше любая правка параметра/логотипа пересобирала модель на лету (`regenerateSoon`/
`regenerateDebounced` прямо в обработчиках). На скруглённом полом контейнере со всеми
скруглениями это ~30–125k треугольников на каждый ребилд → на мобильном UI зависал при
изменении размеров. Теперь правки **стейджатся**, а к модели применяются только по кнопке.

- Новая машина состояния (после `flushPendingRegenerate`): `pendingChanges` + `markPending()` /
  `applyPending()` (кнопка, через `regenerateSoon`) / `applyPendingNow()` (экспорт, синхронно через
  `flushPendingRegenerate`) / `updateApplyButton()`.
- Все обработчики параметров, чекбоксов (`hollow`/`rim`/…), логотипов (add/remove/поля карточек) и
  обеих «детализаций» теперь зовут `markPending()` вместо перегенерации. Клампы полей на blur и
  перерисовка карточек логотипов остаются живыми — на модель они не влияют.
- **Поворот (rx/ry/rz) оставлен живым** — это дешёвый путь `rotateOnly` (без пересборки геометрии)
  и способ осматривать модель; в задачу «не применять параметры на лету» он не входит.
- UI: липкая кнопка `#btn-apply` вверху панели (подсвечена красным, пока есть неприменённые правки;
  «Изменения применены» и disabled, когда нечего применять) + подсказка. Экспорт STL сначала
  применяет накопленное (`applyPendingNow`), чтобы файл соответствовал полям; «сбросить вид» больше
  НЕ пересобирает (чистый сброс камеры).

Проверка: новый `test_apply_button.js` 15/15 (стейджинг не пересобирает модель; кнопка/экспорт
применяют ровно один раз; состояние кнопки и `pendingChanges`). `stub_preamble.js` дополнен
`classList`/`disabled`. Регресс — вся батарея `./run-all.sh` 253/253.

## Этот проход: сетчатое диагональное дно полого контейнера (садовая решётка)

Дно ПЛОСКОГО полого контейнера превращается в **диагональную решётку** (садовая решётка): чистые
рёбра ±45°, ромбовидные сквозные отверстия, плоский профиль. Флажок `latticeFloor` + `latticeCell`
(шаг ромба), `latticeRib` (толщина ребра), `latticeBorder` (сплошной кант в ячейках).

Итерация: первая версия была «ромб, вписанный в квадратную ячейку» — но это читалось как перфорированная
плита (есть и ортогональные рёбра, плоские узлы). Пользователь показал фото садовой решётки → нужен
ЧИСТО диагональный узор. Переделал билдер (выбор: плоский профиль + чистая диагональ).

Строится на **той же мелкой сетке `xMid×zMid`**, что уже использует дно полости и её стенки
(`midSlice()` включает концы → `xMid = xPositions ∩ [-(hw-t),hw-t]`), поэтому все швы совпадают со
стенками/кольцом без конфликта разбиения. Воксельный подход:
- Каждая мелкая ячейка = СПЛОШНАЯ (часть ребра) или ДЫРА. Принадлежность ребру — в повёрнутых координатах
  `uu=(X+Z)/pitch`, `vv=(X−Z)/pitch`: ячейка сплошная, если в пределах `rib/2` (world) от целой линии `uu`
  или `vv` — т.е. лежит на одном из двух семейств ±45°. Рёбра идут по 45°, сетка осевая → границы падают
  на рёбра ячеек; выше разрешение сборки — глаже диагональ.
- **Сплошная ячейка**: верхний квад (дно полости, +Y) + нижний квад (внешнее дно, −Y) + вертикальные
  стенки-рёбра там, где сосед — дыра (нормаль внутрь дыры). Кант (`border`, минимум 1) — сплошной, дыры
  не доходят до стенок; «снаружи региона» = сплошное, поэтому границу закрывает сам контейнер (без стенки
  по периметру решётки).
- **Устранение защемлений (pinch)**: тонкое ребро = ячейки, касающиеся только углом (2×2 шахматка) —
  негерметично (вертикальное ребро используется 4 раза). Проход заливает одну дыру каждой шахматки —
  мостит их (и скругляет узлы пересечения). Монотонно (только добавляет сплошное) → сходится.
- **Внешнее дно** эмитится как **КОЛЬЦО** (ячейки `xP×zP` вне внутреннего прямоугольника); плоская
  полнофутпринтная грань `−Y` больше не строится.

Обмотка берётся на ПЛОСКИХ координатах (`orient` до `taperFn`), затем наклон — швы совпадают с
наклонёнными стенками/кольцом. **`pitch`/`rib` заданы в мм и НЕ зависят от разрешения** — размер
отверстия расцеплён с детализацией стенок/логотипов (стенки на постоянном `logoResolutionHollow`).
Решётка не несёт рельеф — логотипы на `−Y`/`−Y-изнутри` не рисуются, на стенках работают как обычно.

Проверка (Node, реальный код через `buildTrisForShape`): `test_lattice_floor.js` 26/26 —
водонепроницаемо на размерах/шаге/ребре/канте, **тонкое ребро (путь pinch-removal)**, наклон стенок,
логотипы на стенках, регресс «полый без решётки», расцепление (число треугольников стенок побайтно
одинаково при разном `cell`). Прототипы: `hollow-diagnet.js` 11/11 (диагональ+pinch+наклон),
`diag-net-v3.js` 8/8 (автономная плита), ранние `hollow-lattice-v2.js` 17/17 / `v1` 18/18 (ромб-в-квадрате,
заменён). Браузер: полый 50×36×50, шаг 9, ребро 1.6 — watertight (0 открытых/битых), маска дна = чистая
диагональная решётка (ромбы, узлы-пересечения, сплошной кант).

**Ограничения (на будущее):** решётка пока только на ПЛОСКОМ полом (не на скруглённом — там дно строит
другой билдер) и не на борте/лотке. Плоский профиль ребра (не круглый). Диагональ имеет мелкую «лесенку»
по краям (шаг = мелкая ячейка сетки; чем выше `Детализация полый контейнер`, тем глаже).

## Этот проход: логотипы на скруглённом ПОЛОМ/БОРТЕ (Фаза 2)

Логотипы поехали на скруглённый полый контейнер и борт: **рельеф на внешних стенках + дно полости/
кармана**. Переиспользована машинерия Фазы 1:

- `buildAsymBoxSurfaces` получил `dispFnsByFace`: раздаёт логотип-dispFn по граням оболочки. Для
  ВНУТРЕННЕЙ оболочки (flip=true) смещение инвертируется — +глубина стоит ВЫПУКЛО в полость (как у
  внешних стенок). Грани строятся в ЛОКАЛЬНЫХ Y-координатах (yOffset применяется после), а фрейм
  логотипа — в world, поэтому перед проекцией к вершине добавляется yOffset.
- `buildRoundedHollow` / `buildSharpRimHollow` приняли `dispFnsOuter`/`dispFnsInner` + зоны + logoRes.
  Зоны загущают общие `xP/zP` (world) и `yPo` (для sharp — со сдвигом на −ciyO, т.к. оболочка поднята).
  Общий хелпер `addLogoZonesToAxis` (вынесен из `buildAsymRoundedBox`).
- Маршрут `buildTrisForShape` (hollow/rim): условие скругления больше не требует «нет логотипов»;
  `splitHollowLogoDispFns()` делит логотипы на внешние грани (→ outer) и дно полости `-Y-inner`
  (→ inner как грань `-Y`), строит плоские зоны. Плоский полый (без скругления) с логотипами
  по-прежнему идёт в `buildHollowBox`/`buildRimBox`.
- **Кламп** (`faceFilletInsets` расширен на hollow/rim): внешние стенки клампятся по внешним радиусам
  (+ верхний инсет: rolled lip `min(cLip,0.9t)` или у sharp — до `y=hh`), дно полости — по внутренним
  (`max(iFloor, iVert)`). `LOGO_CLAMP_KEYS` дополнен внутренними радиусами, толщиной стенки, высотой борта.

Проверка: `test_fillet_logo_hollow.js` 19/19 (стенки/дно, полый+борт, гравировка, поворот, rolled/sharp,
стена+дно, кламп oversize на всех гранях). Регрессы: fillet-logo(solid) 16/16, rounded-fillet 14/14,
rim 22/22, hollow-taper 19/19, hollow-inner-logo 12/12, resolution 9/9, e2e 21/21, logo-zone 43/43.
Браузер: скруглённый полый куб 60 + кольцо-логотип на внешней стенке — watertight, рельеф в плоской
зоне между скруглёнными рёбрами.

**Итог по совместимости скругления и логотипов:** сплошной куб, полый контейнер, борт/лоток — во всех
логотипы совместимы со скруглением. Наклон+лого+скругление: рельеф слегка тянется наклоном (принято).

## Этот проход: совместимость скругления и логотипов (Фаза 1 — сплошной куб)

Раньше скругление и логотипы были взаимоисключающими. Теперь **на сплошном скруглённом кубе логотип
рельефом ложится на плоские грани**. Как:

- `buildFaceAsymFromAxis` получил параметр `dispFn`: вершины плоской грани смещаются вдоль (постоянной)
  нормали грани на значение хитмапа. Обмотка берётся от ПЛОСКОГО треугольника (orient до смещения), потом
  смещение — стенки рельефа наследуют согласованную обмотку. Общая вершина сетки получает одинаковое
  смещение в каждой ячейке (dispFn — чистая функция u,v) → рельеф герметичен.
- `buildAsymRoundedBox` принимает `dispFnsByFace` + `zonesByAxis` + `logoRes`: в зоне логотипа общие
  массивы позиций дозагущаются (`logoRes` подразбиений на зону, как sharp-box `buildLogoAwareAxisPositions`),
  каждой грани раздаётся её dispFn.
- **Кламп** (`faceFilletInsets`): плоская зона грани сжата на радиус смежного скругления; логотип
  клампится внутрь (у краёв dispFn=0 → шов с цилиндрами рёбер цел). Перекламп всех логотипов при
  изменении размеров/радиусов/режима (`LOGO_CLAMP_KEYS` + слушатели инпутов).
- Логотип-фрейм в fillet-пути строится во ФЛЭТ-пространстве (identity taper), т.к. наклон применяется
  ПОСЛЕ (`taperBoxTris`) ко всему мешу. При наклон+лого+скругление рельеф слегка растягивается наклоном
  (принято); без наклона — точно.

Маршрут `buildTrisForShape` (сплошной куб): логотипы без скругления → старый `buildBoxWithLogos`;
логотипы + скругление → `buildAsymRoundedBox` с dispFns (uniform радиусы тоже идут сюда, т.к.
`genRoundedBox` рельеф не несёт). Проверка: `test_fillet_logo.js` 16/16 (гравировка, асим/большой/uniform
fillet, все грани, поворот, наклон, 2 лого, кламп размера/позиции, перекламп после увеличения радиуса);
регрессы `test_logo_zone` 43/43, `e2e` 21/21, `rounded_fillet` 14/14, `rim` 22/22. Браузер: скруглённый
куб r8 + кольцо-логотип на +Z — watertight, рельеф на плоской грани, рёбра скруглены.

**Не сделано (Фаза 2):** логотипы на скруглённом ПОЛОМ/БОРТЕ (внешние стенки + дно полости) —
там логотип пока по-прежнему сбрасывает скругление.

## Этот проход: ОСТРЫЙ плоский внутренний кант (без шпилей и без закругления)

Предыдущий проход убирал шпили, ЗАКРУГЛЯЯ нулевой кант (0 → радиус вертикали). Пользователь хотел
другое: **острый плоский кант** — проём со скруглёнными вертикальными углами, но ОСТРЫМ верхним ребром,
без конусов. Chamfer-машинерия так не умеет (она привязывает форму проёма к филлету), поэтому добавлен
отдельный билдер `buildSharpRimHollow` (порт из `scratchpad/asym-sharp-rim-v8.js`):

- **Внутренняя полость**: полностью скруглённый бокс, чей ВЕРХ ПОДНЯТ на `iv` (радиус вертикали) — так
  верхний филлет оказывается ВЫШЕ плоскости проёма `y=hh`; затем все треугольники выше `hh` срезаются.
  Срез оставляет вертикальные углы постоянного радиуса (дуги) и прямые верхние рёбра стенок →
  проём = скруглённо-прямоугольный с ОСТРЫМ ребром.
- **Борт**: явное плоское кольцо между граничной петлёй внешней оболочки и петлёй проёма полости. Обе
  петли — реальные извлечённые рёбра (`sharpTopBoundaryLoops`), их вершины общие → швы герметичны.
  Триангуляция кольца: угловая развёртка (`sharpAnnulusSweep`), при самопересечении — фолбэк на
  лофт «короткой диагонали» с перебором старта (`sharpAnnulusLoft`). Развёртка и лофт всегда
  герметичны (переиспользуют вершины петель без «моста»), различаются лишь самопересечением плоского
  кольца — их проблемные случаи разные, объединение закрывает практически всё.

Маршрут в `buildTrisForShape` (полый и борт): `iLip>0` → `buildRoundedHollow` (закатанный кант, как
раньше); `iLip==0` → `buildSharpRimHollow` (острый). UI-подпись: «(0=острый плоский)».

**Доработка (убраны «рожки» на верхних углах):** в остром режиме ВНЕШНЯЯ оболочка тоже получила
острый-плоский верх (тот же raise-truncate). Раньше внешний закатанный кант зажимался до `0.9·t`
(на тонкой стенке — крошечный), а вертикали 10мм на верхнем углу сужались к нему → выступающие
«рожки». Теперь внешние вертикали держат полный радиус до обода (без сужения), верх — острое ребро,
углы гладкие. Два клампа для валидности: (1) `iv ≤ глубина_полости·0.95` — иначе поднятый бокс
полувысоты `(iv+глубина)/2` не вмещает вертикальный филлет (мелкий трей вырождался); (2) `ov ≤ iv+t`
— иначе большой внешний угловой радиус прорезает за стенку («съедает» её на углу). Для совпадающих
радиусов (обычный случай) оба клампа — no-op. Стресс поднялся до **2000/2000** (watertight И
ориентация). Итог: острый режим = весь верх острый (оба ребра); скруглённый (`iLip>0`) = оба канта
закатаны (внешний `filletTop`).

Стресс `asym-sharp-rim-v8.js` (продукт-клампы): **2000/2000 водонепроницаемы**, 1998/2000 ещё и
ориентационно-согласованы (2 — патология «тонкая стенка + огромные радиусы», герметичны, лишь пара
фасеток борта). Регресс на извлечённом из HTML коде: `test_rounded_fillet.js` 14/14 (кант=0 идёт
острым путём), rim 22/22, hollow-taper 19/19, resolution 9/9, inner-logo 12/12. Браузер: точный кейс
юзера (40³ борт, дно=вертик=10, кант=0) — острый плоский проём, шпилей нет, watertight; сплошной/
полый/борт/тапер — все watertight.

**Замечание:** при рассинхроне первый мой рендер показывал устаревший кадр (`regenerate()` меняет
геометрию синхронно, но canvas перерисовывается на следующем кадре) — не баг билдера.

## Этот проход: убраны «шпили» в кармане + защита от хрупких суб-филлетов

Внутренний кант борта/полости при значении 0 давал конусы-«шпили»: если внутренняя вертикаль
скруглена (>0), а кант = 0, то в верхнем углу `cornerMin = min(vert, 0, 0) = 0`, и вертикальный
цилиндр полости сходится в точку на плоскости борта — тем выше, чем мельче карман. Правка в
`buildRoundedHollow`: **нулевой кант откатывается к радиусу внутренней вертикали** (закатанный
внутренний кант, зеркально закатанному ВНЕШНЕМУ), с клампом к полуглубине кармана `ih/2·0.98`,
чтобы кант не уходил глубже полости. Порог `LIP_EPS=0.1`: если откат вышел бы суб-0.1 (исчезающий),
кант остаётся острым. UI-подпись поля: «(0=как вертикаль)».

Отдельно всплыл ПРЕ-существующий баг (не от этой правки, воспроизвёлся и на старом билдере):
суб-микронная внутренняя вертикаль (напр. 0.0001 мм после клампа) строит вырожденный филлет-осколок,
который `manifoldCheck` при dec5 честно ловит как non-manifold (open+bad). Пользователь такое не
задаёт (шаг UI 0.5), но клампы теоретически могут дожать до этого. Правка в `asymRadiiFromGroups`:
любой радиус группы `< 0.1` после клампа **снапится в 0** (острое ребро вместо хрупкого осколка).
Покрывает сплошной куб, полый и борт, внешнюю и внутреннюю оболочки.

Прототип `scratchpad/asym-hollow-v7.js`: 17 directed + рандом-стресс **2000/2000** водонепроницаемы
(с продукт-клампом, кант принудительно 0 в половине случаев). Регресс на извлечённом из HTML коде:
rim 22/22, hollow-taper 19/19, hollow-resolution 9/9, hollow-inner-logo 12/12, e2e 21/21,
logo-zone 43/43, **новый `test_rounded_fillet.js` 14/14** (ранее путь скругления Node-тестами не
покрывался). В браузере: точный кейс юзера (40³ борт, дно=вертик=10, кант=0) — watertight, карман
чисто скруглён, шпилей нет; сплошной/полый/борт — все watertight.

**Не сделано (на будущее):** острый плоский кант БЕЗ шпилей (постоянный радиус вертикали + плоский
клип + скруглённо-прямоугольный борт) — это крупная переделка ядра asym-билдера; текущее решение —
закатывать кант (как и внешний). Следующий запрошенный шаг: совместимость скругления с логотипами.

## Этот проход: скругление режима «борт»/лоток (rounded tray)

Режим «борт» получил полное скругление. Ключевое наблюдение: **трей топологически = полый
корпус**, только внутренняя полость МЕЛКАЯ (дно кармана на `hh−rimHeight`, а не `−hh+t`;
материал под карманом сплошной). Меш идентичен по структуре (внешняя оболочка + полость-карман +
борт), поэтому `buildRoundedHollow` параметризован глубиной кармана `pocketDepth`: задан → трей
(`ih=pocketDepth`, дно на `hh−pocketDepth`), не задан → полый (`ih=h−t`, дно на `−hh+t`). Ветка
`rim` в `buildTrisForShape` при заданном скруглении и без логотипов зовёт `buildRoundedHollow`
с `pocketDepth=rimHeight`; внутренние радиусы клампятся относительно `rh/2`. Прототип
`scratchpad/asym-tray-v6.js`: стресс 2000/2000 герметичны. Браузер: sharp/outer/full-both-lips/
deep-pocket/taper — все watertight, рендер верный (мелкий скруглённый карман в сплошном боксе).

**Итог по асимметричному скруглению:** сплошной куб (3 группы), полый (внешн.+внутр.+оба канта)
и борт/лоток — все скруглены и водонепроницаемы. Взаимоисключение с логотипами сохраняется.

## Этот проход: скруглённый ВНУТРЕННИЙ кант борта

Полость получила скруглённый верхний кант — исчезли острые «шпили» на внутренних верхних
углах. Ключевой ход: внутренняя полость = вывернутый ВЫПУКЛЫЙ бокс-«пробка», поэтому достаточно
дать внутреннему боксу `rTopIn>0` (внутренний бокс перестал занулять верх) — новой вогнутой
машинерии не нужно. Борт стал КОЛЬЦОМ: `buildAsymRimAnnulus` = внешняя +Y грань минус
треугольники, чей центроид внутри внутреннего проёма (`innerMin` из `asymTopMargins`). Т.к.
общие массивы синхронизированы для обеих оболочек, граница внутреннего проёма (прямые + угловые
шевроны) ложится на рёбра этой грани → центроид-тест режет чисто, внутренний край борта точно
совпадает со скруглённым верхним кантом внутренних стенок. Кламп: внутренний кант `≤` внутр.
вертикали (иначе угол проёма управляется меньшей вертикалью, а борт читает больший инсет →
рассинхрон). UI: поле «скругление внутр. канта» (`filletInnerLip`). Прототип
`scratchpad/asym-hollow-v5.js`: стресс 2000/2000 герметичны. Браузер: inner lip / full both lips
/ innerlip>vert-кламп / taper — все watertight, внутренних «шпилей» нет.

**Не сделано:** режим «борт»/лоток (`buildRimBox`) со скруглением.

## Этот проход: скруглённый ВНЕШНИЙ кант борта (rolled outer lip)

Полый режим получил скруглённый верхний кант снаружи — исчезли острые «шпили» на верхних
внешних углах (раньше вертикальная фаска сходила в острый верх при `rTop=0`). Теперь внешний
бокс сохраняет верхние радиусы (`filletTop` → радиус канта), строит верхние цилиндры/сферы, а
борт-рамка = верхняя грань внешнего бокса (`buildAsymRimFaceWithHole`) С ВЫРЕЗОМ под полость
(треугольники с центроидом внутри `[±ihw,±ihd]` отбрасываются — вырез чистый, т.к. границы
на линиях сетки). Геометрическое условие: кант `< t` (иначе фаска нависает над полостью,
`hw−rTopOut < ihw`, борт исчезает) → кламп `rTopOut ≤ 0.9·t`. Прототип `scratchpad/asym-hollow-v4.js`:
стресс 2000/2000 герметичны. В браузере: lip / lip+внешн+внутр / lip>t-кламп / taper — все watertight.

**Не сделано:** внутренний вогнутый кант (внутренние верхние углы всё ещё дают «шпили» —
полость острая сверху); режим «борт»/лоток (`buildRimBox`) со скруглением.

## Этот проход: скругление полого контейнера (внешние + внутренние рёбра)

Полый режим получил скругление, интегрировано в HTML. Композиция переиспользует asym-примитивы:
внешняя оболочка (скруглённые низ+вертикали, открытый острый верх) + внутренняя полость
(скруглённые пол+вертикали, вывернутые нормали, дно на `−hh+t`) делят ОДИН общий массив X/Z
(+ по-оболочечные Y), плоская борт-рамка при `y=hh` их соединяет. Ключевое: diagonal-sync
обеих радиусных схем гоняется до общей неподвижной точки (внутренняя добавляет точки в общий
X/Z → внешнюю надо пересинхронить, и наоборот).

Новые функции в `<script>`: `buildRoundedHollow`, `buildAsymBoxSurfaces`, `buildAsymFlatRim`,
`asymSyncDiagonalPair`/`asymEdgeSpecs`/`asymAllRadii`. UI: 2 поля «скругление дна/вертик.
полости». Ветка `buildTrisForShape` для `hollow`: если задано скругление и нет логотипов →
`buildRoundedHollow` (клампинг радиусов через `asymRadiiFromGroups`, наклон поверх), иначе старый
`buildHollowBox`. Прототип `scratchpad/asym-hollow-v2.js`: стресс 2000/2000 герметичны (5 «фейлов» —
`volume≤0` на «тонкая стенка + огромный радиус», меш при этом watertight; в HTML отсекает clamp).
Проверка в браузере: plain/outer/inner/taper/big-clamp — все watertight, рендер верный.

**Не сделано:** скруглённый кант борта (rolled lip — вертикальная фаска сходит в острый верх),
режим «борт»/лоток (`buildRimBox`) со скруглением.

## Этот проход: асимметричное скругление (12 радиусов) доведено до водонепроницаемости

Незавершённый эксперимент `asym-12-radius.js` (12 независимых радиусов рёбер) доработан до
герметичного меша на всех входных данных. Три точечные правки, все — в согласовании кусков,
не в математике (подробный разбор в `NOTES.md`):

1. `buildFaceAsymFromAxis` переписана: сетка по общим массивам осей + клип каждой ячейки по 4
   «margin»-функциям `u ≤ uHalf − min(ownR, distToCorner₊, distToCorner₋)`, совпадающим
   с касательной кромкой цилиндра (убрало щели/выступы и T-стыки старой сшивки).
2. `syncDiagonal`: исправлена константа диагонали (`vHalf − uHalf`, не зависит от радиуса —
   старая была неверна для углов со смешанными знаками) + итерация до неподвижной точки
   (три семейства осей теперь дозакрывают друг друга).
3. `buildEdgeCylinderFromAxis`: пропуск треугольников нулевой площади → у острого угла
   (`cornerMin = 0`) кромка сходится чистым веером вместо вырожденных квадов.

Проверка: `test-uniform-and-mixed.js` 16/16, `test-3radius-variant.js` 12/12, рандомный стресс
2000/2000 (радиусы до 30, resolution 5–16, 20 % нулевых рёбер) при допуске склейки 5 знаков.

**Интегрировано в HTML.** Функции портированы в `<script>` (vec-хелперы переиспользованы);
в UI куба — 3 группы «верх/низ/вертикали» (`asymRadiiFromGroups` → полный 12-радиусный R),
ветка `buildTrisForShape` выбирает asym при различии групп, иначе старый `genRoundedBox`; наклон
применяется поверх. Клампинг радиусов `K=0.98` (на пределе плоская грань вырождается). Проверка
в браузере (рендер + `manifoldCheck` в странице, все пути watertight) + near-limit стресс 4000/4000.

## Сделано

### Основа
Параметрический STL-генератор, самодостаточный HTML/CSS/JS. Куб — единственная фигура. Три взаимоисключающих режима дна/верха: сплошной, полый контейнер (`hollow`), борт/лоток (`rim`). Наклон стенок (8 углов) работает во всех трёх. Логотипы (до 18, зональная сетка на сплошном кубе) работают во всех трёх, включая грань «−Y изнутри» (общая для hollow/rim). `manifoldCheck` пропускается выше 120k треугольников, debounce на ввод, отдельная детализация для сплошного/полого-и-борта.

## Этот проход: откат отверстий для магнитов

Отверстия для магнитов (запрошены и реализованы в предыдущем проходе — 4 круглых углубления у углов нижней грани, работали во всех 3 режимах) откачены полностью по просьбе.

Откат сделан вручную (правки точечные, не через git — в песочнице не было репозитория), затрагивал:
- `buildFaceFromPositionsTapered` — убран параметр `excludeSquares` и логика исключения квадов.
- Полностью удалён блок MAGNET HOLES: `magnetHoleAxisBreakpoints`, `insertExtraBreakpoints`, `buildMagnetHoleFromGrid`, `clampMagnetHoleParams`, `computeMagnetHoles`.
- `buildBoxWithLogos`, `buildHollowBox`, `buildRimBox` — убран параметр `magnetHoles` и вся связанная логика (вставка breakpoints, exclude-squares для грани −Y, добавление геометрии отверстий).
- `buildTrisForShape` — убран расчёт `magnetHoleData` и его передача во все три диспетчерские ветки; условие для зонального пути сплошного куба вернулось к `logos.length > 0` (без `|| magnetHoleData.length > 0`).
- `SHAPE_PARAMS.box` — убраны записи `magnetHoles`/`magnetDiameter`/`magnetDepth`/`magnetInset`.
- Текст заметки в UI — убрано упоминание отверстий для магнитов.

### Проверка отката
После отката файл побайтово идентичен версии до начала работы над магнитами (сверил md5: `af4f657ac5cbe95ef6fb1cedaa084cc4` — точное совпадение с хэшем, зафиксированным перед стартом той фичи). Полный регресс: `test_logo_zone.js` 43/43, `test_debounce_flow.js` 9/9, `test_hollow_inner_logo.js` 12/12, `test_hollow_resolution.js` 9/9, `test_hollow_taper.js` 19/19, `test_rim_box.js` 22/22, внутренний e2e (не в поставке) 21/21 — итого 135/135, без падений.

`test_magnet_holes.js` удалён из поставки (тестировал уже откаченный код, ссылки на несуществующие функции).

## Где остановились

Откат сделан и проверен. Файл в том же состоянии, что после реализации борта/лотка, до отверстий для магнитов.

### Что осталось (на будущее, не блокирует)
- Известный edge-case: экстремальный наклон + логотип у самого края сплошного куба может дать негерметичный шов.
- ~~Зональной сетки у полого/борта всё ещё нет.~~ Сделано позже (см. запись «зональная сетка на плоском полом/борте»).
- ~~Web Worker для полностью неблокирующей генерации.~~ Тяжёлую проверку герметичности (`manifoldCheck`)
  вынесли в Web Worker (см. запись «Web Worker для проверки герметичности»). Саму генерацию оставили на
  главном потоке осознанно: она несовместима с воркером без слома no-build-одного-файла и Node-харнесса.

## Файлы

Плоская раскладка в корне репозитория. Все тесты гоняются одной командой `./run-all.sh`
(522 проверки в 18 файлах, извлекает `<script>` из HTML и подставляет стабы; ручная
процедура — в шапке каждого теста).

| Файл | Описание |
|------|------|
| parametric-stl-generator.html | Весь инструмент (открыть в браузере) |
| run-all.sh | Прогон всей батареи тестов, per-file + общий итог |
| README.md / PROGRESS.md / NOTES.md | Обзор / журнал разработки / разбор 12-радиусного скругления |
| asym-12-radius.js | Автономный порт 12-радиусного билдера (источник in-page версии) |
| stub_preamble.js | DOM/rAF-стабы для запуска кода страницы под Node |
| minimal-repro-missing-breakpoint.js | Минимальный repro бага сшивки грани (для справки) |
| test_logo_zone.js | 43/43, самодостаточен (`node test_logo_zone.js`) |
| test_lattice_floor.js | 46/46, сетчатое диагональное дно |
| test_squircle.js | 123/123, squircle / супереллипсоид (+ логотипы, cube-sphere/картез. крышки; + полый/борт; раздельные верх/низ) |
| test_holes.js | 27/27, сквозные цилиндрические отверстия на плоском сплошном кубе (+ гейт диспетчера, + защита от перекрытия отверстий на одной оси) |
| test_wall_bulge.js | 53/53, скругление стенок: +логотипы, +углы, полый/борт, полость повторяет выгиб |
| test_multi_model.js | 33/33, несколько моделей (сборка): снапшот/восстановление, merge-экспорт, union-bbox |
| test_rim_box.js | 22/22, режим борт/лоток |
| test_e2e_full.js | 21/21, сквозной прогон `buildTrisForShape` |
| test_hollow_taper.js | 19/19, наклон стенок полого |
| test_fillet_logo_hollow.js | 21/21, логотипы на скруглённом полом/борте |
| test_fillet_logo.js | 16/16, логотипы на скруглённом сплошном кубе |
| test-uniform-and-mixed.js | 16/16, asym-скругление (uniform + смешанное) |
| test_rounded_fillet.js | 20/20, скруглённый кант полого/борта + `filletAxisRes` (плотность осевой сетки) |
| test_hollow_inner_logo.js | 18/18, логотип на грани «−Y изнутри» + зональная сетка плоского полого/борта |
| test-3radius-variant.js | 12/12, asym из 3 групп (верх/низ/вертикали) |
| test_apply_button.js | 15/15, стейджинг параметров + кнопка «Подтвердить» (стаб-счётчики) |
| test_debounce_flow.js | 9/9, debounce/токен генерации (стаб-счётчики) |
| test_hollow_resolution.js | 8/8, детализация полого без «call stack exceeded» |
